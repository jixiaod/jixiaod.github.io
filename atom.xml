<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>百作坊</title>
  <subtitle>100dos.com</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://blog.100dos.com/"/>
  <updated>2017-08-31T02:52:15.076Z</updated>
  <id>http://blog.100dos.com/</id>
  
  <author>
    <name>小弟</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Redis之父：10x程序员应该具备哪些素质</title>
    <link href="http://blog.100dos.com/2017/08/31/redis-father-10x/"/>
    <id>http://blog.100dos.com/2017/08/31/redis-father-10x/</id>
    <published>2017-08-31T02:43:48.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p>原文链接: <a href="http://www.infoq.com/cn/news/2017/04/Redis-father-10x" target="_blank" rel="external">http://www.infoq.com/cn/news/2017/04/Redis-father-10x</a></p>
<p>Fred Brooks（《人月神话》的作者）最早在他的论文“没有银弹——软件工程的本质和偶然性（No Silver Bullet - Essence and Accidents of Software Engineering）”中提出了“10x程序员”的概念。技术社区对于这个概念呈现出两级分化的观点。Redis之父Salvatore Sanfilippo（antirez）列出了9种特质，他认为，如果一个程序员同时具备了这9种特质，那么就可以说他是一个10x程序员。以下内容已获得antirez的翻译授权，查看英文原文<a href="http://antirez.com/news/112" target="_blank" rel="external">The mythical 10x programmer</a>。</p>
<p>一个10x程序员，在相同条件下，可以完成十倍于普通程序员的工作。这里所说的“普通程序员”，是指那些能够胜任自己工作的程序员，只是他们不具备10x程序员的神奇能力。普通程序员代表了这个领域所有专业程序员的平均水准。</p>
<p>对于是否存在10x程序员这种“神兽”，编程社区的观点呈现出两级分化：有人认为根本不存在所谓的10x程序员，有人则认为不仅存在10x程序员，如果找对了门路，甚至能找到100x的程序员。</p>
<p>如果说编程是一项“线性”的工作，那么很明显，10x程序员是不可能存在的。一个跑步运动员怎么可能跑得比另一个快上十倍？在相同时间内，一个建筑工人建造的东西怎么可能是其他人的十倍？不过，编程是一项很特别的设计工作。虽然程序员可能不会参与程序的架构设计，但程序的实现仍然涉及到一些设计工作，而这部分需要程序员去完成。</p>
<p>如果说程序的设计和实现不是线性的，那么在我看来，经验、编码能力、知识和去伪存真的能力就不仅仅是线性的优势，它们在编程过程中相互交织，成倍地发挥效能。当然，如果程序员能够同时胜任设计和实现工作，那么这种现象就尤为明显。任务越是具有目标导向性，程序员就越是能够以更少的付出达成相同的目标，从而体现10x程序员的潜在能力。如果手头的工作很死板，而且限定了可使用的工具和实现方式，那么10x程序员事半功倍的能力就会大打折扣。不过，在不改变大前提的情况下，程序员仍然可能通过局部的设计优化来改进工作，包括在项目的某些部分不按常理出牌。所以，他们可以少付出很多却能达成几乎相同的目标。</p>
<p>在我的二十年程序员生涯中，我与其他程序员一起工作，作为同事，或者由我指导他们达成目标，为Redis和其他项目贡献代码补丁。在工作过程中，我仔细观察他们。与此同时，有很多人说我是一个高能的程序员。不过我不认为自己是一个工作狂，我只是编码速度比较快而已。</p>
<p>下面列出了我认为可以用于区分程序员生产力高低的重要特质。</p>
<p>编程裸技能：完成子任务</p>
<p>从处理编程子任务可以看出一个程序员的短板和长处，比如实现一个函数或者一个算法。但从我的经验来看，擅于应用基本的编程技能来高效完成任务的程序员并非如人们所想得那样普遍存在。有时候，团队里有些不是很称职的程序员，他们甚至不知道该怎么写一个简单的排序算法，但比起那些看似称职却缺乏实战经验的程序员，这些不称职的程序员却能完成更多的工作。</p>
<p>经验：模式匹配</p>
<p>我认为，经验就是一系列解决方案，它们已经被证实可以用于处理一些重复性的任务。经验老道的程序员知道该如何处理各种子任务，这样不但省掉了很多设计工作，而且避免了很多设计错误，而设计错误是简洁性最大的敌人。</p>
<p>专注：实际时间和假设时间</p>
<p>花在编码上的时间不仅要看数量，也要看质量。造成注意力不集中的因素既有内部的，也有外部的。内部的因素包括拖延、对手头的项目不感兴趣（一个人总是做不好自己不喜欢的事情）、缺乏练习、缺乏睡眠。外部因素包括频繁的会议、不固定的工作环境、同事的打扰，等等。集中注意力和避免被打扰对于提高编程效率来说是至关重要的。有时候，为了集中注意力，需要采取一些极端的手段。例如，我会时不时地查看邮件，但大部分邮件先不做回复。</p>
<p>设计权衡：用5%换取90%</p>
<p>项目的非根本性目标在很大程度上导致了设计的复杂性，或者导致无法达成其他更重要的目标，因为根本性功能和非根本性功能在设计上存在竞争关系。如果意识不到这点，复杂性就会随之而来。实现全面的设计并不是件轻而易举的事，付出与回报之间不能通过简单的比例来衡量。对于设计者来说，意识到这一点是很重要的。如果项目要最大化产出，那么就要把精力集中在重要的事情上，并在合理的时间内完成。例如，在设计Disque（一个分布式消息队列）时，我发现对消息进行排序之后，项目的其他方面就会得到实质性的改进：可用性、查询和客户端交互、简洁性和性能。</p>
<p>简洁性</p>
<p>简洁性是成败之间最为明显的分界点。理解复杂性的产生过程有助于理解什么是简洁性。我认为，不愿意做出设计权衡和设计错误的累积是导致复杂性的两个主要因素。</p>
<p>在设计过程中，每次走错一条道，就离最优的方案越来越远。一个初始的设计错误，如果没能被纠正过来，那么可能导致一条道走到黑，最终得到的是一个复杂的系统，而不是对原先系统的重新设计。项目会因此变得更加复杂和低效。</p>
<p>程序员可以在脑子里进行“概念验证”，从大量简单的设计想法中选择可行性最高且最直接的方案，从而达成简洁性。在后续的改进工作中，个人的经验和设计能力开始发挥作用，为子任务找到更加明智的解决方案。</p>
<p>不过，如果系统复杂性不可避免，那么在放弃挣扎之前也要尽量想办法降低系统复杂性，甚至尝试采取完全相反的设计。</p>
<p>完美主义（为了偏袒设计而放弃生产力）</p>
<p>完美主义可以分为两种：一种是追求程序极致性能的工程文化，另一种是个人特质。不管是哪一种完美主义，它们都会对程序员实现快速交付造成阻碍。完美主义和对外部评判的恐惧会导致设计上的偏袒，程序员根据主观的心理因素和无关紧要的衡量参数做出设计决策，却忽略了健壮性、简洁性和及时交付。</p>
<p>知识：理论有益</p>
<p>在处理复杂任务时，具备一些理论方面的知识会对设计产生重要影响，比如数据结构方面的知识、了解计算能力的局限性和一些重要的算法。我们没有必要成为无所不知的超级专家，但至少要知道一些问题的潜在解决方案。例如，在给一个给定流统计单一元素的个数时，我们可以在设计上做出权衡（接受一定程度的错误），并结合概率集合的基数估计（cardinality estimation）算法，避免设计出复杂、缓慢、低内存效能的解决方案。</p>
<p>底层：理解机器原理</p>
<p>程序的很多问题都是源于对计算机工作原理的误解，即使是使用高级语言开发的程序也不外乎如此。这种情况可能导致一个项目需要重新设计和实现，因为项目所使用的工具和算法出现了根本性的错误。精通C语言，知道CPU的工作原理，了解系统内核的行为以及系统调用的实现原理，做到这几点可以挽救你于危难之中。</p>
<p>调试技能</p>
<p>查找和解决bug经常会占用我们大量的时间。查找引起bug的问题根源，在合理的步骤内修复bug，以简单的方式编写包含较少bug的代码，对于程序员来说，做到这几点可以显著提升效率。</p>
<p>一个程序员如果具备了上述几点特质，那么他们的产出将会有10倍的提升，对此我一点也不感到惊讶。综合这些特质，从一个可行的模型开始，实现更简单更好的设计。我认为简洁性就是一种“投机取巧的编程”。简而言之，就是在开发的每个阶段选择性地实现一些功能，以最小化的付出为用户带来最大化的影响。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;原文链接: &lt;a href=&quot;http://www.infoq.com/cn/news/2017/04/Redis-father-10x&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://www.infoq.com/cn/news/2017/04
    
    </summary>
    
      <category term="工作&amp;职场" scheme="http://blog.100dos.com/categories/%E5%B7%A5%E4%BD%9C-%E8%81%8C%E5%9C%BA/"/>
    
    
      <category term="程序员" scheme="http://blog.100dos.com/tags/%E7%A8%8B%E5%BA%8F%E5%91%98/"/>
    
  </entry>
  
  <entry>
    <title>深入理解PHP的ini配置</title>
    <link href="http://blog.100dos.com/2017/08/21/deep-understandint-php-ini-config/"/>
    <id>http://blog.100dos.com/2017/08/21/deep-understandint-php-ini-config/</id>
    <published>2017-08-21T14:50:02.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>好久没更了，今天读到一篇介绍非常详细的文章，转载一下，凑个数。</p>
<p>转自：<a href="http://www.cnblogs.com/driftcloudy/p/4011954.html" target="_blank" rel="external">http://www.cnblogs.com/driftcloudy/p/4011954.html</a></p>
<p>这篇文章不会详细叙述某个ini配置项的用途，这些在手册上已经讲解的面面俱到。我只是想从某个特定的角度去挖掘php的实现机制，会涉及到一些php内核方面的知识:-)</p>
<p>使用php的同学都知道 <code>php.ini</code> 配置的生效会贯穿整个SAPI的生命周期。在一段php脚本的执行过程中，如果手动修改ini配置，是不会启作用的。此时如果无法重启apache或者nginx等，那么就只能显式的在php代码中调用ini_set接口。<br>ini_set是php向我们提供的一个动态修改配置的函数，需要注意的是，利用ini_set所设置的配置与ini文件中设置的配置，其生效的时间范围并不相同。在php脚本执行结束之后，｀ini_set｀的设置便会随即失效。</p>
<p>因此本文打算分两篇，第一篇阐述php.ini配置原理，第二篇讲动态修改php配置。</p>
<p><code>php.ini</code> 的配置大致会涉及到三块数据，<code>configuration_hash</code>，<code>EG(ini_directives)</code>以及<code>PG</code>、<code>BG</code>、<code>PCRE_G</code>、<code>JSON_G</code>、<code>XXX_G</code>等。如果不清楚这三种数据的含义也没有关系，下文会详细解释。</p>
<h3 id="解析INI配置文件"><a href="#解析INI配置文件" class="headerlink" title="解析INI配置文件"></a>解析INI配置文件</h3><p>由于php.ini需要在SAPI过程中一直生效，那么解析ini文件并据此来构建php配置的工作，必定是发生SAPI的一开始。换句话说，也就是必定发生在php的启动过程中。php需要任意一个实际的请求到达之前，其内部已经生成好这些配置。</p>
<p>反映到php的内核，即为<code>php_module_startup</code>函数。</p>
<p><code>php_module_startup</code> 主要负责对php进行启动，通常它会在SAPI开始的时候被调用。btw，还有一个常见的函数是 <code>php_request_startup</code>，它负责将在每个请求到来的时刻进行初始化，<code>php_module_startup</code> 与<code>php_request_startup</code>是两个标识性的动作，<br>不过对他们进行分析并不在本文的探讨范围内。</p>
<p>举个例子，当php挂接在apache下面做一个module，那么apache启动的时候，便会激活所有这些module，其中包括php module。在激活php module时，便会调用到<code>php_module_startup</code>。<code>php_module_startup</code>函数完成了茫茫多的工作，<br>一旦<code>php_module_startup</code>调用结束就意味着，OK，php已经启动，现在可以接受请求并作出响应了。</p>
<p>在php_module_startup函数中，与解析ini文件相关的实现是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* this will read in php.ini, set up the configuration parameters,</span></div><div class="line"><span class="comment">load zend extensions and register php function extensions</span></div><div class="line"><span class="comment">to be loaded later */</span></div><div class="line"><span class="keyword">if</span> (php_init_config(TSRMLS_C) == FAILURE) &#123;</div><div class="line">    <span class="keyword">return</span> FAILURE;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以看到，其实就是调用了php_init_config函数，去完成对ini文件的parse。parse工作主要进行lex&amp;grammar分析，并将ini文件中的key、value键值对提取出来并保存。php.ini的格式很简单，等号左侧为key，右侧为value。每当一对kv被提取出来之后，php将它们存储到哪儿呢？答案就是之前提到的<code>configuration_hash</code>。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">static HashTable configuration_hash;</div></pre></td></tr></table></figure>
<p><code>configuration_hash</code>声明在php_ini.c中，它是一个HashTable类型的数据结构。顾名思义，其实就是张hash表。题外话，在php5.3之前的版本是没法获取<code>configuration_hash</code>的，因为它是php_ini.c文件的一个static的变量。后来php5.3添加了<code>php_ini_get_configuration_hash</code>接口，该接口直接返回<code>&amp;configuration_hash</code>，使 得php各个扩展可以方便的一窥<code>configuration_hash</code>全貌…真是普大喜奔…</p>
<p>注意四点：</p>
<p>第一，<code>php_init_config</code> 不会做除了词法语法以外的任何校验。也就是说，假如我们在ini文件中添加一行 <code>hello=world</code>，只要这是一个格式正确的配置项，那么最终 <code>configuration_hash</code> 中就会包含一个键为hello、值为world的元素，<code>configuration_hash</code> 最大限度的反映出ini文件。</p>
<p>第二，ini文件允许我们以数组的形式进行配置。例如ini文件中写入以下三行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">drift.arr[]=1</div><div class="line">drift.arr[]=2</div><div class="line">drift.arr[]=3</div></pre></td></tr></table></figure>
<p>那么最终生成的 <code>configuration_hash</code> 表中，就会存在一个key为 <code>drift.arr</code> 的元素，其value为一个包含的1,2,3三个数字的数组。这是一种极为罕见的配置方法。</p>
<p>第三，php还允许我们除了默认的php.ini文件（准确说是php-%s.ini）之外，另外构建一些ini文件。这些ini文件会被放入一个额外的目录。该目录由环境变量PHP_INI_SCAN_DIR来指定，当php_init_config解析完了php.ini之后，会再次扫描此目录，然后找出目录中所有.ini文件来分析。这些额外的ini文件中产生的kv键值对，也会被加入到configuration_hash中去。</p>
<p>这是一个偶尔有用的特性，假设我们自己开发php的扩展，却又不想将配置混入php.ini，便可以另外写一份ini，并通过PHP_INI_SCAN_DIR告诉php该去哪儿找到它。当然，其缺点也显而易见，其需要设置额外的环境变量来支持。更好的解决办法是，开发者在扩展中自己调用php_parse_user_ini_file或zend_parse_ini_file去解析对应的ini文件。</p>
<p>第四，在configuration_hash中，key是字符串，那么值的类型是什么？答案也是字符串（除了上述很特殊的数组）。具体来说，比如下面的配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">display_errors = On</div><div class="line">log_errors = Off</div><div class="line">log_errors_max_len = 1024</div></pre></td></tr></table></figure>
<p>那么最后configuration_hash中实际存放的键值对为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">key: &quot;display_errors&quot;</div><div class="line">val : &quot;1&quot;</div><div class="line"></div><div class="line">key: &quot;log_errors&quot;</div><div class="line">val : &quot;&quot;</div><div class="line"></div><div class="line">key: &quot;log_errors_max_len&quot;</div><div class="line">val : &quot;1024&quot;</div></pre></td></tr></table></figure>
<p>注意log_errors，其存放的值连”0”都不是，就是一个实实在在地空字符串。另外，log_errors_max_len也并非数字，而是字符串1024。</p>
<p>分析至此，基本上解析ini文件相关的内容都说清楚了。简单总结一下：</p>
<p>1，解析ini发生在 <code>php_module_startup</code> 阶段<br>2，解析结果存放在 <code>configuration_hash</code> 里。</p>
<h3 id="配置作用到模块"><a href="#配置作用到模块" class="headerlink" title="配置作用到模块"></a>配置作用到模块</h3><p>php的大致结构可以看成是最下层有一个zend引擎，它负责与OS进行交互、编译php代码、提供内存托管等等，在zend引擎的上层，排列着很多很多的模块。其中最核心的就一个Core模块，其他还有比如Standard，PCRE，Date，Session等等…这些模块还有另一个名字叫php扩展。我们可以简单理解为，每个模块都会提供一组功能接口给开发者来调用，举例来说，常用的诸如explode，trim，array等内置函数，便是由Standard模块提供的。</p>
<p>为什么需要谈到这些，是因为在php.ini里除了针对php自身，也就是针对Core模块的一些配置（例如safe_mode，display_errors，max_execution_time等），还有相当多的配置是针对其他不同模块的。</p>
<p>例如，date模块，它提供了常见的date， time，strtotime等函数。在php.ini中，它的相关配置形如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[Date]</div><div class="line">;date.timezone = &apos;Asia/Shanghai&apos;</div><div class="line">;date.default_latitude = 31.7667</div><div class="line">;date.default_longitude = 35.2333</div><div class="line">;date.sunrise_zenith = 90.583333</div><div class="line">;date.sunset_zenith = 90.583333</div></pre></td></tr></table></figure>
<p>除了这些模块拥有独立的配置，zend引擎也是可配的，只不过zend引擎的可配项非常少，只有error_reporting，zend.enable_gc和detect_unicode三项。</p>
<p>在上一小节中我们已经谈到，php_module_startup会调用php_init_config，其目的是解析ini文件并生成configuration_hash。那么接下来在php_module_startup中还会做什么事情呢？很显然，就是会将configuration_hash中的配置作用于Zend，Core，Standard，SPL等不同模块。当然这并非一个一蹴而就的过程，因为php通常会包含有很多模块，php启动的过程中这些模块也会依次进行启动。那么，对模块A进行配置的过程，便是发生在模块A的启动过程中。</p>
<p>有扩展开发经验的同学会直接指出，模块A的启动不就是在PHP_MINIT_FUNCTION(A)中么？</p>
<p>是的，如果模块A需要配置，那么在PHP_MINIT_FUNCTION中，可以调用REGISTER_INI_ENTRIES()来完成。REGISTER_INI_ENTRIES会根据当前模块所需要的配置项名称，去configuration_hash查找用户设置的配置值，并更新到模块自己的全局空间中。</p>
<h3 id="模块的全局空间"><a href="#模块的全局空间" class="headerlink" title="模块的全局空间"></a>模块的全局空间</h3><p>要理解如何将ini配置从configuration_hash作用到各个模块之前，有必要先了解一下php模块的全局空间。对于不同的php模块，均可以开辟一块属于自己的存储空间，并且这块空间对于该模块来说，是全局可见的。一般而言，它会被用来存放该模块所需的ini配置。也就是说，configuration_hash中的配置项，最终会被存放到该全局空间中。在模块的执行过程中，只需要直接访问这块全局空间，就可以拿到用户针对该模块进行的设置。当然，它也经常被用来记录模块在执行过程中的中间数据。</p>
<p>我们以bcmath模块来举例说明，bcmath是一个提供数学计算方面接口的php模块，首先我们来看看它有哪些ini配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">PHP_INI_BEGIN()</div><div class="line">    STD_PHP_INI_ENTRY(&quot;bcmath.scale&quot;, &quot;0&quot;, PHP_INI_ALL, OnUpdateLongGEZero, bc_precision, zend_bcmath_globals, bcmath_globals)</div><div class="line">PHP_INI_END()</div></pre></td></tr></table></figure>
<p>bcmath只有一个配置项，我们可以在php.ini中用bcmath.scale来配置bcmath模块。</p>
<p>接下来继续看看bcmatch模块的全局空间定义。在php_bcmath.h中有如下声明：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ZEND_BEGIN_MODULE_GLOBALS(bcmath)</div><div class="line">    bc_num _zero_;</div><div class="line">    bc_num _one_;</div><div class="line">    bc_num _two_;</div><div class="line">    long bc_precision;</div><div class="line">ZEND_END_MODULE_GLOBALS(bcmath)</div></pre></td></tr></table></figure>
<p>宏展开之后，即为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">typedef struct _zend_bcmath_globals &#123;</div><div class="line">    bc_num _zero_;</div><div class="line">    bc_num _one_;</div><div class="line">    bc_num _two_;</div><div class="line">    long bc_precision;</div><div class="line">&#125; zend_bcmath_globals;</div></pre></td></tr></table></figure>
<p>其实，<code>zend_bcmath_globals</code>类型就是bcmath模块中的全局空间类型。这里仅仅声明了<code>zend_bcmath_globals</code>结构体，在bcmath.c中还有具体的实例化定义：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">// 展开后即为zend_bcmath_globals bcmath_globals;</div><div class="line">ZEND_DECLARE_MODULE_GLOBALS(bcmath)</div></pre></td></tr></table></figure>
<p>可以看出，用<code>ZEND_DECLARE_MODULE_GLOBALS</code>完成了对变量<code>bcmath_globals</code>的定义。</p>
<p>bcmath_globals是一块真正的全局空间，它包含有四个字段。其最后一个字段bc_precision，对应于ini配置中的bcmath.scale。我们在php.ini中设置了bcmath.scale的值，随后在启动bcmath模块的时候，bcmath.scale的值被更新到<code>bcmath_globals.bc_precision</code>中去。</p>
<p>把<code>configuration_hash</code>中的值，更新到各个模块自己定义的xxx_globals变量中，就是所谓的将ini配置作用到模块。一旦模块启动完成，那么这些配置也都作用到位。所以在随后的执行阶段，php模块无需再次访问<code>configuration_hash</code>，模块仅需要访问自己的XXX_globals，就可以拿到用户设定的配置。</p>
<p>bcmath_globals，除了有一个字段为ini配置项，其他还有三个字段为何意？这就是模块全局空间的第二个作用，它除了用于ini配置，还可以存储模块执行过程中的一些数据。</p>
<p><img src="/images/deep-understanding-php.ini-config.png" alt=""></p>
<p>再例如json模块，也是php中一个很常用的模块：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ZEND_BEGIN_MODULE_GLOBALS(json)</div><div class="line">    int error_code;</div><div class="line">ZEND_END_MODULE_GLOBALS(json)</div></pre></td></tr></table></figure>
<p>可以看到json模块并不需要ini配置，它的全局空间只有一个字段error_code。error_code记录了上一次执行json_decode或者json_encode中发生的错误。json_last_error函数便是返回这个error_code，来帮助用户定位错误原因。</p>
<p>为了能够很便捷的访问模块全局空间变量，php约定俗成的提出了一些宏。比如我们想访问json_globals中的error_code，当然可以直接写做json_globals.error_code（多线程环境下不行），不过更通用的写法是定义JSON_G宏：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">#define JSON_G(v) (json_globals.v)</div></pre></td></tr></table></figure>
<p>我们使用JSON_G(error_code)来访问json_globals.error_code。本文刚开始的时候，曾提到PG、BG、JSON_G、PCRE_G，XXX_G等等，这些宏在php源代码中也是很常见的。现在我们可以很轻松的理解它们，PG宏可以访问Core模块的全局变量，BG访问Standard模块的全局变量，PCRE_G则访问PCRE模块的全局变量。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">#define PG(v) (core_globals.v)</div><div class="line">#define BG(v) (basic_globals.v)</div></pre></td></tr></table></figure>
<h3 id="如何确定一个模块需要哪些配置呢？"><a href="#如何确定一个模块需要哪些配置呢？" class="headerlink" title="如何确定一个模块需要哪些配置呢？"></a>如何确定一个模块需要哪些配置呢？</h3><p>模块需要什么样的INI配置，都是在各个模块中自己定义的。举例来说，对于Core模块，有如下的配置项定义：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">PHP_INI_BEGIN()</div><div class="line">    ......</div><div class="line">    STD_PHP_INI_ENTRY_EX(&quot;display_errors&quot;, &quot;1&quot;, PHP_INI_ALL,    OnUpdateDisplayErrors, display_errors, php_core_globals, core_globals, display_errors_mode)</div><div class="line">    STD_PHP_INI_BOOLEAN(&quot;enable_dl&quot;,       &quot;1&quot;, PHP_INI_SYSTEM, OnUpdateBool,          enable_dl,      php_core_globals, core_globals)</div><div class="line">    STD_PHP_INI_BOOLEAN(&quot;expose_php&quot;,      &quot;1&quot;, PHP_INI_SYSTEM, OnUpdateBool,          expose_php,     php_core_globals, core_globals)</div><div class="line">    STD_PHP_INI_BOOLEAN(&quot;safe_mode&quot;,       &quot;0&quot;, PHP_INI_SYSTEM, OnUpdateBool,          safe_mode,      php_core_globals, core_globals)</div><div class="line">    ......</div><div class="line">PHP_INI_END()</div></pre></td></tr></table></figure>
<p>可以在<code>php-src\main\main.c</code>文件大概450+行找到上述代码。其中涉及的宏比较多，有ZEND_INI_BEGIN 、ZEND_INI_END、PHP_INI_ENTRY_EX、STD_PHP_INI_BOOLEAN等等，本文不一一赘述，感兴趣的读者可自行分析。</p>
<p>上述代码进行宏展开后得到：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">static const zend_ini_entry ini_entries[] = &#123;</div><div class="line">    ..</div><div class="line">    &#123; 0, PHP_INI_ALL,    &quot;display_errors&quot;,sizeof(&quot;display_errors&quot;),OnUpdateDisplayErrors,(void *)XtOffsetOf(php_core_globals, display_errors), (void *)&amp;core_globals, NULL, &quot;1&quot;, sizeof(&quot;1&quot;)-1, NULL, 0, 0, 0, display_errors_mode &#125;,</div><div class="line">    &#123; 0, PHP_INI_SYSTEM, &quot;enable_dl&quot;,     sizeof(&quot;enable_dl&quot;),     OnUpdateBool,         (void *)XtOffsetOf(php_core_globals, enable_dl),      (void *)&amp;core_globals, NULL, &quot;1&quot;, sizeof(&quot;1&quot;)-1, NULL, 0, 0, 0, zend_ini_boolean_displayer_cb &#125;,</div><div class="line">    &#123; 0, PHP_INI_SYSTEM, &quot;expose_php&quot;,    sizeof(&quot;expose_php&quot;),    OnUpdateBool,         (void *)XtOffsetOf(php_core_globals, expose_php),     (void *)&amp;core_globals, NULL, &quot;1&quot;, sizeof(&quot;1&quot;)-1, NULL, 0, 0, 0, zend_ini_boolean_displayer_cb &#125;,</div><div class="line">    &#123; 0, PHP_INI_SYSTEM, &quot;safe_mode&quot;,     sizeof(&quot;safe_mode&quot;),     OnUpdateBool,         (void *)XtOffsetOf(php_core_globals, safe_mode),      (void *)&amp;core_globals, NULL, &quot;0&quot;, sizeof(&quot;0&quot;)-1, NULL, 0, 0, 0, zend_ini_boolean_displayer_cb &#125;,</div><div class="line">    ...</div><div class="line">    &#123; 0, 0, NULL, 0, NULL, NULL, NULL, NULL, NULL, 0, NULL, 0, 0, 0, NULL &#125;</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p>我们看到，配置项的定义，其本质上就是定义了一个zend_ini_entry类型的数组。zend_ini_entry结构体的字段具体含义为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">struct _zend_ini_entry &#123;</div><div class="line">    int module_number;                // 模块的id</div><div class="line">    int modifiable;                   // 可被修改的范围，例如php.ini，ini_set</div><div class="line">    char *name;                       // 配置项的名称</div><div class="line">    uint name_length;</div><div class="line">    ZEND_INI_MH((*on_modify));        // 回调函数，配置项注册或修改的时候会调用</div><div class="line">    void *mh_arg1;                    // 通常为配置项字段在XXX_G中的偏移量</div><div class="line">    void *mh_arg2;                    // 通常为XXX_G</div><div class="line">    void *mh_arg3;                    // 通常为保留字段，极少用到</div><div class="line"></div><div class="line">    char *value;                      // 配置项的值</div><div class="line">    uint value_length;</div><div class="line"></div><div class="line">    char *orig_value;                 // 配置项的原始值</div><div class="line">    uint orig_value_length;</div><div class="line">    int orig_modifiable;              // 配置项的原始modifiable</div><div class="line">    int modified;                     // 是否发生过修改，如果有修改，则orig_value会保存修改前的值</div><div class="line"></div><div class="line">    void (*displayer)(zend_ini_entry *ini_entry, int type);</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p>将配置作用到模块——REGISTER_INI_ENTRIES<br>经常能够在不同扩展的PHP_MINIT_FUNCTION里看到REGISTER_INI_ENTRIES。REGISTER_INI_ENTRIES主要负责完成两件事情，第一，对模块的全局空间XXX_G进行填充，同步configuration_hash中的值到XXX_G中去。其次，它还生成了EG(ini_directives)。</p>
<p>REGISTER_INI_ENTRIES也是一个宏，展开之后实则为zend_register_ini_entries方法。具体来看下zend_register_ini_entries的实现：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ZEND_API int zend_register_ini_entries(const zend_ini_entry *ini_entry, int module_number TSRMLS_DC) /* &#123;&#123;&#123; */</div><div class="line">&#123;</div><div class="line">    // ini_entry为zend_ini_entry类型数组，p为数组中每一项的指针</div><div class="line">    const zend_ini_entry *p = ini_entry;</div><div class="line">    zend_ini_entry *hashed_ini_entry;</div><div class="line">    zval default_value;</div><div class="line"></div><div class="line">    // EG(ini_directives)就是registered_zend_ini_directives</div><div class="line">    HashTable *directives = registered_zend_ini_directives;</div><div class="line">    zend_bool config_directive_success = 0;</div><div class="line"></div><div class="line">    // 还记得ini_entry最后一项固定为&#123; 0, 0, NULL, ... &#125;么</div><div class="line">    while (p-&gt;name) &#123;</div><div class="line">        config_directive_success = 0;</div><div class="line"></div><div class="line">        // 将p指向的zend_ini_entry加入EG(ini_directives)</div><div class="line">        if (zend_hash_add(directives, p-&gt;name, p-&gt;name_length, (void*)p, sizeof(zend_ini_entry), (void **) &amp;hashed_ini_entry) == FAILURE) &#123;</div><div class="line">            zend_unregister_ini_entries(module_number TSRMLS_CC);</div><div class="line">            return FAILURE;</div><div class="line">        &#125;</div><div class="line">        hashed_ini_entry-&gt;module_number = module_number;</div><div class="line"></div><div class="line">        // 根据name去configuration_hash中查询，取出来的结果放在default_value中</div><div class="line">        // 注意default_value的值比较原始，一般是数字、字符串、数组等，具体取决于php.ini中的写法</div><div class="line">        if ((zend_get_configuration_directive(p-&gt;name, p-&gt;name_length, &amp;default_value)) == SUCCESS) &#123;</div><div class="line">        // 调用on_modify更新到模块的全局空间XXX_G中</div><div class="line">        if (!hashed_ini_entry-&gt;on_modify || hashed_ini_entry-&gt;on_modify(hashed_ini_entry, Z_STRVAL(default_value), Z_STRLEN(default_value), hashed_ini_entry-&gt;mh_arg1, hashed_ini_entry-&gt;mh_arg2, hashed_ini_entry-&gt;mh_arg3, ZEND_INI_STAGE_STARTUP TSRMLS_CC) == SUCCESS) &#123;</div><div class="line">            hashed_ini_entry-&gt;value = Z_STRVAL(default_value);</div><div class="line">            hashed_ini_entry-&gt;value_length = Z_STRLEN(default_value);</div><div class="line">            config_directive_success = 1;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">        // 如果configuration_hash中没有找到，则采用默认值</div><div class="line">        if (!config_directive_success &amp;&amp; hashed_ini_entry-&gt;on_modify) &#123;</div><div class="line">            hashed_ini_entry-&gt;on_modify(hashed_ini_entry, hashed_ini_entry-&gt;value, hashed_ini_entry-&gt;value_length, hashed_ini_entry-&gt;mh_arg1, hashed_ini_entry-&gt;mh_arg2, hashed_ini_entry-&gt;mh_arg3, ZEND_INI_STAGE_STARTUP TSRMLS_CC);</div><div class="line">        &#125;</div><div class="line">        p++;</div><div class="line">    &#125;</div><div class="line">    return SUCCESS;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>简单来说，可以把上述代码的逻辑表述为：</p>
<p>1、将模块声明的ini配置项添加到 <code>EG(ini_directives)</code>中。注意，ini配置项的值可能在随后被修改。<br>2、尝试去<code>configuration_hash</code>中寻找各个模块需要的ini。</p>
<pre><code>- 如果能够找到，说明用户叜ini文件中配置了该值，那么采用用户的配置。
- 如果没有找到，OK，没有关系，因为模块在声明ini的时候，会带上默认值。
</code></pre><p>3、将ini的值同步到XX_G里面。毕竟在php的执行过程中，起作用的还是这些XXX_globals。具体的过程是调用每条ini配置对应的on_modify方法完成，on_modify由模块在声明ini的时候进行指定。</p>
<p>我们来具体看下on_modify，它其实是一个函数指针，来看两个具体的Core模块的配置声明：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">STD_PHP_INI_BOOLEAN(&quot;log_errors&quot;,      &quot;0&quot;,    PHP_INI_ALL, OnUpdateBool, log_errors,         php_core_globals, core_globals)</div><div class="line">STD_PHP_INI_ENTRY(&quot;log_errors_max_len&quot;,&quot;1024&quot;, PHP_INI_ALL, OnUpdateLong, log_errors_max_len, php_core_globals, core_globals)</div></pre></td></tr></table></figure>
<p>对于log_errors，它的on_modify被设置为OnUpdateBool，对于log_errors_max_len，则on_modify被设置为OnUpdateLong。</p>
<p>进一步假设我们在php.ini中的配置为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">log_errors = On</div><div class="line">log_errors_max_len = 1024</div></pre></td></tr></table></figure>
<p>具体来看下OnUpdateBool函数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ZEND_API ZEND_INI_MH(OnUpdateBool) </div><div class="line">&#123;</div><div class="line">    zend_bool *p;</div><div class="line"></div><div class="line">    // base表示core_globals的地址</div><div class="line">    char *base = (char *) mh_arg2;</div><div class="line"></div><div class="line">    // p表示core_globals的地址加上log_errors字段的偏移量</div><div class="line">    // 得到的即为log_errors字段的地址</div><div class="line">    p = (zend_bool *) (base+(size_t) mh_arg1);  </div><div class="line"></div><div class="line">    if (new_value_length == 2 &amp;&amp; strcasecmp(&quot;on&quot;, new_value) == 0) &#123;</div><div class="line">        *p = (zend_bool) 1;</div><div class="line">    &#125;</div><div class="line">    else if (new_value_length == 3 &amp;&amp; strcasecmp(&quot;yes&quot;, new_value) == 0) &#123;</div><div class="line">        *p = (zend_bool) 1;</div><div class="line">    &#125;</div><div class="line">    else if (new_value_length == 4 &amp;&amp; strcasecmp(&quot;true&quot;, new_value) == 0) &#123;</div><div class="line">        *p = (zend_bool) 1;</div><div class="line">    &#125;</div><div class="line">    else &#123;</div><div class="line">        // configuration_hash中存放的value是字符串&quot;1&quot;，而非&quot;On&quot;</div><div class="line">        // 因此这里用atoi转化成数字1</div><div class="line">        *p = (zend_bool) atoi(new_value);</div><div class="line">    &#125;</div><div class="line">    return SUCCESS;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>最令人费解的估计就是mh_arg1和mh_arg2了，其实对照前面所述的zend_ini_entry定义，mh_arg1，mh_arg2还是很容易参透的。mh_arg1表示字节偏移量，mh_arg2表示XXX_globals的地址。因此，(char *)mh_arg2 + mh_arg1的结果即为XXX_globals中某个字段的地址。具体到本case中，就是计算core_globals中log_errors的地址。因此，当OnUpdateBool最后执行到</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">*p = (zend_bool) atoi(new_value);</div></pre></td></tr></table></figure>
<p>其作用就相当于</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">core_globals.log_errors = (zend_bool) atoi(&quot;1&quot;);</div></pre></td></tr></table></figure>
<p>分析完了OnUpdateBool，我们再来看OnUpdateLong便觉得一目了然：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ZEND_API ZEND_INI_MH(OnUpdateLong)</div><div class="line">&#123;</div><div class="line">    long *p;</div><div class="line">    char *base = (char *) mh_arg2;</div><div class="line"></div><div class="line">    // 获得log_errors_max_len的地址</div><div class="line">    p = (long *) (base+(size_t) mh_arg1);</div><div class="line"></div><div class="line">    // 将&quot;1024&quot;转化成long型，并赋值给core_globals.log_errors_max_len</div><div class="line">    *p = zend_atol(new_value, new_value_length);</div><div class="line">    return SUCCESS;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>最后需要注意的是，<code>zend_register_ini_entries</code> 函数中，如果 <code>configuration_hash</code> 中存在配置，则当调用 <code>on_modify</code> 结束后，<code>hashed_ini_entry</code> 中的<code>value</code>和<code>value_length</code>会被更新。也就是说，如果用户在php.ini中配置过，则 <code>EG(ini_directives)</code> 存放的就是实际配置的值。如果用户没配，<code>EG(ini_directives)</code> 中存放的是声明<code>zend_ini_entry</code>时给出的默认值。</p>
<p>zend_register_ini_entries中的default_value变量命名比较糟糕，相当容易造成误解。其实default_value并非表示默认值，而是表示用户实际配置的值。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>至此，三块数据configuration_hash，EG(ini_directives)以及PG、BG、PCRE_G、JSON_G、XXX_G…已经都交代清楚了。</p>
<p>总结一下：</p>
<p>1、<code>configuration_hash</code>，存放php.ini文件里的配置，不做校验，其值为字符串。<br>2、<code>EG(ini_directives)</code>，存放的是各个模块中定义的 <code>zend_ini_entry</code>，如果用户在php.ini配置过（<code>configuration_hash</code> 中存在），则值被替换为 <code>configuration_hash</code> 中的值，类型依然是字符串。<br>3、<code>XXX_G</code>，该宏用于访问模块的全局空间，这块内存空间可用来存放ini配置，并通过on_modify指定的函数进行更新，其数据类型由XXX_G中的字段声明来决定。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;好久没更了，今天读到一篇介绍非常详细的文章，转载一下，凑个数。&lt;/p&gt;
&lt;p&gt;转自：&lt;a href=&quot;http://www.cnblogs.com/driftcloudy/p/4011954.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http
    
    </summary>
    
      <category term="PHP" scheme="http://blog.100dos.com/categories/PHP/"/>
    
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
  </entry>
  
  <entry>
    <title>PHP 的垃圾回收机制－理解PHP如何解决循环引用导致的内存泄漏问题</title>
    <link href="http://blog.100dos.com/2017/04/07/php-garbage-collection-collect-cycles/"/>
    <id>http://blog.100dos.com/2017/04/07/php-garbage-collection-collect-cycles/</id>
    <published>2017-04-07T08:00:53.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<h5 id="先来看一下什么是循环引用问题？"><a href="#先来看一下什么是循环引用问题？" class="headerlink" title="先来看一下什么是循环引用问题？"></a>先来看一下什么是循环引用问题？</h5><p>在 PHP 5.2 中使用的垃圾回收算法是 Reference Counting（引用计数）。非常简单，为每个 zval 分配一个计数器，当一个 zval 建立时计数器初始化为1，以后每有一个新变量引用此 zval，则计数器加1，而每当减少一个引用的变量则计数器减 1，当垃圾回收机制运作的时候，将所有计数器为0的zval销毁并回收其占用的内存。但是对于以下代码，把数组作为一个元素添加到自己，自身引用自身。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">$a = <span class="keyword">array</span>( <span class="string">'one'</span> );</div><div class="line">$a[] =&amp; $a;</div><div class="line">xdebug_debug_zval( <span class="string">'a'</span> );</div><div class="line"><span class="meta">?&gt;</span></div><div class="line"></div><div class="line"><span class="comment">// output </span></div><div class="line">a: (refcount=<span class="number">2</span>, is_ref=<span class="number">1</span>)=<span class="keyword">array</span> (</div><div class="line">   <span class="number">0</span> =&gt; (refcount=<span class="number">1</span>, is_ref=<span class="number">0</span>)=<span class="string">'one'</span>,</div><div class="line">   <span class="number">1</span> =&gt; (refcount=<span class="number">2</span>, is_ref=<span class="number">1</span>)=...</div><div class="line">)</div></pre></td></tr></table></figure>
<p><img src="/images/php/loop-array.png" alt=""><br>能看到数组变量 <code>a</code> 同时也是这个数组的第二个元素<code>1</code> 指向的变量容器中 <code>refcount</code> 为 2。上面的输出结果中的”…”说明发生了递归操作， 显然在这种情况下”…”表示指向原始数组。</p>
<p>跟刚刚一样，对一个变量调用 unset，将从符号表中删除这个引用，且它指向的 zval 的引用次数也减 1。所以，如果我们在执行完上面的代码后，对变量 <code>$a</code> 调用 unset， 那么变量 <code>$a</code> 和数组元素 <code>1</code> 所指向的变量容器的引用次数减 1，变成 1。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="comment">// output</span></div><div class="line">(refcount=<span class="number">1</span>, is_ref=<span class="number">1</span>)=<span class="keyword">array</span> (</div><div class="line">   <span class="number">0</span> =&gt; (refcount=<span class="number">1</span>, is_ref=<span class="number">0</span>)=<span class="string">'one'</span>,</div><div class="line">   <span class="number">1</span> =&gt; (refcount=<span class="number">1</span>, is_ref=<span class="number">1</span>)=...</div><div class="line">)</div></pre></td></tr></table></figure>
<p><img src="/images/php/leak-array.png" alt=""></p>
<p>尽管不再有任何作用域中的<strong>任何</strong>符号表中的引用指向这个 zval，由于数组元素 <code>1</code> 仍然指向数组本身，所以这个 zval 不能被清除 。因为没有另外的符号指向它，所以没有办法清除这个结构，结果就会导致内存泄漏。</p>
<p>这就是循环引用，虽然 PHP 将在脚本执行结束时会清除这个数据结构，但是在 PHP 清除之前，将耗费不少内存。这中问题如果出现很难被发现。仅仅一两次倒没什么，但是如果出现几千次，甚至几十万次的内存泄漏，这显然是个大问题。尤其是当PHP被作为守护进程，长时间运行时。</p>
<h5 id="那么PHP是如何解决的呢？"><a href="#那么PHP是如何解决的呢？" class="headerlink" title="那么PHP是如何解决的呢？"></a>那么PHP是如何解决的呢？</h5><p>PHP 5.3 的垃圾回收算法仍然以引用计数为基础，但是不再使用简单计数，而是使用了同步回收算法，这个算法由IBM的工程师在论文 Concurrent Cycle Collection in Reference Counted Systems 中提出，包括Java的垃圾回收机制中也有在使用该算法。</p>
<p>下面我们来看看PHP的实现。猜测对象回收时应该会有垃圾回收调用，所以找到Object对象释放的函数。 在 Zend/zend_objects_API.h <code>zend_object_release</code> 函数中</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="keyword">static</span> zend_always_inline void zend_object_release(zend_object *obj)</div><div class="line">&#123;</div><div class="line">    <span class="keyword">if</span> (--GC_REFCOUNT(obj) == <span class="number">0</span>) &#123;</div><div class="line">        zend_objects_store_del(obj);</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (UNEXPECTED(GC_MAY_LEAK((zend_refcounted*)obj))) &#123;</div><div class="line">        gc_possible_root((zend_refcounted*)obj);</div><div class="line">    &#125;   </div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>当一个对象被释放时（调用 <code>zend_object_release</code> 函数），根据简单的引用计数原则，如果引用计数减少到 0，所在变量容器将被清除（free）。否则引用计数减少到非 0 值时，会产生垃圾周期（即调用 <code>gc_possible_root</code> 函数）。这个函数的名字有点奇怪，possible root 可能根？看一下 <code>gc_possible_root</code> 函数内容，在 Zend/zend_gc.c 中 <code>gc_possible_root</code>。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line">ZEND_API void ZEND_FASTCALL gc_possible_root(zend_refcounted *ref)</div><div class="line">&#123;</div><div class="line">    gc_root_buffer *newRoot;</div><div class="line">    newRoot = GC_G(unused);</div><div class="line"></div><div class="line">   <span class="comment">// ....... 省略部分代码</span></div><div class="line"></div><div class="line">    GC_TRACE_SET_COLOR(ref, GC_PURPLE);</div><div class="line">    GC_INFO(ref) = (newRoot - GC_G(buf)) | GC_PURPLE;</div><div class="line">    newRoot-&gt;ref = ref;</div><div class="line"></div><div class="line">    newRoot-&gt;next = GC_G(roots).next;</div><div class="line">    newRoot-&gt;prev = &amp;GC_G(roots);</div><div class="line">    GC_G(roots).next-&gt;prev = newRoot;</div><div class="line">    GC_G(roots).next = newRoot;</div><div class="line">    <span class="comment">// ....</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这个函数把可能根（possible roots）放在了 <code>GC_G(roots)</code> 中，这个 <code>GC_G(roots)</code> 是一个双向链表。结构如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line">typedef struct _gc_root_buffer &#123;</div><div class="line">    zend_refcounted          *ref;</div><div class="line">    struct _gc_root_buffer   *next;     <span class="comment">/* double-linked list               */</span></div><div class="line">    struct _gc_root_buffer   *prev;</div><div class="line">    uint32_t                 refcount;</div><div class="line">&#125; gc_root_buffer;</div></pre></td></tr></table></figure>
<p>并且这个链表的长度是定义是 <code>#define GC_ROOT_BUFFER_MAX_ENTRIES 10001</code>。猜测简单解决循环引用原理：因为已经没有任何符号指向循环引用指向的zval，于是用这个<em>可能根双向链表</em>来记录可能产生内存泄漏的结构体。</p>
<p>接着往下看代码， <code>GC_TRACE_SET_COLOR(ref, GC_PURPLE);</code> 设置颜色为紫色，关于颜色的定义可以在代码注释中看到：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"><span class="comment">* Colors and its meaning</span></div><div class="line"><span class="comment">* ----------------------</span></div><div class="line"><span class="comment">*</span></div><div class="line"><span class="comment">* BLACK  (GC_BLACK)   - In use or free. </span></div><div class="line"><span class="comment">* GREY   (GC_GREY)    - Possible member of cycle.</span></div><div class="line"><span class="comment">* WHITE  (GC_WHITE)   - Member of garbage cycle.</span></div><div class="line"><span class="comment">* PURPLE (GC_PURPLE)  - Possible root of cycle.</span></div><div class="line"><span class="comment">*</span></div></pre></td></tr></table></figure>
<p>紫色表示<em>疑似垃圾（Possible root of cycle）</em>。我们可以推测 PHP 把可能存在内存泄露的引用存放到 Possible roots 里，这样在清理垃圾时，就避免不得不检查所有引用计数。</p>
<p>现在有了垃圾回收的<em>疑似垃圾</em>的一个链表。那么是在何时清除这些垃圾呢？</p>
<p>回看 <code>gc_possible_root</code> 函数中，调用了 <code>gc_collect_cycles</code> ，这个函数其实就是 <code>zend_gc_collect_cycles</code>，在 Zend/zend.c 的 <code>zend_startup</code>中定义实现。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* Set up the default garbage collection implementation. */</span></div><div class="line">gc_collect_cycles = zend_gc_collect_cycles;</div></pre></td></tr></table></figure>
<p>在来看<code>gc_collect_cycles</code>的调用条件，在Zend/zend_gc.c 中 <code>gc_possible_root</code>中</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 略...</span></div><div class="line">    newRoot = GC_G(unused);</div><div class="line">    <span class="keyword">if</span> (newRoot) &#123;</div><div class="line">        GC_G(unused) = newRoot-&gt;prev;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (GC_G(first_unused) != GC_G(last_unused)) &#123;</div><div class="line">        newRoot = GC_G(first_unused);</div><div class="line">        GC_G(first_unused)++;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">if</span> (!GC_G(gc_enabled)) &#123;</div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;    </div><div class="line">        GC_REFCOUNT(ref)++;</div><div class="line">        gc_collect_cycles();</div><div class="line">        GC_REFCOUNT(ref)--;</div><div class="line">        <span class="keyword">if</span> (UNEXPECTED(GC_REFCOUNT(ref)) == <span class="number">0</span>) &#123;</div><div class="line">            zval_dtor_func(ref);</div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;    </div><div class="line">        <span class="keyword">if</span> (UNEXPECTED(GC_INFO(ref))) &#123;</div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;    </div><div class="line">        newRoot = GC_G(unused);</div><div class="line">        <span class="keyword">if</span> (!newRoot) &#123;</div><div class="line"><span class="comment">#if ZEND_GC_DEBUG</span></div><div class="line">            <span class="keyword">if</span> (!GC_G(gc_full)) &#123;</div><div class="line">                fprintf(stderr, <span class="string">"GC: no space to record new root candidate\n"</span>);</div><div class="line">                GC_G(gc_full) = <span class="number">1</span>;</div><div class="line">            &#125;    </div><div class="line"><span class="comment">#endif</span></div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;    </div><div class="line">        GC_G(unused) = newRoot-&gt;prev;</div><div class="line">    &#125;</div><div class="line"><span class="comment">// 略...</span></div></pre></td></tr></table></figure>
<p>看下上面的 if else 在什么条件下能被执行到。<code>unused</code>、<code>first_unused</code>、<code>last_unused</code>在<code>_zend_gc_globals</code>中。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line">typedef struct _zend_gc_globals &#123;</div><div class="line">  <span class="comment">// ...</span></div><div class="line">    gc_root_buffer   *buf;              <span class="comment">/* preallocated arrays of buffers   */</span></div><div class="line">    gc_root_buffer    roots;            <span class="comment">/* list of possible roots of cycles */</span></div><div class="line">    gc_root_buffer   *unused;           <span class="comment">/* list of unused buffers           */</span></div><div class="line">    gc_root_buffer   *first_unused;     <span class="comment">/* pointer to first unused buffer   */</span></div><div class="line">    gc_root_buffer   *last_unused;      <span class="comment">/* pointer to last unused buffer    */</span></div><div class="line"></div><div class="line">  <span class="comment">// ...</span></div><div class="line">&#125; zend_gc_globals;</div></pre></td></tr></table></figure>
<p>于是可以得出结论，仅仅在根缓冲区满了时（也就是<code>GC_ROOT_BUFFER_MAX_ENTRIES</code> 不够时），才对缓冲区内部所有不同的变量容器执行垃圾回收操作。</p>
<p>那么是如何进行垃圾回收的呢？看 <code>zend_gc_collect_cycles</code> 函数就好了。在注释中也有描述</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"></div><div class="line"> * Flow</div><div class="line">* =====</div><div class="line">*</div><div class="line">* The garbage collect cycle starts from <span class="string">'gc_mark_roots'</span>, which traverses the</div><div class="line">* possible roots, <span class="keyword">and</span> calls mark_grey <span class="keyword">for</span> roots are marked purple with</div><div class="line">* depth-first traverse.</div><div class="line">*</div><div class="line">* After all possible roots are traversed <span class="keyword">and</span> marked,</div><div class="line">* gc_scan_roots will be called, <span class="keyword">and</span> each root will be called with</div><div class="line">* gc_scan(root-&gt;ref)</div><div class="line">*</div><div class="line">* gc_scan checkes the colors of possible members.</div><div class="line">*</div><div class="line">* <span class="keyword">If</span> the node is marked <span class="keyword">as</span> grey <span class="keyword">and</span> the refcount &gt; <span class="number">0</span></div><div class="line">*    gc_scan_black will be called on that node to scan it<span class="string">'s subgraph.</span></div><div class="line"><span class="string">* otherwise (refcount == 0), it marks the node white.</span></div><div class="line"><span class="string">*</span></div><div class="line"><span class="string">* A node MAY be added to possbile roots when ZEND_UNSET_VAR happens or</span></div><div class="line"><span class="string">* zend_assign_to_variable is called only when possible garbage node is</span></div><div class="line"><span class="string">* produced.</span></div><div class="line"><span class="string">* gc_possible_root() will be called to add the nodes to possible roots.</span></div><div class="line"><span class="string">*</span></div><div class="line"><span class="string">*</span></div><div class="line"><span class="string">* For objects, we call their get_gc handler (by default '</span>zend_std_get_gc<span class="string">') to</span></div><div class="line"><span class="string">* get the object properties to scan.</span></div><div class="line"><span class="string">*</span></div></pre></td></tr></table></figure>
<p>过程：</p>
<ol>
<li>垃圾回收周期从 <code>gc_mark_roots</code> 函数开始，深度优先遍历所有<em>可能根</em>，调用 <code>mark_grey</code> 把原标记为紫色（Possible root of cycle）的root标记为灰色（Possible member of cycle）。</li>
<li>当所有的root都被遍历并且标记之后，调用 <code>gc_scan_roots</code> ，然后每个root 执行 <code>gc_scan(root-&gt;ref)</code>。</li>
<li><code>gc_scan</code> 检查所有的疑似垃圾成员。</li>
<li>如果节点被标记为灰色，并且 refcount &gt; 0，该节点执行 <code>gc_scan_black</code>扫描它的子图。否则（ refcount == 0），标记节点为白色（Member of garbage cycle 垃圾周期）。</li>
<li>最后被标记为白色的节点会被真正的清除。代码就不贴了，都在<code>zend_gc_collect_cycles</code>函数中。可以说一下，对于 object 调用的是efree(p)。</li>
</ol>
<h5 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h5><ol>
<li>PHP的垃圾回收使用的是同步回收算法（Concurrent Cycle Collection in Reference Counted Systems）。</li>
<li>基本原来是通过一个双向链表来存储可能存在内存泄漏的zval引用，解决因为符号表中已经不存在指向zval的引用导致无法删除zval的问题。</li>
<li>当可能存在内存泄漏的引用被释放时，比如 <code>unset</code> 一个 Object，就把该引用添加到可能根的链表中。</li>
<li>同时判断可能根链表是否已满，如果已满（也就是<code>GC_ROOT_BUFFER_MAX_ENTRIES</code> 不够时），则开启垃圾回收周期。在<code>shutdown_executor</code>（脚本执行结束）时，也会开始垃圾回收周期。</li>
<li><code>zend_gc_collect_cycles</code> 垃圾回收周期中，深度遍历所有可能根。用 BLACK、GREY、WHITE、PURPLE 来记录回收周期中变量的状态，最终清楚垃圾变量。</li>
</ol>
<h5 id="References："><a href="#References：" class="headerlink" title="References："></a>References：</h5><ul>
<li>引用计数基本知识 <a href="http://php.net/manual/zh/features.gc.refcounting-basics.php" target="_blank" rel="external">http://php.net/manual/zh/features.gc.refcounting-basics.php</a></li>
<li>回收周期(Collecting Cycles)  <a href="http://php.net/manual/zh/features.gc.collecting-cycles.php" target="_blank" rel="external">http://php.net/manual/zh/features.gc.collecting-cycles.php</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h5 id=&quot;先来看一下什么是循环引用问题？&quot;&gt;&lt;a href=&quot;#先来看一下什么是循环引用问题？&quot; class=&quot;headerlink&quot; title=&quot;先来看一下什么是循环引用问题？&quot;&gt;&lt;/a&gt;先来看一下什么是循环引用问题？&lt;/h5&gt;&lt;p&gt;在 PHP 5.2 中使用的垃圾回收
    
    </summary>
    
      <category term="PHP" scheme="http://blog.100dos.com/categories/PHP/"/>
    
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
      <category term="源码阅读" scheme="http://blog.100dos.com/tags/%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB/"/>
    
      <category term="PHP7" scheme="http://blog.100dos.com/tags/PHP7/"/>
    
      <category term="GC" scheme="http://blog.100dos.com/tags/GC/"/>
    
  </entry>
  
  <entry>
    <title>Travis CI 持续集成 Hexo 的 Github Pages的博客</title>
    <link href="http://blog.100dos.com/2017/03/24/publish-hexo-post-on-github-pages-by-travis-ci/"/>
    <id>http://blog.100dos.com/2017/03/24/publish-hexo-post-on-github-pages-by-travis-ci/</id>
    <published>2017-03-24T06:45:17.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p>之前重新梳理了 <a href="http://blog.100dos.com/2017/03/14/how-to-use-hexo-post-articles-on-multiple-pcs/">Hexo 博客的发布流程</a>，可以还是觉得麻烦。毕竟操作太多，容易出错。于是想到了用 Travis CI 简化一下，效果不错，省去了不少步骤。现在只需要在 hexo 分支撰写 blog，然后push到github就可以了。Travis 会帮助完成剩下的merge hexo分支、生成静态文件、提交 master 这些工作。</p>
<p>首先，用 Github 账号创建 <a href="https://travis-ci.org/" target="_blank" rel="external">Travis CI</a> 账号，直接用 Github 账号登录，并赋予权限即可。并在项目列表选择并创建 Travis CI 项目 <code>jixiaod / jixiaod.github.io</code>项目。</p>
<p>接着，在 Github 的配置里生成 <code>Personal access tokens</code>，然后在Travis CI里配置的 Environment Variables 里配置添加，一会要在 <code>.travis.yml</code> 中使用。并设置 <code>hexo</code> 为默认分支，在 <code>git push origin hexo</code> 时，触发 travis  build。<br><img src="/images/travsi-ci-personal-access-token.png" alt=""></p>
<p>编写 <code>.travis.yml</code> 文件，参考<a href="https://github.com/jixiaod/jixiaod.github.io/blob/hexo/.travis.yml" target="_blank" rel="external">我的配置</a> </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">language: node_js</div><div class="line">sudo: required</div><div class="line">node_js:</div><div class="line">  - 5.4.0</div><div class="line">cache:</div><div class="line">  directories:</div><div class="line">    - node_modules</div><div class="line">before_script:</div><div class="line">  - git clone --branch master https://github.com/jixiaod/jixiaod.github.io.git public</div><div class="line">script:</div><div class="line">  - npm run build</div><div class="line">after_success:</div><div class="line">  - cd public</div><div class="line">  - git config user.name &quot;Ji Gang&quot;</div><div class="line">  - git config user.email &quot;ji.xiaod@gmail.com&quot;</div><div class="line">  - git add --all .</div><div class="line">  - git commit -m &quot;travis ci auto build&quot;</div><div class="line">  - git push https://$Personal_access_tokens@github.com/jixiaod/jixiaod.github.io.git master</div><div class="line">branches:</div><div class="line">  only:</div><div class="line">    - hexo</div></pre></td></tr></table></figure>
<p>具体Travis 使用参见<a href="https://docs.travis-ci.com" target="_blank" rel="external">官方文档</a>，针对以上配置的说明流程如下：</p>
<ul>
<li>before_script：检出 master 分支代码到 public 文件目录。需要知道的是，在此之前 travis 已经帮你把hexo代码检出来了，实际上 public 目录是在 hexo 的目录之下。</li>
<li>script: <code>npm run build</code> 参见 <a href="https://github.com/jixiaod/jixiaod.github.io/blob/hexo/package.json" target="_blank" rel="external">package.json</a>，实际执行的是 <code>hexo generate</code>，从新生成了静态文件。</li>
<li>after_success: 最后把所有修改提交到了 master 分支，我们是用 <code>$Personal_access_tokens</code> 获得的 github 权限。</li>
</ul>
<p>现在发布一篇文章只需要：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">~(hexo)$ hexo new XXXX</div><div class="line">~(hexo)$ git add .</div><div class="line">~(hexo)$ git commit -m &apos;xxx&apos;</div><div class="line">~(hexo)$ git push origin hexo</div></pre></td></tr></table></figure>
<p>剩下的工作，travis 全都能搞定了，比原来简化了许多。</p>
<h4 id="俩个遇到的错误解决："><a href="#俩个遇到的错误解决：" class="headerlink" title="俩个遇到的错误解决："></a>俩个遇到的错误解决：</h4><ul>
<li>第一次 build 的时候报了一个莫名其妙的错误，“No Rakefile found (looking for: rakefile, Rakefile, rakefile.rb, Rakefile.rb)”。后来发现是Nodejs 的版本写的有问题。所以 build 前检查 <code>.travis.yml</code> 是否书写正确很有必要，可以在<a href="http://lint.travis-ci.org" target="_blank" rel="external">http://lint.travis-ci.org</a> 检查是否有问题。</li>
<li>Could not find .travis.yml, using standard configuration.<ul>
<li>从新同步 Travis CI 账号跟 Github的账号。在 Account 页面，点击 Sync acount</li>
<li>也可能是你弄错了build的分支，正在build的分支里确实没有 <code>.travis.yml</code> 文件。</li>
</ul>
</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前重新梳理了 &lt;a href=&quot;http://blog.100dos.com/2017/03/14/how-to-use-hexo-post-articles-on-multiple-pcs/&quot;&gt;Hexo 博客的发布流程&lt;/a&gt;，可以还是觉得麻烦。毕竟操作太多，容易出错。
    
    </summary>
    
      <category term="流程&amp;工具" scheme="http://blog.100dos.com/categories/%E6%B5%81%E7%A8%8B-%E5%B7%A5%E5%85%B7/"/>
    
    
      <category term="hexo" scheme="http://blog.100dos.com/tags/hexo/"/>
    
      <category term="travis-ci" scheme="http://blog.100dos.com/tags/travis-ci/"/>
    
  </entry>
  
  <entry>
    <title>理解 PHP 的写时复制和写时改变</title>
    <link href="http://blog.100dos.com/2017/03/24/copy-and-chage-on-write-with-php/"/>
    <id>http://blog.100dos.com/2017/03/24/copy-and-chage-on-write-with-php/</id>
    <published>2017-03-24T03:45:33.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>对于这样一段代码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">$a = <span class="number">1</span>;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div><div class="line">$b = $a;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div></pre></td></tr></table></figure>
<p>output<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">tiger➜/opt/work/tmp» /opt/php.5.6.25/bin/php ref_count.php                                                                                                                                             [11:17:01]</div><div class="line">a: (refcount=1, is_ref=0)=1</div><div class="line">a: (refcount=2, is_ref=0)=1</div></pre></td></tr></table></figure></p>
<p>第一行 <code>$a = 1</code>，第二行把 <code>$a</code> 赋值给 <code>$b</code>。这样的话，<code>$a</code> 和 <code>$b</code> 其实都是1，如果在内存中存两份的话，不仅多浪费一块内存，而且还多了分配和管理内存的开销。是的，PHP也是采取内存共享的策略，对于上面的一段代码，<code>$a</code> 和 <code>$b</code> 其实都是指向了一块共享内存，同一个 zval。</p>
<p><code>refcount</code> 就是当前的zval被引用的计数，当第一行代码<code>$a=1;</code> 创建这个变量时，只有 <code>$a</code> 指向这个zval，所以 <code>refcount</code> 等于1。当<code>$b=$a</code> 执行时，变量 <code>$b</code>也指向了这个 zval，所以 <code>refcount</code> 加1。</p>
<p>现在我们来看看什么是<strong>写时复制 Copy on Write</strong>，看下面一段代码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">$a = <span class="number">1</span>;</div><div class="line">$b = $a;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div><div class="line">$b = <span class="number">2</span>;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div></pre></td></tr></table></figure>
<p>output<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">tiger➜/opt/work/tmp» /opt/php.5.6.25/bin/php ref_count.php                                                                                                                                             [11:16:22]</div><div class="line">a: (refcount=2, is_ref=0)=1</div><div class="line">a: (refcount=1, is_ref=0)=1</div></pre></td></tr></table></figure></p>
<p>代码第四行，我们对 <code>$b</code> 重新赋值 2。PHP 在修改变量之前会先检查当前 zval 的 <code>refcount</code> 是否大于1，原本 <code>$a</code> 和 <code>$b</code> 都指向同一个zval，<code>refcount</code>  &gt; 1。PHP 就会复制出一个新的 zval 来，原来的 zval 的 <code>refcount</code> 减 1。使得变脸 <code>$a</code> 和 <code>$b</code> 分离，这个机制就是<strong>写时复制</strong> 。</p>
<p>再来看看什么是<strong>写时改变 Change on Write</strong></p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">$a = <span class="number">1</span>;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div><div class="line">$b =&amp; $a;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div><div class="line">$b = <span class="number">2</span>;</div><div class="line">xdebug_debug_zval(<span class="string">'a'</span>);</div></pre></td></tr></table></figure>
<p>output<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">tiger➜/opt/work/tmp» /opt/php.5.6.25/bin/php ref_count.php                                                                                                                                             [11:33:20]</div><div class="line">a: (refcount=1, is_ref=0)=1</div><div class="line">a: (refcount=2, is_ref=1)=1</div><div class="line">a: (refcount=2, is_ref=1)=2</div></pre></td></tr></table></figure></p>
<p>这段代码执行后，<code>$a</code> 也会呗修改成2。当 <code>$b =&amp; $a;</code> 执行时，<code>is_ref</code> 被设置为1。此时虽然 zal 的 <code>refcount</code> 是2，但是对 <code>$b</code> 重新赋值并不会触发写时复制，而是先判断了 <code>is_ref</code> 是否等于 1。<code>is_ref</code> 等于1 则不分离，这就是<strong>写时改变</strong>。</p>
<p>需要注意的是，我用的PHP的5.6.25版本，在PHP7中，由于zval的结构体进行了优化，所以并不适用。而且像 <code>debug_zval_dump</code> 也不好用了。</p>
<blockquote>
<p>debug_zval_dump’s refcount was already a bit broken before PHP 7 came along: since call-time pass-by-ref is not available, theoretically the refcount should always be 1 or 2, but that’s not terribly helpful. Then PHP 7 started making more changes to how variables move around.</p>
<p>Since 7.1.2 the example shows refcount(3), which is more useful but blatantly contradictory to the docs. <a href="https://3v4l.org/ntX3A" target="_blank" rel="external">https://3v4l.org/ntX3A</a> In PHP 5.6 it shows refcount(1) which is less useful but agrees with the docs.</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;对于这样一段代码：&lt;/p&gt;
&lt;figure class=&quot;highlight php&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;meta&quot;&gt;&amp;lt;?php&lt;/span&gt;&lt;/div&gt;&lt;d
    
    </summary>
    
      <category term="PHP" scheme="http://blog.100dos.com/categories/PHP/"/>
    
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
      <category term="写时复制" scheme="http://blog.100dos.com/tags/%E5%86%99%E6%97%B6%E5%A4%8D%E5%88%B6/"/>
    
      <category term="写时改变" scheme="http://blog.100dos.com/tags/%E5%86%99%E6%97%B6%E6%94%B9%E5%8F%98/"/>
    
  </entry>
  
  <entry>
    <title>深入理解 PHP7 之 REFERENCE</title>
    <link href="http://blog.100dos.com/2017/03/22/deeper-understanding-php7-reference/"/>
    <id>http://blog.100dos.com/2017/03/22/deeper-understanding-php7-reference/</id>
    <published>2017-03-22T07:53:49.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>转载：<a href="https://github.com/laruence/php7-internal/edit/master/reference.md" target="_blank" rel="external">https://github.com/laruence/php7-internal/edit/master/reference.md</a></p>
<h4 id="REFERENCE"><a href="#REFERENCE" class="headerlink" title="REFERENCE"></a>REFERENCE</h4><p> 上一章说过引用(REFERENCE)在PHP5的时候是一个标志位, 而在PHP7以后我们把它变成了一种新的类型:<code>IS_REFERNCE</code>. 然而引用是一种很常见的应用, 所以这个变化带来了很多的变化, 也给我们在做PHP7开发的时候, 因为有的时候疏忽忘了处理这个类型, 而带来不少的bug.</p>
<p> 最简单的情况, 就是在处理各种类型的时候, 从此以后我们要多考虑这种新的类型, 比如在PHP7中, 这样的代码形式就变得很常见了:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">try_again:</div><div class="line">swtich (Z_TYPE_P(zv)) &#123;</div><div class="line">    <span class="keyword">case</span> IS_TRING:</div><div class="line">    <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">case</span> IS_ARRAY:</div><div class="line">    <span class="keyword">break</span>;</div><div class="line">    ...</div><div class="line">    <span class="keyword">case</span> IS_REFERENCE:</div><div class="line">    zv = Z_REFVAL_P(zv); <span class="comment">//解引用</span></div><div class="line">    <span class="keyword">goto</span> try_again;</div><div class="line">    <span class="keyword">break</span>;  </div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p> 如果大家自己写的扩展, 如果忘了考虑这种新的类型, 那么就会导致问题.</p>
<h4 id="为什么"><a href="#为什么" class="headerlink" title="为什么?"></a>为什么?</h4><p> 那么既然这种新类型会带来这么多问题, 那么当时为什么要用把引用变成一种类型呢? 为什么不还是使用一个标志位呢?</p>
<p> 一句话来说, 就是我们不得不这么做. -_#</p>
<p> 前面说到, Hashtable直接存储的是zval, 这样在符号表中, 俩个zval如何共用一个数值呢? 对于字符串等复杂类型来说还好, 我们貌似可以在<code>zend_refcounted</code>结构中加入一个标志位来表明是引用来解决, 然而这个也会遇到Change On Write带来的复制, 但是我们知道在PHP7中, 一些类型是直接存储在zval中的, 比如<code>IS_LONG</code>, 但是引用类型是需要引用计数的, 那么对于一个是<code>IS_LONG</code>并且又是<code>IS_REFERNCE</code>的zval该如何表示呢?</p>
<p> 为此, 我们创造了这个新的类型:</p>
<p> <img src="/images/reference.png" alt="IS_REFERNCE"></p>
<p> 如图所示, 引用是一种新的类型:<code>zend_reference</code>, 对于<code>IS_REFERNCE</code>类型的zval, <code>zval.value.ref</code>是一个指向<code>zend_reference</code>的指针, 它包含了引用计数和一个zval, 具体的zval的值是存在<code>zval.value.ref-&gt;val</code>中的.</p>
<p> 所以对于<code>IS_LONG</code>的引用来说, 就用一个类型是<code>IS_REFERNCE</code>的zval, 它指向一个<code>zend_reference</code>, 而这个<code>zend_reference-&gt;val</code>中是一个类型为<code>IS_LONG</code>的zval.</p>
<h4 id="Change-On-Write"><a href="#Change-On-Write" class="headerlink" title="Change On Write"></a>Change On Write</h4><p> PHP采用引用计数来做简单的垃圾回收, 考虑如下的代码:<br><figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line"><span class="number">1.</span> $val = <span class="string">"laruence"</span>;</div><div class="line"><span class="number">2.</span> $ref = &amp;$val;</div><div class="line"><span class="number">3.</span> $copy = $val;</div><div class="line"><span class="meta">?&gt;</span></div></pre></td></tr></table></figure></p>
<p> <code>$ref</code>和<code>$val</code>是指向同一个zval的引用, 在PHP5的时候, 我们是通过一个引用计数为2, 并且引用标志位为1来表示这种情况, 当把<code>$val</code>复制给<code>$copy</code>(line 3)的时候, 我们发现$val是一个计数大于1的引用, 所以要产生Change on write, 也就是分离. 所以我们需要复制这个zval. </p>
<p> 而在PHP7中, 情况就变得简单了很多, 首先在引用赋值给<code>$ref</code>(line 2)的时候, 生成一个<code>IS_REFERNCE</code>类型, 然后因为此时有俩个变量引用它所以<code>zend_reference</code>这个结构的引用计数<code>zval.value.ref-&gt;gc.refcount</code>为2.</p>
<p> 再随后的赋值给<code>$copy</code>(line 3)的时候, 发现<code>$val</code>是一个引用, 于是让<code>$copy</code>指向的是<code>zval.value.ref-&gt;val</code>, 也就是字符串值为<code>laruence</code>的zval, 然后把zval的引用计数+1, 也就是<code>zval.value.ref-&gt;val.value.str.gc.refcount</code>为2. 并没有产生复制.</p>
<p> 从而这就很好的解决了上一章所说的PHP5的那个经典的问题, 比如我们在PHP7下运行上一章的那个问题, 我们得到的结果是:<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$ php-7.0/sapi/cli/php /tmp/1.php</div><div class="line">Used 0.00021380008539</div><div class="line">Used 0.00020173048281</div></pre></td></tr></table></figure></p>
<p> 可见确实没有发生复制, 从而不会产生任何的性能问题.</p>
<p>####</p>
<p>待续….</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;转载：&lt;a href=&quot;https://github.com/laruence/php7-internal/edit/master/reference.md&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://github.com/laruenc
    
    </summary>
    
      <category term="PHP" scheme="http://blog.100dos.com/categories/PHP/"/>
    
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
      <category term="PHP7" scheme="http://blog.100dos.com/tags/PHP7/"/>
    
      <category term="reference" scheme="http://blog.100dos.com/tags/reference/"/>
    
  </entry>
  
  <entry>
    <title>深入理解 PHP7 之 zval</title>
    <link href="http://blog.100dos.com/2017/03/22/deeper-understanding-php7-zval/"/>
    <id>http://blog.100dos.com/2017/03/22/deeper-understanding-php7-zval/</id>
    <published>2017-03-22T02:42:21.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>转载：<a href="https://github.com/laruence/php7-internal/blob/master/zval.md" target="_blank" rel="external">https://github.com/laruence/php7-internal/blob/master/zval.md</a></p>
<p>PHP7已经发布, 如承诺, 我也要开始这个系列的文章的编写, 今天我想先和大家聊聊zval的变化. 在讲zval变化的之前我们先来看看zval在PHP5下面是什么样子</p>
<h4 id="PHP5"><a href="#PHP5" class="headerlink" title="PHP5"></a>PHP5</h4><h5 id="zval回顾"><a href="#zval回顾" class="headerlink" title="zval回顾"></a>zval回顾</h5><p>在PHP5的时候, zval的定义如下:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> _<span class="title">zval_struct</span> &#123;</span></div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        <span class="keyword">long</span> lval;</div><div class="line">        <span class="keyword">double</span> dval;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            <span class="keyword">char</span> *val;</div><div class="line">            <span class="keyword">int</span> len;</div><div class="line">        &#125; str;</div><div class="line">        HashTable *ht;</div><div class="line">        zend_object_value obj;</div><div class="line">        zend_ast *ast;</div><div class="line">    &#125; value;</div><div class="line">    zend_uint refcount__gc;</div><div class="line">    zend_uchar type;</div><div class="line">    zend_uchar is_ref__gc;</div><div class="line">&#125;;</div></pre></td></tr></table></figure></p>
<p>对PHP5内核有了解的同学应该对这个结构比较熟悉, 因为zval可以表示一切PHP中的数据类型, 所以它包含了一个type字段, 表示这个zval存储的是什么类型的值, 常见的可能选项是<code>IS_NULL</code>, <code>IS_LONG</code>, <code>IS_STRING</code>, <code>IS_ARRAY</code>, <code>IS_OBJECT</code>等等.</p>
<p>根据type字段的值不同, 我们就要用不同的方式解读value的值, 这个value是个联合体, 比如对于type是<code>IS_STRING</code>, 那么我们应该用<code>value.str</code>来解读<code>zval.value</code>字段, 而如果type是<code>IS_LONG</code>, 那么我们就要用<code>value.lval</code>来解读.</p>
<p>另外, 我们知道PHP是用引用计数来做基本的垃圾回收的, 所以zval中有一个<code>refcount__gc</code>字段, 表示这个zval的引用数目, 但这里有一个要说明的, 在5.3以前, 这个字段的名字还叫做<code>refcount</code>, 5.3以后, 在引入新的垃圾回收算法来对付循环引用计数的时候, 作者加入了大量的宏来操作<code>refcount</code>, 为了能让错误更快的显现, 所以改名为<code>refcount__gc</code>, 迫使大家都使用宏来操作<code>refcount</code>.</p>
<p>类似的, 还有<code>is_ref</code>, 这个值表示了PHP中的一个类型是否是引用, 这里我们可以看到是不是引用是一个标志位.</p>
<p>这就是PHP5时代的zval, 在2013年我们做PHP5的opcache JIT的时候, 因为JIT在实际项目中表现不佳, 我们转而意识到这个结构体的很多问题. 而PHPNG项目就是从改写这个结构体而开始的.</p>
<h5 id="存在的问题"><a href="#存在的问题" class="headerlink" title="存在的问题"></a>存在的问题</h5><p>PHP5的zval定义是随着Zend Engine 2诞生的, 随着时间的推移, 当时设计的局限性也越来越明显:</p>
<p>首先这个结构体的大小是(在64位系统)24个字节, 我们仔细看这个<code>zval.value</code>联合体, 其中<code>zend_object_value</code>是最大的长板, 它导致整个value需要16个字节, 这个应该是很容易可以优化掉的, 比如把它挪出来, 用个指针代替,因为毕竟<code>IS_OBJECT</code>也不是最最常用的类型.</p>
<p>第二, 这个结构体的每一个字段都有明确的含义定义, 没有预留任何的自定义字段, 导致在PHP5时代做很多的优化的时候, 需要存储一些和zval相关的信息的时候, 不得不采用其他结构体映射, 或者外部包装后打补丁的方式来扩充zval, 比如5.3的时候新引入专门解决循环引用的GC, 它不得采用如下的比较hack的做法:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* The following macroses override macroses from zend_alloc.h */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">undef</span>  ALLOC_ZVAL</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> ALLOC_ZVAL(z)                                   \</span></div><div class="line">    <span class="keyword">do</span> &#123;                                                \</div><div class="line">        (z) = (zval*)emalloc(<span class="keyword">sizeof</span>(zval_gc_info));     \</div><div class="line">        GC_ZVAL_INIT(z);                                \</div><div class="line">    &#125; <span class="keyword">while</span> (<span class="number">0</span>)</div></pre></td></tr></table></figure></p>
<p>它用<code>zval_gc_info</code>劫持了zval的分配:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">zval_gc_info</span> &#123;</span></div><div class="line">    zval z;</div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        gc_root_buffer       *buffered;</div><div class="line">        <span class="class"><span class="keyword">struct</span> _<span class="title">zval_gc_info</span> *<span class="title">next</span>;</span></div><div class="line">    &#125; u;</div><div class="line">&#125; zval_gc_info;</div></pre></td></tr></table></figure></p>
<p>然后用<code>zval_gc_info</code>来扩充了zval, 所以实际上来说我们在PHP5时代申请一个zval其实真正的是分配了32个字节, 但其实GC只需要关心<code>IS_ARRAY和IS_OBJECT</code>类型, 这样就导致了大量的内存浪费.</p>
<p>还比如我之前做的Taint扩展, 我需要对于给一些字符串存储一些标记, zval里没有任何地方可以使用, 所以我不得不采用非常手段:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">Z_STRVAL_PP(ppzval) = erealloc(Z_STRVAL_PP(ppzval), Z_STRLEN_PP(ppzval) + <span class="number">1</span> + PHP_TAINT_MAGIC_LENGTH);</div><div class="line">PHP_TAINT_MARK(*ppzval, PHP_TAINT_MAGIC_POSSIBLE);</div></pre></td></tr></table></figure></p>
<p>就是把字符串的长度扩充一个int, 然后用magic number做标记写到后面去, 这样的做法安全性和稳定性在技术上都是没有保障的</p>
<p>第三, PHP的zval大部分都是按值传递, 写时拷贝的值, 但是有俩个例外, 就是对象和资源, 他们永远都是按引用传递, 这样就造成一个问题, 对象和资源在除了zval中的引用计数以外, 还需要一个全局的引用计数, 这样才能保证内存可以回收. 所以在PHP5的时代, 以对象为例, 它有俩套引用计数, 一个是zval中的, 另外一个是obj自身的计数:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">zend_object_store_bucket</span> &#123;</span></div><div class="line">    zend_bool destructor_called;</div><div class="line">    zend_bool valid;</div><div class="line">    <span class="keyword">union</span> _store_bucket &#123;</div><div class="line">        <span class="class"><span class="keyword">struct</span> _<span class="title">store_object</span> &#123;</span></div><div class="line">            <span class="keyword">void</span> *object;</div><div class="line">            <span class="keyword">zend_objects_store_dtor_t</span> dtor;</div><div class="line">            <span class="keyword">zend_objects_free_object_storage_t</span> free_storage;</div><div class="line">            <span class="keyword">zend_objects_store_clone_t</span> clone;</div><div class="line">            <span class="keyword">const</span> zend_object_handlers *handlers;</div><div class="line">            zend_uint refcount;</div><div class="line">            gc_root_buffer *buffered;</div><div class="line">        &#125; obj;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            <span class="keyword">int</span> next;</div><div class="line">        &#125; free_list;</div><div class="line">    &#125; bucket;</div><div class="line">&#125; zend_object_store_bucket;</div></pre></td></tr></table></figure></p>
<p>除了上面提到的两套引用以外, 如果我们要获取一个object, 则我们需要通过如下方式:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line">EG(objects_store).object_buckets[Z_OBJ_HANDLE_P(z)].bucket.obj</div></pre></td></tr></table></figure>
<p>经过漫长的多次内存读取, 才能获取到真正的objec对象本身. 效率可想而知.</p>
<p>这一切都是因为Zend引擎最初设计的时候, 并没有考虑到后来的对象. 一个良好的设计, 一旦有了意外, 就会导致整个结构变得复杂, 维护性降低, 这是一个很好的例子.</p>
<p>第四, 我们知道PHP中, 大量的计算都是面向字符串的, 然而因为引用计数是作用在zval的, 那么就会导致如果要拷贝一个字符串类型的zval, 我们别无他法只能复制这个字符串. 当我们把一个zval的字符串作为key添加到一个数组里的时候, 我们别无他法只能复制这个字符串. 虽然在PHP5.4的时候, 我们引入了INTERNED STRING, 但是还是不能根本解决这个问题.</p>
<p>还比如, PHP中大量的结构体都是基于Hashtable实现的, 增删改查Hashtable的操作占据了大量的CPU时间, 而字符串要查找首先要求它的Hash值, 理论上我们完全可以把一个字符串的Hash值计算好以后, 就存下来, 避免再次计算等等</p>
<p>第五, 这个是关于引用的, PHP5的时代, 我们采用写时分离, 但是结合到引用这里就有了一个经典的性能问题:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">dummy</span><span class="params">($array)</span> </span>&#123;&#125;</div><div class="line"></div><div class="line">    $array = range(<span class="number">1</span>, <span class="number">100000</span>);</div><div class="line"></div><div class="line">    $b = &amp;$array;</div><div class="line"></div><div class="line">    dummy($array);</div><div class="line"><span class="meta">?&gt;</span></div></pre></td></tr></table></figure>
<p>当我们调用dummy的时候, 本来只是简单的一个传值就行的地方, 但是因为$array曾经引用赋值给了$b, 所以导致$array变成了一个引用, 于是此处就会发生分离, 导致数组复制, 从而极大的拖慢性能, 这里有一个简单的测试:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">$array = range(<span class="number">1</span>, <span class="number">100000</span>);</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">dummy</span><span class="params">($array)</span> </span>&#123;&#125;</div><div class="line"></div><div class="line">$i = <span class="number">0</span>;</div><div class="line">$start = microtime(<span class="keyword">true</span>);</div><div class="line"><span class="keyword">while</span>($i++ &lt; <span class="number">100</span>) &#123;</div><div class="line">    dummy($array);</div><div class="line">&#125;</div><div class="line"></div><div class="line">printf(<span class="string">"Used %sS\n"</span>, microtime(<span class="keyword">true</span>) - $start);</div><div class="line"></div><div class="line">$b = &amp;$array; <span class="comment">//注意这里, 假设我不小心把这个Array引用给了一个变量</span></div><div class="line">$i = <span class="number">0</span>;</div><div class="line">$start = microtime(<span class="keyword">true</span>);</div><div class="line"><span class="keyword">while</span>($i++ &lt; <span class="number">100</span>) &#123;</div><div class="line">    dummy($array);</div><div class="line">&#125;</div><div class="line">printf(<span class="string">"Used %sS\n"</span>, microtime(<span class="keyword">true</span>) - $start);</div><div class="line"><span class="meta">?&gt;</span></div></pre></td></tr></table></figure>
<p>我们在5.6下运行这个例子, 得到如下结果:<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$ php-5.6/sapi/cli/php /tmp/1.php</div><div class="line">Used 0.00045204162597656S</div><div class="line">Used 4.2051479816437S</div></pre></td></tr></table></figure></p>
<p>相差1万倍之多. 这就造成, 如果在一大段代码中, 我不小心把一个变量变成了引用(比如foreach as &amp;$v), 那么就有可能触发到这个问题, 造成严重的性能问题, 然而却又很难排查.</p>
<p>第六, 也是最重要的一个, 为什么说它重要呢? 因为这点促成了很大的性能提升, 我们习惯了在PHP5的时代调用<code>MAKE_STD_ZVAL</code>在堆内存上分配一个zval, 然后对他进行操作, 最后呢通过<code>RETURN_ZVAL</code>把这个zval的值”copy”给<code>return_value</code>, 然后又销毁了这个zval, 比如<code>pathinfo</code>这个函数:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">PHP_FUNCTION(pathinfo)</div><div class="line">&#123;</div><div class="line">.....</div><div class="line">    MAKE_STD_ZVAL(tmp);</div><div class="line">    array_init(tmp);</div><div class="line">.....</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (opt == PHP_PATHINFO_ALL) &#123;</div><div class="line">        RETURN_ZVAL(tmp, <span class="number">0</span>, <span class="number">1</span>);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">.....</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>这个tmp变量, 完全是一个临时变量的作用, 我们又何必在堆内存分配它呢? <code>MAKE_STD_ZVAL/ALLOC_ZVAL</code>在PHP5的时候, 到处都有, 是一个非常常见的用法, 如果我们能把这个变量用栈分配, 那无论是内存分配, 还是缓存友好, 都是非常有利的</p>
<p>还有很多, 我就不一一详细列举了, 但是我相信你们也有了和我们当时一样的想法, zval必须得改改了, 对吧? </p>
<h4 id="PHP7"><a href="#PHP7" class="headerlink" title="PHP7"></a>PHP7</h4><h5 id="现在的zval"><a href="#现在的zval" class="headerlink" title="现在的zval"></a>现在的zval</h5><p>到了PHP7中, zval变成了如下的结构, 要说明的是, 这个是现在的结构, 已经和PHPNG时候有了一些不同了, 因为我们新增加了一些解释 (联合体的字段), 但是总体大小, 结构, 是和PHPNG的时候一致的:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> _<span class="title">zval_struct</span> &#123;</span></div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        zend_long         lval;             <span class="comment">/* long value */</span></div><div class="line">        <span class="keyword">double</span>            dval;             <span class="comment">/* double value */</span></div><div class="line">        zend_refcounted  *counted;</div><div class="line">        zend_string      *str;</div><div class="line">        zend_array       *arr;</div><div class="line">        zend_object      *obj;</div><div class="line">        zend_resource    *res;</div><div class="line">        zend_reference   *ref;</div><div class="line">        zend_ast_ref     *ast;</div><div class="line">        zval             *zv;</div><div class="line">        <span class="keyword">void</span>             *ptr;</div><div class="line">        zend_class_entry *ce;</div><div class="line">        zend_function    *func;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            <span class="keyword">uint32_t</span> w1;</div><div class="line">            <span class="keyword">uint32_t</span> w2;</div><div class="line">        &#125; ww;</div><div class="line">    &#125; value;</div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            ZEND_ENDIAN_LOHI_4(</div><div class="line">                zend_uchar    type,         <span class="comment">/* active type */</span></div><div class="line">                zend_uchar    type_flags,</div><div class="line">                zend_uchar    const_flags,</div><div class="line">                zend_uchar    reserved)     <span class="comment">/* call info for EX(This) */</span></div><div class="line">        &#125; v;</div><div class="line">        <span class="keyword">uint32_t</span> type_info;</div><div class="line">    &#125; u1;</div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        <span class="keyword">uint32_t</span>     var_flags;</div><div class="line">        <span class="keyword">uint32_t</span>     next;                 <span class="comment">/* hash collision chain */</span></div><div class="line">        <span class="keyword">uint32_t</span>     cache_slot;           <span class="comment">/* literal cache slot */</span></div><div class="line">        <span class="keyword">uint32_t</span>     lineno;               <span class="comment">/* line number (for ast nodes) */</span></div><div class="line">        <span class="keyword">uint32_t</span>     num_args;             <span class="comment">/* arguments number for EX(This) */</span></div><div class="line">        <span class="keyword">uint32_t</span>     fe_pos;               <span class="comment">/* foreach position */</span></div><div class="line">        <span class="keyword">uint32_t</span>     fe_iter_idx;          <span class="comment">/* foreach iterator index */</span></div><div class="line">    &#125; u2;</div><div class="line">&#125;;</div></pre></td></tr></table></figure></p>
<p>虽然看起来变得好大, 但其实你仔细看, 全部都是联合体, 这个新的zval在64位环境下,现在只需要16个字节(2个指针size), 它主要分为俩个部分, <code>value</code>和扩充字段, 而扩充字段又分为<code>u1</code>和<code>u2</code>俩个部分, 其中<code>u1</code>是type info,  <code>u2</code>是各种辅助字段.</p>
<p>其中<code>value</code>部分, 是一个<code>size_t</code>大小(一个指针大小), 可以保存一个指针, 或者一个<code>long</code>, 或者一个<code>double</code>.</p>
<p>而type info部分则保存了这个zval的类型. 扩充辅助字段则会在多个其他地方使用, 比如<code>next</code>, 就用在取代Hashtable中原来的拉链指针, 这部分会在以后介绍HashTable的时候再来详解.</p>
<h5 id="类型"><a href="#类型" class="headerlink" title="类型"></a>类型</h5><p>PHP7中的zval的类型做了比较大的调整, 总体来说有如下17种类型:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* regular data types */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_UNDEF                    0</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_NULL                     1</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_FALSE                    2</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_TRUE                     3</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_LONG                     4</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_DOUBLE                   5</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_STRING                   6</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_ARRAY                    7</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_OBJECT                   8</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_RESOURCE                 9</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_REFERENCE                10</span></div><div class="line"></div><div class="line"><span class="comment">/* constant expressions */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_CONSTANT                 11</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_CONSTANT_AST             12</span></div><div class="line"></div><div class="line"><span class="comment">/* fake types */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> _IS_BOOL                    13</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_CALLABLE                 14</span></div><div class="line"></div><div class="line"><span class="comment">/* internal types */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_INDIRECT                 15</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_PTR                      17</span></div></pre></td></tr></table></figure></p>
<p>其中PHP5的时候的<code>IS_BOOL</code>类型, 现在拆分成了<code>IS_FALSE</code>和<code>IS_TRUE</code>俩种类型. 而原来的引用是一个标志位, 现在的引用是一种新的类型.</p>
<p>对于<code>IS_INDIRECT</code>和<code>IS_PTR</code>来说, 这俩个类型是用在内部的保留类型, 用户不会感知到, 这部分会在后续介绍HashTable的时候也一并介绍.</p>
<p>从PHP7开始, 对于在zval的<code>value</code>字段中能保存下的值, 就不再对他们进行引用计数了, 而是在拷贝的时候直接赋值, 这样就省掉了大量的引用计数相关的操作, 这部分类型有:<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">IS_LONG</div><div class="line">IS_DOUBLE</div></pre></td></tr></table></figure></p>
<p>当然对于那种根本没有值, 只有类型的类型, 也不需要引用计数了:<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">IS_NULL</div><div class="line">IS_FALSE</div><div class="line">IS_TRUE</div></pre></td></tr></table></figure></p>
<p>而对于复杂类型, 一个<code>size_t</code>保存不下的, 那么我们就用<code>value</code>来保存一个指针, 这个指针指向这个具体的值, 引用计数也随之作用于这个值上, 而不在是作用于zval上了.</p>
<p><img src="/images/zval.png" alt="zval示意图"></p>
<p>以<code>IS_ARRAY</code>为例:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> _<span class="title">zend_array</span> &#123;</span></div><div class="line">    zend_refcounted_h gc;</div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            ZEND_ENDIAN_LOHI_4(</div><div class="line">                zend_uchar    flags,</div><div class="line">                zend_uchar    nApplyCount,</div><div class="line">                zend_uchar    nIteratorsCount,</div><div class="line">                zend_uchar    reserve)</div><div class="line">        &#125; v;</div><div class="line">        <span class="keyword">uint32_t</span> flags;</div><div class="line">    &#125; u;</div><div class="line">    <span class="keyword">uint32_t</span>          nTableMask;</div><div class="line">    Bucket           *arData;</div><div class="line">    <span class="keyword">uint32_t</span>          nNumUsed;</div><div class="line">    <span class="keyword">uint32_t</span>          nNumOfElements;</div><div class="line">    <span class="keyword">uint32_t</span>          nTableSize;</div><div class="line">    <span class="keyword">uint32_t</span>          nInternalPointer;</div><div class="line">    zend_long         nNextFreeElement;</div><div class="line">    <span class="keyword">dtor_func_t</span>       pDestructor;</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p><code>zval.value.arr</code>将指向上面的这样的一个结构体, 由它实际保存一个数组, 引用计数部分保存在<code>zend_refcounted_h</code>结构中:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">zend_refcounted_h</span> &#123;</span></div><div class="line">    <span class="keyword">uint32_t</span>         refcount;          <span class="comment">/* reference counter 32-bit */</span></div><div class="line">    <span class="keyword">union</span> &#123;</div><div class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></div><div class="line">            ZEND_ENDIAN_LOHI_3(</div><div class="line">                zend_uchar    type,</div><div class="line">                zend_uchar    flags,    <span class="comment">/* used for strings &amp; objects */</span></div><div class="line">                <span class="keyword">uint16_t</span>      gc_info)  <span class="comment">/* keeps GC root number (or 0) and color */</span></div><div class="line">        &#125; v;</div><div class="line">        <span class="keyword">uint32_t</span> type_info;</div><div class="line">    &#125; u;</div><div class="line">&#125; zend_refcounted_h;</div></pre></td></tr></table></figure></p>
<p>所有的复杂类型的定义, 开始的时候都是<code>zend_refcounted_h</code>结构, 这个结构里除了引用计数以外, 还有GC相关的结构. 从而在做GC回收的时候, GC不需要关心具体类型是什么, 所有的它都可以当做<code>zend_refcounted*</code>结构来处理.</p>
<p>另外有一个需要说明的就是大家可能会好奇的<code>ZEND_ENDIAN_LOHI_4</code>宏, 这个宏的作用是简化赋值, 它会保证在大端或者小端的机器上, 它定义的字段都按照一样顺序排列存储, 从而我们在赋值的时候, 不需要对它的字段分别赋值, 而是可以统一赋值, 比如对于上面的array结构为例, 就可以通过:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">arr1.u.flags = arr2.u.flags;</div></pre></td></tr></table></figure></p>
<p>一次完成相当于如下的赋值序列:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">arr1.u.v.flags              = arr2.u.v.flags;</div><div class="line">arr1.u.v.nApplyCount        = arr2.u.v.nApplyCount;</div><div class="line">arr1.u.v.nIteratorsCount    = arr2.u.v.nIteratorsCount;</div><div class="line">arr1.u.v.reserve            = arr2.u.v.reserve;</div></pre></td></tr></table></figure></p>
<p>还有一个大家可能会问到的问题是, 为什么不把type类型放到zval类型的前面, 因为我们知道当我们去用一个zval的时候, 首先第一点肯定是先去获取它的类型. 这里的一个原因是, 一个是俩者差别不大, 另外就是考虑到如果以后JIT的话, zval的类型如果能够通过类型推导获得, 就根本没有必要去读取它的type值了.</p>
<h5 id="标志位"><a href="#标志位" class="headerlink" title="标志位"></a>标志位</h5><p>除了数据类型以外， 以前的经验也告诉我们， 一个数据除了它的类型以外， 还应该有很多其他的属性， 比如对于INTERNED STRING，它是一种在整个PHP请求期都存在的字符串(比如你写在代码中的字面量), 它不会被引用计数回收. 在5.4的版本中我们是通过预先申请一块内存， 然后再这个内存中分配字符串， 最后用指针地址来比较， 如果一个字符串是属于INTERNED STRING的内存范围内， 就认为它是INTERNED STRING. 这样做的缺点显而易见， 就是当内存不够的时候， 我们就没有办法分配INTERNED STRING了， 另外也非常丑陋， 所以如果一个字符串能有一些属性定义则这个实现就可以变得很优雅.</p>
<p>还有， 比如现在我们对于<code>IS_LONG</code>, <code>IS_TRUE</code>等类型不再进行引用计数了， 那么当我们拿到一个zval的时候如何判断它需要不需要引用计数呢？ 想当然的我们可能会说用:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (Z_TYPE_P(zv) &gt;= IS_STRING) &#123;</div><div class="line">  <span class="comment">//需要引用计数</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>但是你忘了， 还有INTERNED STRING的存在啊， 所以你也许要这么写了：<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (Z_TYPE_P(zv) &gt;= IS_STRING &amp;&amp; !IS_INTERNED(Z_STR_P(zv))) &#123;</div><div class="line">  <span class="comment">//需要引用计数</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>是不是已经让你感觉到有点不对劲了? 嗯，别急， 还有呢， 我们还在5.6的时候引入了常量数组， 这个数组呢会存储在Opcache的共享内存中， 它也不需要引用计数：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (Z_TYPE_P(zv) &gt;= IS_STRING &amp;&amp; !IS_INTERNED(Z_STR_P(zv))</div><div class="line">    &amp;&amp; (Z_TYPE_P(zv) != IS_ARRAY || !Z_IS_IMMUTABLE(Z_ARRVAL(zv)))) &#123;</div><div class="line"> <span class="comment">//需要引用计数</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>你是不是也觉得这简直太丑陋了， 简直不能忍受这样墨迹的代码， 对吧？</p>
<p>是的，我们早想到了，回头看之前的zval定义， 注意到<code>type_flags</code>了么? 我们引入了一个标志位， 叫做<code>IS_TYPE_REFCOUNTED</code>, 它会保存在<code>zval.u1.v.type_flags</code>中， 我们对于需要引用计数的类型就赋予这个标志， 所以上面的判断就可以变得很优雅：<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (!(Z_TYPE_FLAGS(zv) &amp; IS_TYPE_REFCOUNTED)) &#123;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>而对于INTERNED STRING来说， 这个<code>IS_STR_INTERNED</code>标志位应该是作用于字符串本身而不是zval的.</p>
<p>那么类似这样的标志位一共有多少呢？作用于zval的有：<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">IS_TYPE_CONSTANT            <span class="comment">//是常量类型</span></div><div class="line">IS_TYPE_IMMUTABLE           <span class="comment">//不可变的类型， 比如存在共享内存的数组</span></div><div class="line">IS_TYPE_REFCOUNTED          <span class="comment">//需要引用计数的类型</span></div><div class="line">IS_TYPE_COLLECTABLE         <span class="comment">//可能包含循环引用的类型(IS_ARRAY, IS_OBJECT)</span></div><div class="line">IS_TYPE_COPYABLE            <span class="comment">//可被复制的类型， 还记得我之前讲的对象和资源的例外么？ 对象和资源就不是</span></div><div class="line">IS_TYPE_SYMBOLTABLE         <span class="comment">//zval保存的是全局符号表， 这个在我之前做了一个调整以后没用了， 但还保留着兼容，</span></div><div class="line">                            <span class="comment">//下个版本会去掉</span></div></pre></td></tr></table></figure></p>
<p>作用于字符串的有:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">IS_STR_PERSISTENT           <span class="comment">//是malloc分配内存的字符串</span></div><div class="line">IS_STR_INTERNED             <span class="comment">//INTERNED STRING</span></div><div class="line">IS_STR_PERMANENT            <span class="comment">//不可变的字符串， 用作哨兵作用</span></div><div class="line">IS_STR_CONSTANT             <span class="comment">//代表常量的字符串</span></div><div class="line">IS_STR_CONSTANT_UNQUALIFIED <span class="comment">//带有可能命名空间的常量字符串</span></div></pre></td></tr></table></figure></p>
<p>作用于数组的有:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_ARRAY_IMMUTABLE  <span class="comment">//同IS_TYPE_IMMUTABLE</span></span></div></pre></td></tr></table></figure></p>
<p>作用于对象的有：<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line">IS_OBJ_APPLY_COUNT          <span class="comment">//递归保护</span></div><div class="line">IS_OBJ_DESTRUCTOR_CALLED    <span class="comment">//析构函数已经调用</span></div><div class="line">IS_OBJ_FREE_CALLED          <span class="comment">//清理函数已经调用</span></div><div class="line">IS_OBJ_USE_GUARDS           <span class="comment">//魔术方法递归保护</span></div><div class="line">IS_OBJ_HAS_GUARDS           <span class="comment">//是否有魔术方法递归保护标志</span></div></pre></td></tr></table></figure></p>
<p>有了这些预留的标志位， 我们就会很方便的做一些以前不好做的事情， 就比如我自己的Taint扩展， 现在把一个字符串标记为污染的字符串就会变得无比简单：<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* it's important that make sure </span></div><div class="line"><span class="comment"> * this value is not used by Zend or</span></div><div class="line"><span class="comment"> * any other extension agianst string */</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_STR_TAINT_POSSIBLE    (1&lt;&lt;7)</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> TAINT_MARK(str)     (GC_FLAGS((str)) |= IS_STR_TAINT_POSSIBLE)</span></div></pre></td></tr></table></figure></p>
<p>这个标记就会一直随着这个字符串的生存而存在的， 省掉了我之前的很多tricky的做法.</p>
<h4 id="ZVAL预先分配"><a href="#ZVAL预先分配" class="headerlink" title="ZVAL预先分配"></a>ZVAL预先分配</h4><p> 前面我们说过, PHP5的zval分配采用的是堆上分配内存, 也就是在PHP预案代码中随处可见的MAKE_STD_ZVAL和ALLOC_ZVAL宏. 我们也知道了本来一个zval只需要24个字节, 但是算上gc_info, 其实分配了32个字节, 再加上PHP自己的内存管理在分配内存的时候都会在内存前面保留一部分信息:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">zend_mm_block</span> &#123;</span></div><div class="line">    zend_mm_block_info info;</div><div class="line"><span class="meta">#<span class="meta-keyword">if</span> ZEND_DEBUG</span></div><div class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> magic;</div><div class="line"><span class="meta"># <span class="meta-keyword">ifdef</span> ZTS</span></div><div class="line">    THREAD_T thread_id;</div><div class="line"><span class="meta"># <span class="meta-keyword">endif</span></span></div><div class="line">    zend_mm_debug_info debug;</div><div class="line"><span class="meta">#<span class="meta-keyword">elif</span> ZEND_MM_HEAP_PROTECTION</span></div><div class="line">    zend_mm_debug_info debug;</div><div class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></div><div class="line">&#125; zend_mm_block;</div></pre></td></tr></table></figure></p>
<p>  从而导致实际上我们只需要24字节的内存, 但最后竟然分配48个字节之多.</p>
<p>  然而大部分的zval, 尤其是扩展函数内的zval, 我们想想它接受的参数来自外部的zval, 它把返回值返回给return_value, 这个也是来自外部的zval, 而中间变量的zval完全可以采用栈上分配. 也就是说大部分的内部函数都不需要在堆上分配内存, 它需要的zval都可以来自外部.</p>
<p>  于是当时我们做了一个大胆的想法, 所有的zval都不需要单独申请. </p>
<p>  而这个也很容易证明, PHP脚本中使用的zval, 要么存在于符号表, 要么就以临时变量(<code>IS_TMP_VAR</code>)或者编译变量(<code>IS_CV</code>)的形式存在. 前者存在于一个Hashtable中, 而在PHP7中Hashtable默认保存的就是zval, 这部分的zval完全可以在Hashtable分配的时候一次性分配出来, 后面的存在于execute_data之后, 数量也在编译时刻确定好了, 也可以随着execute_data一次性分配, 所以我们确实不再需要单独在堆上申请zval了.</p>
<p>  所以, 在PHP7开始, 我们移除了MAKE_STD_ZVAL/ALLOC_ZVAL宏, 不再支持存堆内存上申请zval. 函数内部使用的zval要么来自外面输入, 要么使用在栈上分配的临时zval.</p>
<p>  在后来的实践中, 总结出来的可能对于开发者来说最大的变化就是, 之前的一些内部函数, 通过一些操作获得一些信息, 然后分配一个zval, 返回给调用者的情况:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">static</span> zval * <span class="title">php_internal_function</span><span class="params">()</span> </span>&#123;</div><div class="line">    .....</div><div class="line">    str = external_function();</div><div class="line"> </div><div class="line">    MAKE_STD_ZVAL(zv);</div><div class="line"></div><div class="line">    ZVAL_STRING(zv, str, <span class="number">0</span>);</div><div class="line"></div><div class="line">    <span class="keyword">return</span> zv;</div><div class="line">&#125;</div><div class="line">PHP_FUNCTION(test) &#123;</div><div class="line">    RETURN_ZVAL(php_internal_function(), <span class="number">1</span>, <span class="number">1</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>  要么修改为, 这个zval由调用者传递:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">php_internal_function</span><span class="params">(zval *zv)</span> </span>&#123;</div><div class="line">    .....</div><div class="line">    str = external_function();</div><div class="line"> </div><div class="line">    ZVAL_STRING(zv, str);</div><div class="line">    efree(str);</div><div class="line">&#125;</div><div class="line"></div><div class="line">PHP_FUNCTION(test) &#123;</div><div class="line">    php_internal_function(return_value);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>   要么修改为, 这个函数返回原始素材:<br><figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">char</span> * <span class="title">php_internal_function</span><span class="params">()</span> </span>&#123;</div><div class="line">    .....</div><div class="line">    str = external_function();</div><div class="line">    <span class="keyword">return</span> str;</div><div class="line">&#125;</div><div class="line"></div><div class="line">PHP_FUNCTION(test) &#123;</div><div class="line">    str = php_internal_function();</div><div class="line">    RETURN_STRING(str);</div><div class="line">    efree(str);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>  (这块还没想好怎么说, 本来我是要引出Hashtable不再存在zval**, 从而引出引用类型的存在的必要性, 但是如果不先讲Hashtable的结构, 这个引出貌似很突兀, 先这么着吧, 以后再来修改)</p>
<p>  到现在我们基本上把zval的变化概况介绍完毕, 抽象的来说, 其实在PHP7中的zval, 已经变成了一个值指针, 它要么保存着原始值, 要么保存着指向一个保存原始值的指针. 也就是说现在的zval相当于PHP5的时候的zval <em>. 只不过相比于zval </em>, 直接存储zval, 我们可以省掉一次指针解引用, 从而提高缓存友好性.</p>
<p>  其实PHP7的性能, 我们并没有引入什么新的技术模式, 不过就是主要来自, 持续不懈的降低内存占用, 提高缓存友好性, 降低执行的指令数的这些原则而来的, 可以说PHP7的重构就是这三个原则.</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;转载：&lt;a href=&quot;https://github.com/laruence/php7-internal/blob/master/zval.md&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://github.com/laruence/php
    
    </summary>
    
      <category term="PHP" scheme="http://blog.100dos.com/categories/PHP/"/>
    
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
      <category term="PHP7" scheme="http://blog.100dos.com/tags/PHP7/"/>
    
      <category term="zval" scheme="http://blog.100dos.com/tags/zval/"/>
    
  </entry>
  
  <entry>
    <title>如何使用 hexo 在多台 PC 发布文章？</title>
    <link href="http://blog.100dos.com/2017/03/14/how-to-use-hexo-post-articles-on-multiple-pcs/"/>
    <id>http://blog.100dos.com/2017/03/14/how-to-use-hexo-post-articles-on-multiple-pcs/</id>
    <published>2017-03-14T04:11:57.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>最近公司和家里各有一台电脑，有需求在俩台电脑上都能发布 Hexo 文章。怎么办呢？</p>
<p><a href="https://www.zhihu.com/question/21193762" target="_blank" rel="external">知乎上的回答提供了思路</a>，即创建俩个分支 master 和 hexo，下面的方案大体一样，具体操作流程详细描述一下。如何在PC1上<a href="http://blog.100dos.com/2016/05/15/build-blog-sys-by-hexo-on-github-pages/">创建 Hexo 项目</a>，可以参考之前的文章。如果像我一样已经有在线的 Github Pages，可以考虑重新创建 Project，这样可以保证 master 和 hexo 代码纯净。</p>
<p>重新部署 hexo：</p>
<ol>
<li>重新创建 hexo 项目 <code>hexo init blog.100dos.com</code> ，拷贝之前的 _config.yml、 theme、README.md、CNAME 等资源到新的hexo</li>
<li>提交代码到master，然后创建 hexo 分支，切换到 hexo 分支</li>
<li>确保 hexo 分支的代码 <code>hexo server</code> 运行时，跟你之前的博客一样</li>
<li>提交 hexo 分支代码，切换到 master，<code>git merge hexo</code> 合并刚刚的修改（如果有的话）</li>
<li>最后在查看 <code>hexo server</code> master 分支下的代码，没问题的话，准备提交github。如果有问题，在 hexo 分支修改，然后再 merge 到 master</li>
<li>删除 Github 上的原Project</li>
<li>提交 hexo 分支到Github，<code>git push origin hexo</code></li>
<li>部署 master 分支，<code>hexo d --g</code></li>
<li>OK完成重新部署工作。</li>
</ol>
<p>如何在其他电脑发布文章？</p>
<ol>
<li>检出代码 git clone git@github.com:jixiaod/jixiaod.github.io.git</li>
<li>切换到 hexo 分支，创建文章 <code>hexo new xxxxx</code>，文章编写完成后，提交</li>
<li>切换到master，合并hexo分支新编写的文章，<code>git merge hexo</code></li>
<li>部署 master 分支，<code>hexo d --g</code>，发布新文章成功</li>
</ol>
<p>亲测，有效。可以查看<a href="https://github.com/jixiaod/jixiaod.github.io" target="_blank" rel="external">我的 Github Hexo 代码</a>。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近公司和家里各有一台电脑，有需求在俩台电脑上都能发布 Hexo 文章。怎么办呢？&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://www.zhihu.com/question/21193762&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;知乎上的回答提
    
    </summary>
    
      <category term="流程&amp;工具" scheme="http://blog.100dos.com/categories/%E6%B5%81%E7%A8%8B-%E5%B7%A5%E5%85%B7/"/>
    
    
      <category term="hexo" scheme="http://blog.100dos.com/tags/hexo/"/>
    
  </entry>
  
  <entry>
    <title>图解 autoscan、aclocal、automake、autoconf、autoheader、configure</title>
    <link href="http://blog.100dos.com/2017/03/14/understand-aclocal-autoconf-automake-autoheader-configure/"/>
    <id>http://blog.100dos.com/2017/03/14/understand-aclocal-autoconf-automake-autoheader-configure/</id>
    <published>2017-03-14T03:02:23.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<h3 id="autoscan-autoconf"><a href="#autoscan-autoconf" class="headerlink" title="autoscan (autoconf)"></a>autoscan (autoconf)</h3><p>扫描源代码以搜寻普通的可移植性问题，比如检查编译器、库、头文件等，生成文件configure.scan，它是configure.ac的一个雏形。</p>
<h3 id="aclocal-automake"><a href="#aclocal-automake" class="headerlink" title="aclocal (automake)"></a>aclocal (automake)</h3><p>根据已经安装的宏，用户定义宏和 acinclude.m4 文件中的宏将 configure.ac 文件所需要的宏集中定义到文件 aclocal.m4 中。aclocal是一个 perl 脚本程序，它的定义是：</p>
<blockquote>
<p>aclocal - create aclocal.m4 by scanning configure.ac</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">user input files   optional input     process          output files</div><div class="line">================   ==============     =======          ============</div><div class="line"> </div><div class="line">                    acinclude.m4 - - - - -.</div><div class="line">                                          V</div><div class="line">                                      .-------,</div><div class="line">configure.ac ------------------------&gt;|aclocal|</div><div class="line">                 &#123;user macro files&#125; -&gt;|       |------&gt; aclocal.m4</div><div class="line">                                      `-------&apos;</div></pre></td></tr></table></figure>
<h3 id="autoheader-autoconf"><a href="#autoheader-autoconf" class="headerlink" title="autoheader(autoconf)"></a>autoheader(autoconf)</h3><p>根据 configure.ac 中的某些宏，比如 cpp 宏定义，运行 m4，生成 autoconfig.h.in</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">user input files    optional input     process          output files</div><div class="line">================    ==============     =======          ============</div><div class="line"> </div><div class="line">                    aclocal.m4 - - - - - - - .</div><div class="line">                                             |</div><div class="line">                                             V</div><div class="line">                                     .----------,</div><div class="line">configure.ac -----------------------&gt;|autoheader|----&gt; autoconfig.h.in</div><div class="line">                                     `----------&apos;</div></pre></td></tr></table></figure>
<h3 id="automake"><a href="#automake" class="headerlink" title="automake"></a>automake</h3><p>automake 将 Makefile.am 中定义的结构建立 Makefile.in，然后 configure 脚本将生成的 Makefile.in 文件转换为 Makefile。如果在 configure.ac 中定义了一些特殊的宏，比如 <code>AC_PROG_LIBTOOL</code>，它会调用 libtoolize，否则它会自己产生 config.guess 和 config.sub。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">user input files   optional input   processes          output files</div><div class="line">================   ==============   =========          ============</div><div class="line"> </div><div class="line">                                     .--------,</div><div class="line">                                     |        | - - -&gt; COPYING</div><div class="line">                                     |        | - - -&gt; INSTALL</div><div class="line">                                     |        |------&gt; install-sh</div><div class="line">                                     |        |------&gt; missing</div><div class="line">                                     |automake|------&gt; mkinstalldirs</div><div class="line">configure.ac -----------------------&gt;|        |</div><div class="line">Makefile.am  -----------------------&gt;|        |------&gt; Makefile.in</div><div class="line">                                     |        |------&gt; stamp-h.in</div><div class="line">                                 .---+        | - - -&gt; config.guess</div><div class="line">                                 |   |        | - - -&gt; config.sub</div><div class="line">                                 |   `------+-&apos;</div><div class="line">                                 |          | - - - -&gt; config.guess</div><div class="line">                                 |libtoolize| - - - -&gt; config.sub</div><div class="line">                                 |          |--------&gt; ltmain.sh</div><div class="line">                                 |          |--------&gt; ltconfig</div><div class="line">                                 `----------&apos;</div></pre></td></tr></table></figure>
<h3 id="autoconf"><a href="#autoconf" class="headerlink" title="autoconf"></a>autoconf</h3><p>将 configure.ac 中的宏展开，生成 configure 脚本。这个过程可能要用到 aclocal.m4 中定义的宏。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">user input files   optional input   processes          output files</div><div class="line">================   ==============   =========          ============</div><div class="line"> </div><div class="line">                   aclocal.m4 - - - - - -.</div><div class="line">                                         V</div><div class="line">                                     .--------,</div><div class="line">configure.ac -----------------------&gt;|autoconf|------&gt; configure -----&gt;autoconfig.h, Makefile</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;autoscan-autoconf&quot;&gt;&lt;a href=&quot;#autoscan-autoconf&quot; class=&quot;headerlink&quot; title=&quot;autoscan (autoconf)&quot;&gt;&lt;/a&gt;autoscan (autoconf)&lt;/h3&gt;&lt;p&gt;扫描源代码以
    
    </summary>
    
      <category term="C/C++" scheme="http://blog.100dos.com/categories/C-C/"/>
    
    
      <category term="autoscan" scheme="http://blog.100dos.com/tags/autoscan/"/>
    
      <category term="automake" scheme="http://blog.100dos.com/tags/automake/"/>
    
      <category term="configure" scheme="http://blog.100dos.com/tags/configure/"/>
    
  </entry>
  
  <entry>
    <title>Centos7 安装 Gitlab-CE</title>
    <link href="http://blog.100dos.com/2017/03/03/installing-gitlab-ce-on-centos7/"/>
    <id>http://blog.100dos.com/2017/03/03/installing-gitlab-ce-on-centos7/</id>
    <published>2017-03-03T03:30:02.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<ol>
<li>安装配置依赖项<br>如想使用Postfix来发送邮件,在安装期间请选择’Internet Site’. 您也可以用sendmai或者 配置SMTP服务 并 使用SMTP发送邮件.<br>在 Centos 6 和 7 系统上, 下面的命令将在系统防火墙里面开放HTTP和SSH端口.</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">sudo yum install curl policycoreutils openssh-server openssh-clients</div><div class="line">sudo systemctl enable sshd</div><div class="line">sudo systemctl start sshd</div><div class="line">sudo yum install postfix</div><div class="line">sudo systemctl enable postfix</div><div class="line">sudo systemctl start postfix</div><div class="line">sudo firewall-cmd --permanent --add-service=http</div><div class="line">sudo systemctl reload firewalld</div></pre></td></tr></table></figure>
<p>如果提示防火墙没打开，执行<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">systemctl start firewalld</div></pre></td></tr></table></figure></p>
<ol>
<li>添加GitLab仓库,并安装到服务器上<br>curl -sS <a href="http://packages.gitlab.cc/install/gitlab-ce/script.rpm.sh" target="_blank" rel="external">http://packages.gitlab.cc/install/gitlab-ce/script.rpm.sh</a> | sudo bash<br>sudo yum install gitlab-ce</li>
</ol>
<ol>
<li><p>启动GitLab</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">sudo gitlab-ctl reconfigure</div></pre></td></tr></table></figure>
</li>
<li><p>使用浏览器访问GitLab</p>
</li>
</ol>
<p>首次访问GitLab,系统会让你重新设置管理员的密码,设置成功后会返回登录界面.<br>默认的管理员账号是root,如果你想更改默认管理员账号,请输入上面设置的新密码登录系统后修改帐号</p>
<p>5.配置域名访问<br>以上基本是官方内容，以下配置自己的域名。首先添加 godaddy的域名 A 记录到服务器IP。</p>
<p>在文件<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">vi /etc/gitlab/gitlab.rb</div></pre></td></tr></table></figure></p>
<p>找到nginx的配置文件位置，并修改默认域名为自己的域名。</p>
<p>更多可官方安装文档 ：<a href="https://www.gitlab.cc/downloads/#centos7" target="_blank" rel="external">https://www.gitlab.cc/downloads/#centos7</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;ol&gt;
&lt;li&gt;安装配置依赖项&lt;br&gt;如想使用Postfix来发送邮件,在安装期间请选择’Internet Site’. 您也可以用sendmai或者 配置SMTP服务 并 使用SMTP发送邮件.&lt;br&gt;在 Centos 6 和 7 系统上, 下面的命令将在系统防火墙里面开放H
    
    </summary>
    
      <category term="linux" scheme="http://blog.100dos.com/categories/linux/"/>
    
    
      <category term="centos" scheme="http://blog.100dos.com/tags/centos/"/>
    
      <category term="gitlab" scheme="http://blog.100dos.com/tags/gitlab/"/>
    
  </entry>
  
  <entry>
    <title>CentOS 7 从零开始搭建 LNMP 环境（nginx/1.10.3 PHP/7.1.2 Mysql/5.7.17）</title>
    <link href="http://blog.100dos.com/2017/03/03/installing-centos7-nginx1-10-3-php7-mysql5-7/"/>
    <id>http://blog.100dos.com/2017/03/03/installing-centos7-nginx1-10-3-php7-mysql5-7/</id>
    <published>2017-03-03T02:10:36.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>最近买了 Linode VPS 服务。5刀每月，只有1G的内存，编译 Mysql 内存溢出，然后升级到2G内存的版本，每月10刀……以下纪录 LNMP 环境安装过程：</p>
<h2 id="安装-Nginx"><a href="#安装-Nginx" class="headerlink" title="安装 Nginx"></a>安装 Nginx</h2><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># yum install wget</span></div><div class="line">~<span class="comment"># wget http://nginx.org/download/nginx-1.10.3.tar.gz</span></div><div class="line">~<span class="comment"># tar zxf nginx-1.10.3.tar.gz</span></div><div class="line">~<span class="comment"># cd nginx-1.10.3</span></div><div class="line"></div><div class="line">~<span class="comment"># yum install pcre-devel pcre openssl openssl-devel</span></div><div class="line">~<span class="comment"># yum install gcc</span></div><div class="line">~<span class="comment"># ./configure --prefix=/opt/nginx</span></div><div class="line">~<span class="comment"># make &amp; make install</span></div></pre></td></tr></table></figure>
<p>修改配置文件，启动 Nginx服务<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># /opt/nginx/sbin/nginx</span></div></pre></td></tr></table></figure></p>
<p>重启<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># /opt/nginx/sbin/nginx -s reload</span></div></pre></td></tr></table></figure></p>
<h2 id="安装-PHP"><a href="#安装-PHP" class="headerlink" title="安装 PHP"></a>安装 PHP</h2><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># wget http://jp2.php.net/get/php-7.1.2.tar.gz/from/this/mirror</span></div><div class="line">~<span class="comment"># tar zxf mirror</span></div><div class="line">~<span class="comment"># cd php-7.1.2</span></div></pre></td></tr></table></figure>
<figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># yum install zlib-devel zlib libxml2 libxml2-devel  libpng libpng-devel libcurl-devel curl libcurl libzip-devel libzip gd bzip2-devel bzip2</span></div><div class="line"></div><div class="line">~<span class="comment"># ./configure --prefix=/opt/php  --enable-fpm --with-openssl  --with-zlib  --with-mysqli=mysqlnd  --enable-pcntl --enable-soap --enable-zip --enable-sockets --enable-mbstring  --with-curl --with-pcre-regex  --with-bz2 --with-gd  --with-pdo-mysql=mysqlnd  --with-mysqli=mysqlnd   --enable-maintainer-zts</span></div><div class="line"></div><div class="line">~<span class="comment"># make &amp; make install</span></div></pre></td></tr></table></figure>
<p>启动 php-fpm，使用默认的配置文件<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># cd /opt/php</span></div><div class="line">~<span class="comment"># mv etc/php-fpm.conf.default etc/php-fpm.conf</span></div><div class="line">~<span class="comment"># mv etc/php-fpm.d/www.conf.default etc/php-fpm.d/www.conf</span></div><div class="line">~<span class="comment"># sbin/php-fpm -y etc/php-fpm.conf</span></div></pre></td></tr></table></figure></p>
<h2 id="安装-Mysql"><a href="#安装-Mysql" class="headerlink" title="安装 Mysql"></a>安装 Mysql</h2><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># wget https://dev.mysql.com/get/Downloads/MySQL-5.7/mysql-5.7.17.tar.gz</span></div><div class="line">~<span class="comment"># tar zxf mysql-5.7.17.tar.gz</span></div><div class="line">~<span class="comment"># cd mysql-5.7.17</span></div><div class="line"></div><div class="line">// cmake安装mysql，会自行下载并安装boost</div><div class="line"></div><div class="line">~<span class="comment"># yum install  cmake gcc gcc-c++ ncurses-devel cmake libaio  bison</span></div><div class="line"></div><div class="line">~<span class="comment"># cmake . -DCMAKE_INSTALL_PREFIX=/opt/mysql -DWITH_BOOST=/opt/boost/ -DMYSQL_DATADIR=/opt/mysql/data -DWITH_INNOBASE_STORAGE_ENGINE=1 -DMYSQL_UNIX_ADDR==/opt/mysql/mysql.sock -DMYSQL_USER=mysql -DDEFAULT_CHARSET=utf8 -DDEFAULT_COLLATION=utf8_general_ci</span></div><div class="line"></div><div class="line">~<span class="comment"># make &amp; make install</span></div></pre></td></tr></table></figure>
<p>创建 Mysql 账户<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># groupadd mysql</span></div><div class="line">~<span class="comment"># useradd -r -g mysql -s /bin/false mysql</span></div><div class="line">~<span class="comment"># chown -R mysql .</span></div><div class="line">~<span class="comment"># chgrp -R mysql .</span></div></pre></td></tr></table></figure></p>
<p>启动 Mysql 服务</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">//生成初始化 Mysql 帐号root 密码</div><div class="line">[root@li1571-79 mysql]<span class="comment"># bin/mysqld --initialize --user=mysql</span></div><div class="line">2017-03-03T02:59:23.230496Z 0 [Warning] TIMESTAMP with implicit DEFAULT value is deprecated. Please use --explicit_defaults_for_timestamp server option (see documentation <span class="keyword">for</span> more details).</div><div class="line">2017-03-03T02:59:23.576085Z 0 [Warning] InnoDB: New <span class="built_in">log</span> files created, LSN=45790</div><div class="line">2017-03-03T02:59:23.628088Z 0 [Warning] InnoDB: Creating foreign key constraint system tables.</div><div class="line">2017-03-03T02:59:23.685971Z 0 [Warning] No existing UUID has been found, so we assume that this is the first time that this server has been started. Generating a new UUID: 67ce62cb-ffbd-11e6-a9b9-f23c91e57db2.</div><div class="line">2017-03-03T02:59:23.688763Z 0 [Warning] Gtid table is not ready to be used. Table <span class="string">'mysql.gtid_executed'</span> cannot be opened.</div><div class="line">2017-03-03T02:59:23.690301Z 1 [Note] A temporary password is generated <span class="keyword">for</span> root@localhost: &amp;H4FpSyD/b6&lt;</div></pre></td></tr></table></figure>
<p>最后的 <code>&amp;H4FpSyD/b6&lt;</code> 即是生成的临时账户，启动 Mysql 服务<br><figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">~<span class="comment"># cp support-files/mysql.server /opt/mysql/bin</span></div><div class="line">~<span class="comment"># cd /opt/mysql</span></div><div class="line">~<span class="comment"># bin/mysql.server start</span></div></pre></td></tr></table></figure></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">[root@li1571-79 mysql]<span class="comment"># bin/mysql -uroot -p -h127.0.0.1 -P3306</span></div><div class="line">Enter password:</div><div class="line">输入临时帐号密码，即可进入mysql 客户端。第一次登录，强制需要修改临时密码</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">mysql&gt; alter user &apos;root&apos;@&apos;localhost&apos; identified by &apos;123abc&apos;;</div><div class="line">Query OK, 0 rows affected (0.01 sec)</div><div class="line"></div><div class="line">mysql&gt; show databases;</div><div class="line">+--------------------+</div><div class="line">| Database           |</div><div class="line">+--------------------+</div><div class="line">| information_schema |</div><div class="line">| mysql              |</div><div class="line">| performance_schema |</div><div class="line">| sys                |</div><div class="line">+--------------------+</div><div class="line">4 rows in set (0.01 sec)</div></pre></td></tr></table></figure>
<p>到这里，LNMP 到这里LNMP 初始默认环境搭建完成。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近买了 Linode VPS 服务。5刀每月，只有1G的内存，编译 Mysql 内存溢出，然后升级到2G内存的版本，每月10刀……以下纪录 LNMP 环境安装过程：&lt;/p&gt;
&lt;h2 id=&quot;安装-Nginx&quot;&gt;&lt;a href=&quot;#安装-Nginx&quot; class=&quot;head
    
    </summary>
    
      <category term="linux" scheme="http://blog.100dos.com/categories/linux/"/>
    
    
      <category term="mysql" scheme="http://blog.100dos.com/tags/mysql/"/>
    
      <category term="PHP" scheme="http://blog.100dos.com/tags/PHP/"/>
    
      <category term="linux" scheme="http://blog.100dos.com/tags/linux/"/>
    
      <category term="nginx" scheme="http://blog.100dos.com/tags/nginx/"/>
    
  </entry>
  
  <entry>
    <title>进程和线程</title>
    <link href="http://blog.100dos.com/2017/02/27/processes-and-threads/"/>
    <id>http://blog.100dos.com/2017/02/27/processes-and-threads/</id>
    <published>2017-02-27T03:11:14.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://zh.wikipedia.org/wiki/行程" target="_blank" rel="external">进程</a>（process）是计算机中已运行程序的实体。关于进程需要知道的几点：</p>
<ul>
<li>进程是程序的基本执行实体。</li>
<li>在面向线程设计的系统（如当代多数操作系统、Linux 2.6及更新的版本）中，进程本身不是基本运行单位，而是线程的容器。</li>
<li>程序本身只是指令、数据及其组织形式的描述，进程才是程序（那些指令和数据）的真正运行实例。</li>
<li>若干进程有可能与同一个程序相关系，且每个进程皆可以同步（循序）或异步（平行）的方式独立运行。</li>
<li>现代计算机系统可在同一段时间内以进程的形式将多个程序加载到存储器中，并借由时间共享（或称时分复用），以在一个处理器上表现出同时（平行性）运行的感觉。</li>
<li>同样的，使用多线程技术（多线程即每一个线程都代表一个进程内的一个独立执行上下文）的操作系统或计算机架构，同样程序的平行线程，可在多CPU主机或网络上真正同时运行（在不同的CPU上）。</li>
<li>进程需要一些资源才能完成工作，如CPU使用时间、存储器、文件以及I/O设备，且为依序逐一进行，也就是每个CPU核心任何时间内仅能运行一项进程。</li>
<li>进程在运行时，状态（state）会改变。进城的状态包括：<ul>
<li>新生（new）：进程新产生中。</li>
<li>运行（running）：正在运行。</li>
<li>等待（waiting）：等待某事发生，例如等待用户输入完成。亦称“阻塞”（blocked）</li>
<li>就绪（ready）：排班中，等待CPU。</li>
<li>结束（terminated）：完成运行。</li>
</ul>
</li>
<li>进程间通信（IPC Inter-process communication）:每个进程都有自己的一部分独立的系统资源，彼此是隔离的。为了能使不同的进程互相访问资源并进行协调工作，才有了进程间通信。</li>
<li>进程控制块是操作系统能够支持多进程和提供多处理的结构。 </li>
<li>当操作系统做进程切换时，它会执行两步操作，一是中断当前处理器中的进程，二是执行下一个进程。 不管是中断还是执行，进程控制块中的程序计数器、上下文数据和进程状态都会发生变化。</li>
</ul>
<p>在 PHP 中的 <a href="http://php.net/manual/zh/book.pcntl.php" target="_blank" rel="external">PCNTL</a> 扩展实现了Unix方式的进程创建，程序执行，信号处理以及进程的中断。但是只能 CLI 环境下使用，且在 Windows 平台不可用。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line"></div><div class="line">$pid = pcntl_fork();</div><div class="line"><span class="comment">//父进程和子进程都会执行下面代码</span></div><div class="line"><span class="keyword">if</span> ($pid == <span class="number">-1</span>) &#123;</div><div class="line">    <span class="comment">//错误处理：创建子进程失败时返回-1.</span></div><div class="line">     <span class="keyword">die</span>(<span class="string">'could not fork'</span>);</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> ($pid) &#123;</div><div class="line">     <span class="comment">//父进程会得到子进程号，所以这里是父进程执行的逻辑</span></div><div class="line">     pcntl_wait($status); <span class="comment">//等待子进程中断，防止子进程成为僵尸进程。</span></div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">     <span class="comment">//子进程得到的$pid为0, 所以这里是子进程执行的逻辑。</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><a href="https://zh.wikipedia.org/wiki/线程" target="_blank" rel="external">线程</a>（thread）是操作系统能够进行运算调度的最小单位。</p>
<ul>
<li>它被包含在进程之中，是进程中的实际运作单位。</li>
<li>一条线程指的是进程中一个单一顺序的控制流，一个进程中可以并发多个线程，每条线程并行执行不同的任务。</li>
<li>在Unix System V及SunOS中也被称为轻量进程（lightweight processes），但轻量进程更多指内核线程（kernel thread），而把用户线程（user thread）称为线程。</li>
<li>线程是独立调度和分派的基本单位。线程可以操作系统内核调度的内核线程，如Win32线程；由用户进程自行调度的用户线程，如Linux平台的POSIX Thread；或者由内核与用户进程，如Windows 7的线程，进行混合调度。</li>
<li>同一进程中的多条线程将共享该进程中的全部系统资源，如虚拟地址空间，文件描述符和信号处理等等。但同一进程中的多个线程有各自的调用栈（call stack），自己的寄存器环境（register context），自己的线程本地存储（thread-local storage）。</li>
<li>一个进程可以有很多线程，每条线程并行执行不同的任务。</li>
<li>线程的四种基本状态：<ul>
<li>产生（spawn）</li>
<li>中断（block）</li>
<li>非中断（unblock）</li>
<li>结束（finish）</li>
</ul>
</li>
</ul>
<p>在PHP中有 <a href="http://php.net/manual/zh/intro.pthreads.php" target="_blank" rel="external">pthreads</a> 多线程扩展，实现了面向对象的 多线程 API。可以创建、读取、写入以及执行多线程应用，并可以在多个线程之间进行同步控制。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">workerThread</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</div><div class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($num)</span></span>&#123;</div><div class="line">        <span class="keyword">$this</span>-&gt;num = $num;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">run</span><span class="params">()</span></span>&#123;</div><div class="line">        <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</div><div class="line">            <span class="keyword">echo</span> <span class="keyword">$this</span>-&gt;num . <span class="string">"\n"</span>;</div><div class="line">            sleep(<span class="number">1</span>);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">for</span> ($i=<span class="number">0</span>; $i &lt; <span class="number">50</span>; $i++)&#123;</div><div class="line">    $workers[$i] = <span class="keyword">new</span> workerThread($i);</div><div class="line">    $workers[$i]-&gt;start();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>线程较之进程，其优势在于一个快，不管是创建新的线程还是终止一个线程；或者是线程间的切换还是线程间共享数据或通信，其速度与进程相比都有较大的优势。进程和线程区别：</p>
<ul>
<li>线程是在一个进程内的，可以共享内存变量实现线程间通信。<ul>
<li>而进程间通信是要借助于<a href="https://zh.wikipedia.org/wiki/行程間通訊" target="_blank" rel="external">进程间通信（IPC）</a>，文件、管道、消息队列等等。</li>
</ul>
</li>
<li>线程比进程更轻量级，开大量进程会比线程消耗更多系统资源。<ul>
<li>比如 PHP-FPM 模块。通常情况下，一个PHP-FPM 进程占用的内存为10M左右，假如支持10000并发，就需要100G的内存。一台物理机的资源是有限的，就那么多CPU和内存，决定了单独依靠 PHP-FPM 不可能开发出单机百万级并发的系统。</li>
</ul>
</li>
</ul>
<p>我们用多进程和多线程的很大因素是为了解决并发问题，先理清一个概念并发和并行是不是一个意思？</p>
<blockquote>
<p>并发和并行的区别就是一个处理器同时处理多个任务和多个处理器或者是多核的处理器同时处理多个不同的任务。 前者是逻辑上的同时发生（simultaneous），而后者是物理上的同时发生。</p>
</blockquote>
<p>由于进程的性能问题，我们觉得并发使用的更多的线程，而不是进程。在处理网络请求时，多线程模型下，可以采用一个独立线程接受请求然后派发到各个 worker 线程的方式。但是使用多线程也存在一些问题：</p>
<ul>
<li>线程读写变量存在同步问题，需要加锁</li>
<li>锁的粒度过大会有性能问题，可能会导致只有1个线程在运行，其他线程都在等待锁。这样就不是并行了</li>
<li>同时使用多个锁，逻辑复杂，一旦某个锁没被正确释放，可能会发生线程死锁</li>
<li>某个线程发生致命错误会导致整个进程崩溃</li>
</ul>
<p>多进程方式更加稳定，另外利用进程间通信（IPC）也可以实现数据共享。至于为什么进程想对安全稳定，主要是依靠进程<a href="https://zh.wikipedia.org/zh-cn/沙盒_(電腦安全" target="_blank" rel="external">沙箱</a>)，可以查看这篇文章<a href="http://zkread.com/article/584903.html" target="_blank" rel="external">《为什么浏览器采用多进程模型》</a>。</p>
<ul>
<li>共享内存，这种方式和线程间读写变量是一样的，需要加锁，会有同步、死锁问题。</li>
<li>消息队列，可以采用多个子进程抢队列模式，性能很好</li>
<li>PIPE，UnixSock，TCP，UDP。可以使用read/write来传递数据，TCP/UDP方式使用socket来通信，子进程可以分布运行</li>
</ul>
<p>Swoole 框架的多进程和多线程的设计实用就非常合理，在需要稳定的时候使用进程，在需要效率的时候使用线程。盗个图</p>
<p><img src="/images/swoole-process-threads.png" alt=""></p>
<p>Swoole 中 worker/task 进程都是由 Manager 进程 Fork 并管理的。</p>
<ul>
<li>子进程结束运行时，Manager 进程负责回收此子进程，避免成为僵尸进程。并创建新的子进程</li>
<li>服务器关闭时，Manager 进程将发送信号给所有子进程，通知子进程关闭服务</li>
<li>服务器 reload 时，Manager进程会逐个关闭/重启子进程</li>
</ul>
<p>Swoole 提供了完善的进程管理机制，当Worker进程异常退出，如发生PHP的致命错误、被其他程序误杀，或达到 max_request 次数之后正常退出。主进程会重新拉起新的 Worker 进程。 Worker 进程内可以像普通的 apache+php 或者 php-fpm 中写代码。不需要像 Node.js 那样写异步回调的代码。</p>
<p>Swoole 的Master主进程是一个多线程的程序，其中一组很重要的是Reactor线程。Swoole 在接收到一个请求连接之后，将这个链接分配给一个固定的Reactor线程，并由这个线程来监听socket。在socket可读时读取数据，并进行协议解析，将请求投递到Worker进程。在socket可写时将数据发送给TCP客户端。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://zh.wikipedia.org/wiki/行程&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;进程&lt;/a&gt;（process）是计算机中已运行程序的实体。关于进程需要知道的几点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;进程是程序的基本
    
    </summary>
    
      <category term="底层原理" scheme="http://blog.100dos.com/categories/%E5%BA%95%E5%B1%82%E5%8E%9F%E7%90%86/"/>
    
    
      <category term="线程" scheme="http://blog.100dos.com/tags/%E7%BA%BF%E7%A8%8B/"/>
    
      <category term="进程" scheme="http://blog.100dos.com/tags/%E8%BF%9B%E7%A8%8B/"/>
    
      <category term="swoole" scheme="http://blog.100dos.com/tags/swoole/"/>
    
  </entry>
  
  <entry>
    <title>8w+拉勾网招聘数据帮你查询 - 家附近的工作</title>
    <link href="http://blog.100dos.com/2017/02/09/nearby-jobs/"/>
    <id>http://blog.100dos.com/2017/02/09/nearby-jobs/</id>
    <published>2017-02-09T06:25:08.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p>从<a href="https://www.lagou.com/" target="_blank" rel="external">拉勾网</a>抓取的 86341 条招聘企业数据，企业地址匹配 LBS 经纬度取自<a href="http://lbs.amap.com/console/show/picker" target="_blank" rel="external">高德开放平台API</a>，成功匹配到 83090 条经纬度数据。没有做详细做企业主正在招聘岗位的数据，所以很大可能情况会遇到附近的公司没有招聘信息的情况，但是某些方面还是能大概了解自己附近有什么值得考虑的公司，有一定参考价值。</p>
<h2 id="查询附近的工作"><a href="#查询附近的工作" class="headerlink" title="查询附近的工作"></a>查询附近的工作</h2><p>Github地址: <a href="https://github.com/jixiaod/NearbyJobs" target="_blank" rel="external">https://github.com/jixiaod/NearbyJobs</a></p>
<h3 id="下载-mongo-数据包"><a href="#下载-mongo-数据包" class="headerlink" title="下载 mongo 数据包"></a>下载 mongo 数据包</h3><p><a href="https://github.com/jixiaod/NearbyJobs/blob/master/jobs.tar.gz" target="_blank" rel="external">https://github.com/jixiaod/NearbyJobs/blob/master/jobs.tar.gz</a></p>
<h3 id="导入-mongo-数据"><a href="#导入-mongo-数据" class="headerlink" title="导入 mongo 数据"></a>导入 mongo 数据</h3><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">~# tar zxf jobs.tar.gz</div><div class="line">~# mongoimport -d jobs -c company --upsert jobs.json</div></pre></td></tr></table></figure>
<h3 id="geo-查询"><a href="#geo-查询" class="headerlink" title="geo 查询"></a>geo 查询</h3><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">// 进入mongodb，我的是默认环境</div><div class="line">~# mongo</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">&gt; use jobs</div><div class="line">// 创建索引</div><div class="line">&gt; db.company.createIndex(&#123; &quot;loc&quot;: &quot;2d&quot; &#125;)</div><div class="line">// 查询附近的工作，自己的坐标可以从[高德开放平台API]获得。</div><div class="line">&gt; db.company.find(&#123;&quot;loc&quot;:&#123;&quot;$near&quot;:[116.673886,39.943799]&#125;&#125;,&#123;&quot;short_name&quot;:1, &quot;address&quot;:1&#125;)</div></pre></td></tr></table></figure>
<p><img src="https://raw.githubusercontent.com/jixiaod/NearbyJobs/master/nearby-jobs.png" alt=""></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;从&lt;a href=&quot;https://www.lagou.com/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;拉勾网&lt;/a&gt;抓取的 86341 条招聘企业数据，企业地址匹配 LBS 经纬度取自&lt;a href=&quot;http://lbs.amap.com/co
    
    </summary>
    
      <category term="流程&amp;工具" scheme="http://blog.100dos.com/categories/%E6%B5%81%E7%A8%8B-%E5%B7%A5%E5%85%B7/"/>
    
    
      <category term="mongo" scheme="http://blog.100dos.com/tags/mongo/"/>
    
      <category term="lbs" scheme="http://blog.100dos.com/tags/lbs/"/>
    
  </entry>
  
  <entry>
    <title>扫雷 - 微信小程序</title>
    <link href="http://blog.100dos.com/2017/01/19/minesweeper-on-wechat-app/"/>
    <id>http://blog.100dos.com/2017/01/19/minesweeper-on-wechat-app/</id>
    <published>2017-01-19T02:55:08.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>微信小程序这几天很火，9号正式发布，今天19号过了10天，到现在略有降温的趋势。个大厂商对微信小程序的态度很不看好，最激进的要数[罗辑思维]，直接下架了自己的[得到APP]。滴滴、京东等APP也只保留了最精简的功能，某些担心过于完善的功能会抢夺APP本身的用户。不过这也印证了小程序的初衷，小而精、用完即走。（并不否认腾讯微信平台确实流氓的事实）</p>
<blockquote>
<p>PS：墨迹天气也开发了微信小程序版本，并且在1月9号第一时间发布，欢迎使用。</p>
</blockquote>
<p>作为技术人员，怎能不亲手试试小程序开发呢？扫雷 Minesweeper</p>
<h2 id="实现思路："><a href="#实现思路：" class="headerlink" title="实现思路："></a>实现思路：</h2><ul>
<li>如果不了解扫雷的游戏规则，请自行 Google</li>
<li>绘制一张 10*10 的地图，并且标注地雷和非地雷，并且需要用数字表示每个点周围地雷的数量</li>
<li>用 mineMap[x][y] 表示一个点位<ul>
<li>mineMap[x][y] &lt; 0 (代码中 == -1) 初始状态</li>
<li>0 &lt; mineMap[x][y] &lt; 9 该点周围地雷的数量</li>
<li>mineMap[x][y] == 9 表示该点是地雷</li>
<li>mineMap[x][y] &gt; 9 （代码中 == 10）表示该点插旗</li>
</ul>
</li>
</ul>
<h2 id="Github"><a href="#Github" class="headerlink" title="Github"></a>Github</h2><ul>
<li><a href="https://github.com/jixiaod/wechat-app-minesweeper" target="_blank" rel="external">https://github.com/jixiaod/wechat-app-minesweeper</a></li>
</ul>
<h2 id="如何跑起来？"><a href="#如何跑起来？" class="headerlink" title="如何跑起来？"></a>如何跑起来？</h2><ul>
<li>下载 &amp; 安装 <a href="https://mp.weixin.qq.com/debug/wxadoc/dev/devtools/download.html" target="_blank" rel="external">微信小程序开发工具</a> </li>
<li>配置 wechat-app-minesweeper 代码目录到开发工具即可</li>
</ul>
<h2 id="玩法"><a href="#玩法" class="headerlink" title="玩法"></a>玩法</h2><ul>
<li>需要注意的是，由于没有鼠标右键标记Flag，简单做了 Flag 切换，开启后，方可标记Flag</li>
<li>最初的想法是通过 tap 和 longtap 来区分扫雷和Flag，但是开发工具不能区分这俩个事件，只能作罢</li>
</ul>
<p><img src="/images/wechat-minesweeper.jpeg" alt=""></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;微信小程序这几天很火，9号正式发布，今天19号过了10天，到现在略有降温的趋势。个大厂商对微信小程序的态度很不看好，最激进的要数[罗辑思维]，直接下架了自己的[得到APP]。滴滴、京东等APP也只保留了最精简的功能，某些担心过于完善的功能会抢夺APP本身的用户。不过这也印证
    
    </summary>
    
      <category term="Javascript" scheme="http://blog.100dos.com/categories/Javascript/"/>
    
    
      <category term="Javascript" scheme="http://blog.100dos.com/tags/Javascript/"/>
    
      <category term="微信小程序" scheme="http://blog.100dos.com/tags/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F/"/>
    
      <category term="wechat" scheme="http://blog.100dos.com/tags/wechat/"/>
    
  </entry>
  
  <entry>
    <title>［读书笔记］－ 《大清相国》</title>
    <link href="http://blog.100dos.com/2016/12/27/reading-note-the-trime-minister-of-qing-dynasty/"/>
    <id>http://blog.100dos.com/2016/12/27/reading-note-the-trime-minister-of-qing-dynasty/</id>
    <published>2016-12-27T11:18:35.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p><strong>很多事情不像表面上那么简单，尤其是在官场，要考虑点更多。</strong>发生的时间、涉及的官员、所涉及到官员之间的关系、地位、各自的立场。</p>
<p>皇上：要顾全皇族的利益，凡事涉及皇族，一定是要偏袒皇族，照顾其颜面。朝中大臣以和为贵，可以接受做官贪赃枉法，但是对上对皇上要有敬畏之心，对下对百姓不能做太过分出格。</p>
<p>陈廷敬：心怀天下百姓，一心要肃清官场腐败。</p>
<p>鳌拜／明珠／索额图：一人之下，万人之上。任人唯亲，独揽大权。</p>
<p>高士其：墙头草，唯利是图。</p>
<p>等等……</p>
<p>书中围绕陈廷敬一生为官，发生的诸多故事展开。故事的来龙去脉都大同小异，朝廷出了什么事（比如大户统筹案、宝泉局毁钱鬻铜案等），然后派陈廷敬微服私访查明真相，结果又查出来了个大贪官，然后最终如何处置这点事儿。</p>
<p>事故虽然不算太精彩，但更精彩的部分是揣摩为官的道理。如何看准官场的风向，想要参人，也要看这个人是参得参不得。皇上用的人参不得，皇上宠信的大臣参不得，皇上宠信的大臣的人也要看情况是不是可以参。总之要先摸清皇上的意思，最简单的情况，皇上示意可以参，你参就一点毛病没有。</p>
<p><strong>身为人臣，最忌讳功高盖主。</strong>虽然在书的最后章节里，皇上对陈廷敬的评价是“宽大老成，几近完人！”，但是请注意也只是“几近”哦，因为完人只有皇帝他老人家而已。此时陈廷敬也已是七旬老人，事君也已有四十八年，可又能怎样呢？得到皇上嘉许时不也还要赶忙连连扣头谢恩。所以你陈廷敬如果是完人，那皇上又是什么呢？</p>
<p>陈廷敬一心要肃清官场，整天参来参去。弄的皇帝很不开心，但是陈廷敬为官清廉，又没有确着的罪证。皇上只好让张英随便编个理由参他，导致陈廷敬第一次被贬。连明珠后来都跟萨穆哈都说：“你以为陈廷敬真的有罪？他根本就没罪！”。</p>
<p>后来，陈廷敬自己也有所悟：“我让皇上知道天下没几个好官了，我就完了；我让天下人知道大清没几个清官了，天下就完了。”</p>
<p><strong>皇上也得哄着。</strong>如果让皇上觉得你认为他是傻逼，你就完了！！！你只能比皇上更傻逼……</p>
<p>最终，陈廷敬用“等”、“忍”、“稳”、“恨”四字诀，扳倒了明珠和索额图，坐上了大清相国的位置。等待时机，逆境要忍，做事要稳，下手要狠。</p>
<p>好吧，挺难的，突然想到冯小刚给广告词：“做人做事，安全第一……”。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;很多事情不像表面上那么简单，尤其是在官场，要考虑点更多。&lt;/strong&gt;发生的时间、涉及的官员、所涉及到官员之间的关系、地位、各自的立场。&lt;/p&gt;
&lt;p&gt;皇上：要顾全皇族的利益，凡事涉及皇族，一定是要偏袒皇族，照顾其颜面。朝中大臣以和为贵，可以接受做官贪赃
    
    </summary>
    
      <category term="读书笔记" scheme="http://blog.100dos.com/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="读书笔记" scheme="http://blog.100dos.com/tags/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
  </entry>
  
  <entry>
    <title>癌症离我们不远</title>
    <link href="http://blog.100dos.com/2016/12/14/cancer-is-not-far-from-us/"/>
    <id>http://blog.100dos.com/2016/12/14/cancer-is-not-far-from-us/</id>
    <published>2016-12-14T10:10:51.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>前几天知道了妈得了癌症的消息，原来新闻里、电视里不相干的陌生人得的病，也来到了我的身边。原来癌症离我也并不远……</p>
<p>刚知道的时候心情不免低落，一个是为老妈难过，刚到60便染此恶疾，虽有希望治愈，治疗过程不免受苦；在一个也为自己难过，刚刚步入正轨的生活，又被被打乱了。本来还打算着让老妈过来帮带带孩子呢，结果计划全变了……孩子还未满月，老婆尚未做完月子。家里出了这么大的事情，也不敢告诉她。怕月子里身子虚，心情压抑，对恢复有影响。</p>
<p>上周末已回老家抚顺看过，现在在抚顺矿务局医院（当地三甲）接受治疗。老妈的状态还不错，些许安慰。刚做完化疗，吃不下东西。中午一家人吃了点东西，都吐了。晚上还好，因为打了抗呕吐的针，而且是上周五最后做的化疗，已隔了2天，身体已无太大反应。</p>
<p>最近看了好多关于癌症的资料，老妈得得是鼻癌（鼻咽癌）。很是吃紧，离大脑太近，后期如果癌细胞扩散到脑腔，就会引发头痛，而且是痛的要撞墙的那种。据老妈自己说，最近感觉还好，感觉肿块相比之前软了许多，或许治疗起作用了。今天电话又说，做了放疗，并无大碍。而且吃饭很好，吃了一碗。</p>
<p>在医院时候陪父母跟老婆和孩子视频聊天，老人家很开心，这个时间段，让老人保持心情舒畅很重要。也告诉老爸钱方面不要担心，更不要在老妈面前提钱字。老爸也确实很上火，家里确实不太富裕。不过我觉得还好吧，毕竟有三个子女，看病的话大家分担一下，压力还没有那么大。病还是要治的，就算治不好，多活一年是一年。也能多陪老爸一年，老伴老伴，到老了才是伴。假如说老妈真的没了，老爸难免会孤单。</p>
<p>今天又跟家里视频，老人家，总是看见孩子就很开心。孩子这时候真是法宝，且当作你奶奶的开心果吧……</p>
<p>琢磨着过年时偶接老俩口过来住几天，看看孩子。</p>
<p>查看癌症资料，无意间看到了一位癌症患者的博客，<a href="http://xin-sheng.hxwk.org/" target="_blank" rel="external">辛声文集</a>，翻看了很多篇文章。甚是佩服博主的乐观态度，但是停更很久了，不知道现在是否安好。</p>
<p>健康生活很重要，乐观的生活态度也少不了。猜测老妈大概就是因为心情长期压抑导致的癌症，总是看不惯这个那个，埋怨这个那个。可能有点像姥姥，不过姥姥倒是活了很大年纪，所以也吃不太准，也可能是姥姥比较善忘的缘故。老妈可能一件烦心事可以憋很久吧。本身老人都是农村人，体力活干过不少，身体应该还是不错的。总之，癌症又有点来的不明所以。</p>
<p>病既来之，只好安之……</p>
<p>老爸那边更多的是需要宽慰、开导，老妈那边要多多的哄她开心。毕竟也刚刚 60 出头，刚刚结束了一辈子的辛苦，还没有开始享受生活。</p>
<p>同时告诫自己：锻炼身体、保持健康、凡事轻看、心情愉悦，还有多多赚钱……</p>
<p>如果你也在看，共勉之。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前几天知道了妈得了癌症的消息，原来新闻里、电视里不相干的陌生人得的病，也来到了我的身边。原来癌症离我也并不远……&lt;/p&gt;
&lt;p&gt;刚知道的时候心情不免低落，一个是为老妈难过，刚到60便染此恶疾，虽有希望治愈，治疗过程不免受苦；在一个也为自己难过，刚刚步入正轨的生活，又被被打乱
    
    </summary>
    
      <category term="杂" scheme="http://blog.100dos.com/categories/%E6%9D%82/"/>
    
    
      <category term="癌症" scheme="http://blog.100dos.com/tags/%E7%99%8C%E7%97%87/"/>
    
  </entry>
  
  <entry>
    <title>告别 斯蒂芬·杰拉德</title>
    <link href="http://blog.100dos.com/2016/12/13/say-goodbye-to-stevengerrard/"/>
    <id>http://blog.100dos.com/2016/12/13/say-goodbye-to-stevengerrard/</id>
    <published>2016-12-13T09:47:55.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<p>杰拉德退役的消息出来好几天了，想想还是写一篇吧。以纪念我浦最伟大的队长……</p>
<blockquote>
<p>一个人守护一座城，一座城守护一个人 – 杰拉德和利物浦</p>
</blockquote>
<p><img src="/images/steven-gerrard.jpg" alt=""></p>
<p>我看球应该算是比较晚的，02年世界杯因为中国队杀入了世界杯决赛圈，大家都在看球。那时候不太懂，就跟着一起瞎看，人都认不全，什么是越位都不懂……大概也就知道足球，只能用脚，不能用手吧。</p>
<p>记得小时候有一件T恤，上面印的是［迈克 欧文］，我当时也不知道。后来有别的小朋友看见我穿那件衣服，问到：你喜欢欧文啊？我哪里知道谁是欧文，只是怕跌了面子，直道：我喜欢欧文，欧文是我偶像！</p>
<p>至于欧文是［英格兰］的球员，在［利物浦足球俱乐部］踢球都是后话了。再后来又认识了杰拉德，并不喜欢他的风格，因为他跑起来的样子怪怪的……虽然速度很快。</p>
<p>真正接触足球源于《实况足球》这款游戏，游戏里让我认识了很多球员。玩的时候大家都会自己的主队，巴西、意大利、法国……他们跳差不多了，我当然不能跟他们一样，英格兰的整体能力也是不错的，而且“欧文是我偶像”，就选了英格兰做主队。</p>
<p>游戏里每个球员都有能力值，80以上就是能力很不错的球员了。杰拉德就是能力非常全面的球员，各项能力都是80+。射门精度和射门力量90+，速度也要比［双德］之一的兰帕德要快。甚是喜欢，那时候还没怎么看过利物浦的比赛，对于杰拉德的印象就是在游戏中，反正很屌！</p>
<p>高中生活很是压抑，平时的消遣就是晚自习看《足球周刊》和《体坛周报》，比看课本都认真，真可以说咬文嚼字。周末休息也要去［PS房］踢几局实况（就是出租Play Station的小店，当时玩一个小时4块钱，当时PS2代），我几乎就选英格兰踢，上课上自习也没事研究英格兰阵型，我主打442，中场菱形站位。我认为菱形站位比平行站位，进攻和防守都会更加均衡一些。中场如果对方有速度快的前锋，就杰拉德拖后，速度快、身体平衡高，回追一追一个准，否则，就兰帕德拖后。除非遇到巴西阿德里亚诺那种，就只能上中后卫坎贝尔的了……</p>
<blockquote>
<p>锋线：欧文、鲁尼<br>中场：乔 科尔、杰拉德、兰帕德、贝克汉姆<br>后卫：阿什利 科尔、费迪南德、坎贝尔、加里 内维尔<br>门将：罗宾逊<br>替补：迪福、克劳奇、唐宁、列侬、哈格里弗斯、特里、卡拉格、詹姆斯</p>
</blockquote>
<p>这大概是06年世界杯时期的阵容吧，忘却了……06年英格兰有实力拼一下世界杯冠军的。可惜欧文开场没多久莫名其妙自己拉伤，可惜英格兰对阵葡萄牙又输在了12码线……</p>
<p>那一年杰拉德也是正直巅峰，26岁的年纪，04/05年伊斯坦布尔之夜过去没多久。好像是宿命，英格兰同利物浦一样沉沦了……都是在苦苦挣扎，利物浦慢慢掉队，从欧洲冠军到进不了欧冠。</p>
<p>但是杰拉德一直没有离开，用他的忠诚捍卫着利物浦的荣耀。五冠王之后，青训同伴欧文 04 年就转投到皇马。好友阿隆索、金童托雷斯，直道苏牙相继离开，只有杰拉德抵挡住了金钱的诱惑，更多的是忍受没有冠军的寂寞。</p>
<p>2014年，杰对拼着老脸说服苏牙再留下一年，曾经离冠军如此之近，最后俩轮仍然保持着领先优势。直到杰拉德滑倒的一瞬间，整个世界都黑暗了。很久之后，有记者提到当时那个球，杰拉德依然无法释怀，注定是杰队一辈子的痛……</p>
<p>最后一个赛季，杰队无法接受罗杰斯只能坐冷板凳的安排，转投美国大联盟踢了自己的最后一个赛季。</p>
<p>渣叔临危受命利物浦，带领球队踢得越来越好，现排名英超老二。然而我们的老队长杰拉德宣布退役了……</p>
<p>80年出生，36岁的年纪。作为利物浦青训体系自己培养的青年才俊，18岁进入一线队，直到退役。虽然最后一个赛季到美国大联盟，但是那是唯一不可能与利物浦为敌的地方。杰拉得把最好的岁月献给了利物浦，把他最坏的记忆也留在了利物浦。</p>
<blockquote>
<p>我们记住的是他永远的全力以赴<br>我们记住的是无数的炮弹般的大力远射<br>我们记住的是无数鼓励队友的士气的画面<br>我们记住的是高举欧冠奖杯利物浦队长<br>我们记住的是那个初出茅庐的青涩少年<br>我记不住的是何时变成了利物浦死忠、杰拉德的死忠。</p>
</blockquote>
<p>只奢望年华和青春永不逝去。最好的现在就在，最好的过去就在，最好的永远都在。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;杰拉德退役的消息出来好几天了，想想还是写一篇吧。以纪念我浦最伟大的队长……&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;一个人守护一座城，一座城守护一个人 – 杰拉德和利物浦&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;/images/steven-gerra
    
    </summary>
    
      <category term="杂" scheme="http://blog.100dos.com/categories/%E6%9D%82/"/>
    
    
      <category term="足球" scheme="http://blog.100dos.com/tags/%E8%B6%B3%E7%90%83/"/>
    
      <category term="利物浦" scheme="http://blog.100dos.com/tags/%E5%88%A9%E7%89%A9%E6%B5%A6/"/>
    
  </entry>
  
  <entry>
    <title>How to C in 2016</title>
    <link href="http://blog.100dos.com/2016/12/05/how-to-c-in-2016/"/>
    <id>http://blog.100dos.com/2016/12/05/how-to-c-in-2016/</id>
    <published>2016-12-05T03:23:52.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>英文原文：<a href="https://matt.sh/howto-c" target="_blank" rel="external">https://matt.sh/howto-c</a></p>
<p>这原本是一篇我写在 2015 年没有发布出来的草稿。为了让大家也能受益，我决定把它从我的草稿文件夹里拿出来。并且基本是原文，未做过多修改。大概只是把年份改了一下，2015 改成了 2016。</p>
<p>如有任何修正／改进／意见，请发送邮件到 matt@matt.sh</p>
<p>C 的第一条规则：如果能避免用C实现，就不要用C。<br>如果你必须要用C，你就必须要遵守C的最新规定。</p>
<p>在<a href="https://www.bell-labs.com/usr/dmr/www/chist.html" target="_blank" rel="external">1970年代早期</a>，C 就开始流行起来。人们跟随着C语言的发展革新“学习 C 语言”的各种各样的要点，然后你学会的这些知识开始欺骗你。每个人都坚持的认为自己最开始学习部分才是正确的，这样使得写C的开发人员对C语言的开发有着不同的理解。</p>
<p>重要的是，不要让“80年代～90年代学习C的旧思维和老观念”束缚住你的手脚。</p>
<p>这篇文章首先假设读者是在当前最新的平台且使用最新的标准，没有多度的需要对一些过时的东西的兼容需求。我们不应该完全的去使用过时的标准，因为确实有一些公司拒绝去更新已经用了20年的老系统。</p>
<h1 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h1><p>C99标准（C99 的意思是“1999年的 C 语言标准”；C11 是“2011年定义的 C 语言标准，所以 11 &gt; 99”）。</p>
<ul>
<li>clang, default<ul>
<li>clang 是一个C11的默认扩展版本（<code>GNU C11 模式</code>），所以对最新的特征编译不需要额外的参数配置。</li>
<li>如果希望用C11标准，需要添加 <code>-std=c11</code>；如果想要使用C99，使用 <code>-std=c99</code></li>
<li>clang 编译源代码会比 gcc 要快。</li>
</ul>
</li>
<li>gcc 需要指定 <code>-std=c99</code> 或者 <code>-std=c11</code><ul>
<li>gcc 编译源文件比 clang 要慢，但是有时会生成更快的代码。性能对比和回归测试很重要。</li>
<li>gcc-5 默认是指 <code>GNU C11 模式</code>（跟 clang 一样），但是如果需要确切的 C11 或者 C99，需要加上参数 <code>-std=c11</code> 或 <code>-std-c99</code></li>
</ul>
</li>
</ul>
<p>优化</p>
<ul>
<li>-O2, -O3<ul>
<li>通常情况下需要 <code>-O2</code>，有时需要 <code>-O3</code>。俩个级别都做测试（使用不同的编译器程序），然后保留性能最好的二进制文件。</li>
</ul>
</li>
<li>-Os<ul>
<li><code>-Os</code> 如果你写代码时，有考虑到缓存效率的话，会有帮助。</li>
</ul>
</li>
</ul>
<p>警告</p>
<ul>
<li><code>-Wall -Wextra -pedantic</code><ul>
<li><a href="https://twitter.com/oliviergay/status/685389448142565376" target="_blank" rel="external">更新版本的编译器</a>已经有 <code>-Wpedantic</code>，同时这些编译器还在支持 <code>-pedantic</code> 以保证向后的兼容性。</li>
</ul>
</li>
<li>测试时，所有平台都要加上 <code>-Werror</code> 和 <code>-Wshadow</code> <ul>
<li>如果在最终的正式代码编译时加上 <code>-Werror</code>，情况会变得很微妙。因为不同的平台、编译器和类库会提示不同的警告。因为不同平台GCC版本问题，出现一些意想不到的提示，让编译到一半的程序终止是大家都不想看到的。</li>
</ul>
</li>
<li>其他的不错的选项，<code>-Wstrict-overflow</code> <code>-fno-strict-aliasing</code><ul>
<li>要么指明 <code>-fno-strict-aliasing</code>，要么确定访问的指针从它们创建开始类型一值不变。使用 <code>-fno-strict-aliasing</code> 是更佳稳妥的方式，因为现存的C代码中，用到强制类型转换的地方还是挺多的。［注：Strict aliasing 是C或C++编译器的一种假设：不同类型的指针绝对不会指向同一块内存区域。可参考进一步了解］</li>
</ul>
</li>
<li>现在，Clang 对于一些正当的语法也会报警告，所以你需要添加 <code>-Wno-missing-field-initalizers</code><ul>
<li>GCC 在 4.7.0 版本之后修正了这些不必要的警告。</li>
</ul>
</li>
</ul>
<p>构建</p>
<ul>
<li>隔离原则 (Compilation units 编译单元)<ul>
<li>构建C项目的通常办法是拆分每一个源文件编译成一个个目标文件，最后再把所有的目标文件link到一起。这个做在进行开发时是非常好的方法，但是对于代码的性能和优化就不太友好了。用这种方法，编译器不能发现一些存在在文件边界之间的潜在优化点。</li>
</ul>
</li>
<li>LTO - Link Time Optimisation （链接时的优化）<ul>
<li>LTO 解决了“被分离的源代码文件分析和优化的问题”。通过中间语言来标注编译生成的目标文件，所以LTO能够在链接的时候实现代码优化。</li>
<li>LTO 明显的降低了链接过程的速度，但是使用 make -j 可以有所改善，如果你的构建的的文件包含很多并不相互依赖的目标文件（.a, .so, .dylib, 测试可执行文件，应用程序可执行文件等）</li>
<li><a href="http://llvm.org/docs/LinkTimeOptimization.html" target="_blank" rel="external">clang LTO</a> (<a href="http://llvm.org/docs/GoldPlugin.html" target="_blank" rel="external">guide</a>)</li>
<li><a href="https://gcc.gnu.org/onlinedocs/gccint/LTO-Overview.html" target="_blank" rel="external">gcc LTO</a></li>
<li>在2016年，clang 和 gcc 支持 LTO 只需要在编译目标文件和最终链接库和程序时，加上 <code>-flto</code> 到命令行的选项。</li>
<li><code>LTO</code> 仍然需要被细心看管。有时，你的程序有部分代码被当作附加库，没有被直接使用。在链接时，LTO 会放弃部分函数或代码，因为LTO检查到这些代码没有被使用，不需要被包含到最终的链接结果中。<br>架构</li>
</ul>
</li>
<li><code>-march=native</code><ul>
<li>给予编译器使用所有的CPU特性的权限。</li>
<li>再一次强调，性能测试和回归测试很重要（比较不同编译器且不同编译器版本的性能）。确保性能提升的同时没有带来问题。</li>
</ul>
</li>
<li><code>-msse2</code> 和 <code>-msse4.2</code> 如果你需要的编译结果不是根据“你自己编译环境”的特征。</li>
</ul>
<h1 id="写代码"><a href="#写代码" class="headerlink" title="写代码"></a>写代码</h1><h2 id="类型"><a href="#类型" class="headerlink" title="类型"></a>类型</h2><p>如果你写C代码还是使用 <code>char</code>、<code>int</code>、<code>short</code>、<code>long</code>、<code>unsigned</code>，那么你已经是在犯错误了。</p>
<p>现如今，你应该使用 <code>#include &lt;stdint.h&gt;</code>，然后使用标准的类型。</p>
<p>更多细节，请参考 <a href="http://pubs.opengroup.org/onlinepubs/9699919799/basedefs/stdint.h.html" target="_blank" rel="external">sdtint.h specification</a></p>
<p>常用的标准类型如下：</p>
<ul>
<li><code>int8_t</code>, <code>int16_t</code>, <code>int32_t</code>, <code>int64_t</code>  — 有符号的整型</li>
<li><code>uint8_t</code>, <code>uint16_t</code>, <code>uint32_t</code>, <code>uint64_t</code> — 无符号的整型</li>
<li><code>float</code> — 标准的32位浮点数</li>
<li><code>double</code> — 标准的64位浮点数</li>
</ul>
<p>注意，<code>char</code> 已经不存在了。<code>char</code> 在C语言中经常是一个误称，且被误用了。</p>
<p>开发者通常会滥用 <code>char</code> 去表示“字节”，即使是去做无符号的字节操作。当要表示无符号的字节或8位字节值时应该使用 <code>uint8_t</code>。当要表示无符号的字节序列或一串字节值应该使用 <code>uint8_t *</code>。</p>
<h3 id="特殊的标准类型"><a href="#特殊的标准类型" class="headerlink" title="特殊的标准类型"></a>特殊的标准类型</h3><p>作为标准固定长度的 <code>uint16_t</code> 和 <code>int32_t</code>，我们也有 <strong>fast</strong> 和 <strong>least</strong> 类型，可以在 <a href="http://pubs.opengroup.org/onlinepubs/9699919799/basedefs/stdint.h.html" target="_blank" rel="external">stdint.h specification</a> 看到他们的定义。</p>
<p>有以下的<strong>快速类型</strong>：</p>
<ul>
<li>有符号的整型：<code>int_fast8_t</code>，<code>int_fast16_t</code>，<code>int_fast32_t</code>，<code>int_fast64_t</code> </li>
<li>无符号的整型：<code>uint_fast8_t</code>，<code>uint_fast16_t</code>，<code>uint_fast32_t</code>，<code>uint_fast64_t</code> </li>
</ul>
<p>快速类型提供了一个最小的占用 <code>X</code>位的字节数，但是没有办法真正需要的存储大小是多少。如果在你的目标平台有更大的类型被更好的支持，<code>fast</code>类型将会自动去选择这个的更大类型。</p>
<p>有个非常好的例子，在一些 64 位系统中，当你需要 <code>uint_fast16_t</code>，最终会使用 <code>uint64_t</code>。因为处理一个字量长度的整型将会比操作32位一半（16位）的整型要快得多。</p>
<p>但是并不是在所有的系统，都这样遵循 <em>fast</em> 类型的原则。 OSX 就是一个特例，<em>fast</em> 类型被<a href="https://opensource.apple.com/source/xnu/xnu-1456.1.26/EXTERNAL_HEADERS/stdint.h" target="_blank" rel="external">定义成跟他对应的固定长度小大是一致的</a>。</p>
<p>快速类型在自描述代码中也是有用的。如果你知道你的计数器只需要16位，但是你更希望你的计算能够使用64位。因为64位在你的平台速度更快，这是使用<code>uint_fast16_t</code> 就会很有用。在64位 Linux 平台下，<code>uint_fast16_t</code> 会实际使用更快的64位计数器，而在代码层面“这里只需要16位”。</p>
<p>关于快速类型还需要注意的是：它会影响一些测试用例。如果你需要测试存储位宽的情况，使用 <code>uint_fast16_t</code> 会增加你需要测试通过的平台数量（以保证测试覆盖），在一些平台（OS X）是16位，另外一些平台（Linux）是64位。</p>
<p>快速类型和 <code>int</code> 一样，在不同平台，有不确定的标准长度。但是对于快速类型，你能够在代码中限制这种不确定性到安全位置（计数器、有边界检测的临时变量）。</p>
<p><strong>最小类型</strong>有：</p>
<ul>
<li>有符号整型：<code>int_least8_t</code>, <code>int_least16_t</code>,  <code>int_least32_t</code>,  <code>int_least64_t</code></li>
<li>无符号的整型：<code>uint_least8_t</code>,  <code>uint_least16_t</code>,  <code>uint_least32_t</code>,  <code>uint_least64_t</code></li>
</ul>
<p>最小类型提供给你申请类型的最紧凑的字节位。</p>
<p>事实上，最小类型规范是指最小类型只是定义标准固定宽度类型，因为标准款度类型已经提供了确切的对应的最小字节数。</p>
<h3 id="要不要使用-int-类型"><a href="#要不要使用-int-类型" class="headerlink" title="要不要使用 int 类型"></a>要不要使用 <em>int</em> 类型</h3><p>一些读者也表达了他们对 int 的真爱，至死方休。我想指出的是，如果你使用的类型的长度是不可控的话，这样在技术上是不可能正确的。</p>
<p>同见 <a href="http://pubs.opengroup.org/onlinepubs/9699919799/basedefs/inttypes.h.html#tag_13_19_06" target="_blank" rel="external">RATIONALE</a> 中的 <a href="http://pubs.opengroup.org/onlinepubs/9699919799/basedefs/inttypes.h.html" target="_blank" rel="external">inttypes.h</a>，就是为了解决非固定位宽度的不安全问题。如果你真的足够聪明理解在开发过程中， <code>int</code> 在一些平台是16位，在其他平台是32位。同时在所有使用 <code>int</code> 的地方都对16位和32位的边界进行了测试，那么请放心使用<code>int</code>。</p>
<p>对于那些其他hold不住所有多层决策树平台规范结构的人，我们可以使用固定宽度的类型。这样就能写出更加正确的代码，减少需要理解概念的困扰，并且减少需要做过多测试的麻烦。</p>
<p>或者，规范中更简明的说明：“IOS C 标准整型提升规则能够造成不可预期的静默修改。”</p>
<p>祝你好运。</p>
<h3 id="永远不要使用-char-的一个特例"><a href="#永远不要使用-char-的一个特例" class="headerlink" title="永远不要使用 char 的一个特例"></a>永远不要使用 <em>char</em> 的一个特例</h3><p>在 2016 年，唯一接受使用 <code>char</code> 类型的场景是，加入已经存在的 API 需要 <code>char</code> 类型（例如<code>strncat</code>、printf函数中的“%s”占位符等）或者如果在初始化只读字符串（例如 <code>const char *hello = &quot;hello&quot;;</code>），因为字符串（<code>&quot;hello&quot;</code>）的 C 类型是 <code>char []</code>。</p>
<p>而且：在C11中增加了本地 unicode 支持，对于像 <code>const char *abcgrr = u8&quot;abc&quot;;</code>多字节UTF-8字符串<br>类型仍然是 <code>char []</code> 。</p>
<h3 id="永远不要使用-int，long等-的一个特例"><a href="#永远不要使用-int，long等-的一个特例" class="headerlink" title="永远不要使用{int，long等}的一个特例"></a>永远不要使用{<em>int</em>，<em>long</em>等}的一个特例</h3><p>如果你调用函数时使用它其作为原本的返回类型或原本的参数，参照使用函数原型或API文档中说明的类型。</p>
<h3 id="符号"><a href="#符号" class="headerlink" title="符号"></a>符号</h3><p>任何情况下，都不应该在代码中输入 <code>unsigned</code> 字符。我们现在可以在写代码时改掉使用丑陋的多词组合类型的习惯，它既影响代码的可读性，也影响使用。当你能使用 <code>uint64_t</code>时，谁还想去输入 <code>unsigned long long int</code>？<code>stdint.h</code> 头文件中的类型更加明确，表达也更准确，传达意图也更好，而且代码排版使用更好，可读性更强。</p>
<h3 id="整型指针"><a href="#整型指针" class="headerlink" title="整型指针"></a>整型指针</h3><p>但是，你可能会说，“在脏指针运算时，我需要把指针转换给 <code>long</code> 类型！”</p>
<p>你可以这么说，但你完全错了。</p>
<p>运算时，正确的指针类型是定义在 <code>&lt;stdint.h&gt;</code> 头文件中的 <code>uintptr_t</code>，同时也可以用 <a href="http://pubs.opengroup.org/onlinepubs/7908799/xsh/stddef.h.html" target="_blank" rel="external">stddef.h</a> 头文件中的 <code>ptrdiff_t</code>。</p>
<p>不要使用</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">long</span> diff = (<span class="keyword">long</span>)ptrOld - (<span class="keyword">long</span>)ptrNew;</div></pre></td></tr></table></figure>
<p>使用</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">ptrdiff_t</span> diff = (<span class="keyword">uintptr_t</span>)ptrOld - (<span class="keyword">uintptr_t</span>)ptrNew;</div></pre></td></tr></table></figure>
<p>或者使用</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="built_in">printf</span>(<span class="string">"%p is unaligned by %"</span> PRIuPTR <span class="string">" bytes.\n"</span>, (<span class="keyword">void</span> *)p, ((<span class="keyword">uintptr_t</span>)somePtr &amp; (<span class="keyword">sizeof</span>(<span class="keyword">void</span> *) - <span class="number">1</span>)));</div></pre></td></tr></table></figure>
<h3 id="系统相关的类型"><a href="#系统相关的类型" class="headerlink" title="系统相关的类型"></a>系统相关的类型</h3><p>如果继续争论，“在32位平台我要使用32位long类型，64位平台我要使用64位long类型！”</p>
<p>如果我们先忽略你在不同平台使用不同大小的类型的动机时，你也许是故意给编写代码制造困难，但你依然不希望为了系统相关类型去使用 <code>long</code> 类型。</p>
<p>在这种情况下，你应该使用 <code>intptr_t</code> 当前平台存储一个指针值的整型类型。</p>
<p>在现代的32位平台，<code>intptr_t</code> 是 <code>int32_t</code> 类型。</p>
<p>在现代的64位平台，<code>intptr_t</code> 是 <code>int63_t</code> 类型。</p>
<p>还有，<code>intptr_t</code> 对应 <code>uintptr_t</code> 类型。</p>
<p>对于指针便宜量，我们有一个更适合的类型 <code>ptrdiff_t</code>，它是存储指针差值的正确类型。</p>
<h3 id="最大值存储问题"><a href="#最大值存储问题" class="headerlink" title="最大值存储问题"></a>最大值存储问题</h3><p>你是否需要一个能存储任何整数的整型类型吗？</p>
<p>此时，人们倾向于使用已知类型中的最大类型，比如转换较小的无符号类型到 <code>uint64_t</code>，但是有更多的技术上正确的方法来确保一个值可以存储为另一个值。</p>
<p>对于任意的整数，最安全的容器就是 <code>intmax_t</code> (<code>uintmax_t</code>也可以)。你能够在不损失精读的情况下，赋值或转换有符号的整数为 <code>intmax_t</code>。并且也能够在不损失精读的情况下，赋值或转换无符号的整数为<code>uintmax_t</code>。</p>
<h3 id="其他类型"><a href="#其他类型" class="headerlink" title="其他类型"></a>其他类型</h3><p>应用最广泛的系统相关类型是 <code>size_t</code>， 它由 <a href="http://pubs.opengroup.org/onlinepubs/7908799/xsh/stddef.h.html" target="_blank" rel="external">stddef.h</a> 头文件提供。</p>
<p><code>size_t</code> 基本表示为“能够存储数组最大索引的整数”，同时它也表示程序中能够存储内的存最大偏移量。</p>
<p>在实际应用中，<code>size_t</code> 是 <code>sizeof</code> 操作符的返回类型。</p>
<p>无论在哪种情况下：在所有平台，<code>size_t</code> 实际上的定义都跟 <code>uintptr_t</code> 是一样的，所以在32位平台 <code>size_t</code> 是 <code>uint32_t</code>，在64位平台 <code>size_t</code> 是 <code>uint64_t</code>。</p>
<p>还有 <code>ssize_t</code>，它表示一个有符号的 <code>size_t</code>，用于库函数的返回值，出错时返回 -1。（注意： <code>ssize_t</code> 是 POSIX 中的定义，在windows接口中不可用。）</p>
<p>那么，我们可以在任意的系统相关类型的函数参数中都使用 <code>size_t</code> 吗？技术上来说，<code>size_t</code> 是 <code>sizeof</code> 的返回类型，所以任何接受字节数大小值的函数都可以使用 <code>size_t</code>。</p>
<p>还有其他的一些使用方法：<code>size_t</code> 可以是malloc的参数，并且 <code>ssize_t</code> 是 <code>read()</code>和 <code>write()</code> 的返回类型（除了windows平台，<code>ssize_t</code> 不存在，返回值只是 <code>int</code>）。</p>
<h3 id="打印类型"><a href="#打印类型" class="headerlink" title="打印类型"></a>打印类型</h3><p>在打印时，不能做类型转换。</p>
<p>通常需要选择使用正确的说明符，在 <a href="http://pubs.opengroup.org/onlinepubs/9699919799/basedefs/inttypes.h.html" target="_blank" rel="external">inttypes.h</a> 中被定义。</p>
<p>包括，但不限于：</p>
<ul>
<li>size_t - %zu</li>
<li>ssize_t - %zd</li>
<li>ptrdiff_t - %td</li>
<li>原始指针的值 - %p （现代的编译器中打印16进制；先转换指针为 <code>(void *)</code>）</li>
<li>int64_t - “%” PRId64</li>
<li>uint64_t - “%” PRIu64<ul>
<li>64位类型打印时，只需要使用 <code>PRI[udixXo]64</code> 的宏打印。</li>
<li>Why？<ul>
<li>在一些平台下，64位值是 <code>long</code> 类型，然而在一些其他的平台上是 <code>long long</code>。这些宏提供正确的跨平台规定的格式规范。</li>
<li>如果不使用这些宏，事实上是不可能指定一个正确的跨平台格式化字符串的，因为类型类型发生了变化。因为类型变换（然后，请记住，打印之前转换值类型是不安全的，也不和逻辑）。</li>
</ul>
</li>
<li><code>intptr_t</code> - “%” PRIdPTR</li>
<li><code>uintptr_t</code> - “%” PRIuPTR</li>
<li><code>intmax_t</code> - “%” PRIdMAX</li>
<li><code>uintmax_t</code> - “%” PRIuMAX</li>
</ul>
</li>
</ul>
<p>关于 <code>PRI*</code> 格式说明符的一个注释：它们是宏，并且宏在特定平台上扩展为正确的 printf 类型说明符。这意味着你不能做：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="built_in">printf</span>(<span class="string">"Local number: %PRIdPTR\n\n"</span>, someIntPtr);</div></pre></td></tr></table></figure>
<p>但是因为它们是宏，你可以：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="built_in">printf</span>(<span class="string">"Local number: %"</span> PRIdPTR <span class="string">"\n\n"</span>, someIntPtr);</div></pre></td></tr></table></figure>
<p>注意你需要把 <code>%</code> 写在格式字符串文字中，但是类型说明符是在格式字符串文字之外。因为所有相邻字符串被预处理器连接成一个最终的字符串。</p>
<h2 id="C99-允许在任何地方定义变量"><a href="#C99-允许在任何地方定义变量" class="headerlink" title="C99 允许在任何地方定义变量"></a>C99 允许在任何地方定义变量</h2><p>所以，不要这么写代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">uint8_t</span> input)</span> </span>&#123;</div><div class="line">    <span class="keyword">uint32_t</span> b;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (input &gt; <span class="number">3</span>) &#123;</div><div class="line">        <span class="keyword">return</span>;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    b = input;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>要这么写代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">uint8_t</span> input)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (input &gt; <span class="number">3</span>) &#123;</div><div class="line">        <span class="keyword">return</span>;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">uint32_t</span> b = input;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>警告：如果代码中有紧密的循环，要检查你初始化的位置。有时分散的声明可能会导致意外的性能变差。对于常规非快速路径代码（这是普遍情况），变量定义最好尽可能清晰，并且将定义放到初始化语句旁边能很大提高代码可读性。</p>
<h2 id="C99-允许在-for-循环中定义计数器"><a href="#C99-允许在-for-循环中定义计数器" class="headerlink" title="C99 允许在 for 循环中定义计数器"></a>C99 允许在 <em>for</em> 循环中定义计数器</h2><p>所以，不要这样写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">uint32_t</span> i;</div><div class="line"></div><div class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)</div></pre></td></tr></table></figure>
<p>要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)</div></pre></td></tr></table></figure>
<p>例外的情况是：当你需要在循环结束时还要复用计数器，显然你不能把计数器定义在循环作用域内部。</p>
<h2 id="现代的编译器支持-pragma-once"><a href="#现代的编译器支持-pragma-once" class="headerlink" title="现代的编译器支持 #pragma once"></a>现代的编译器支持 <em>#pragma once</em></h2><p>所以，不要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> PROJECT_HEADERNAME</span></div><div class="line"><span class="meta">#<span class="meta-keyword">define</span> PROJECT_HEADERNAME</span></div><div class="line">.</div><div class="line">.</div><div class="line">.</div><div class="line"><span class="meta">#<span class="meta-keyword">endif</span> <span class="comment">/* PROJECT_HEADERNAME */</span></span></div></pre></td></tr></table></figure>
<p>要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="meta">#<span class="meta-keyword">pragma</span> once</span></div></pre></td></tr></table></figure>
<p><code>#pragma once</code> 告诉编译器只引入头文件一次，你不再需要用头文件中的三行预处理命令来确保。pragma 预处理命令已经被所有平台所有编译器支持，推荐使用。</p>
<p>更多细节，参见 <a href="https://en.wikipedia.org/wiki/Pragma_once" target="_blank" rel="external">pragma once</a> 中支持的预处理器列表。</p>
<h2 id="C-允许静态自动分配初始化数组"><a href="#C-允许静态自动分配初始化数组" class="headerlink" title="C 允许静态自动分配初始化数组"></a>C 允许静态自动分配初始化数组</h2><p>所以，不要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">uint32_t</span> numbers[<span class="number">64</span>];</div><div class="line">    <span class="built_in">memset</span>(numbers, <span class="number">0</span>, <span class="keyword">sizeof</span>(numbers));</div></pre></td></tr></table></figure>
<p>这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">uint32_t</span> numbers[<span class="number">64</span>] = &#123;<span class="number">0</span>&#125;;</div></pre></td></tr></table></figure>
<h2 id="C-允许静态自动分配初始化结构体"><a href="#C-允许静态自动分配初始化结构体" class="headerlink" title="C 允许静态自动分配初始化结构体"></a>C 允许静态自动分配初始化结构体</h2><p>所以，不要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thing</span> &#123;</span></div><div class="line">        <span class="keyword">uint64_t</span> index;</div><div class="line">        <span class="keyword">uint32_t</span> counter;</div><div class="line">    &#125;;</div><div class="line"></div><div class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">thing</span> <span class="title">localThing</span>;</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">initThing</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</div><div class="line">        <span class="built_in">memset</span>(&amp;localThing, <span class="number">0</span>, <span class="keyword">sizeof</span>(localThing));</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thing</span> &#123;</span></div><div class="line">    <span class="keyword">uint64_t</span> index;</div><div class="line">    <span class="keyword">uint32_t</span> counter;</div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thing</span> <span class="title">localThing</span> = &#123;</span><span class="number">0</span>&#125;;</div></pre></td></tr></table></figure>
<p>   <strong>重要提示</strong>：如果结构体有填充，<code>{0}</code> 将不能把多余的填充字节置为零。举例来说，<code>stuct thing</code> 在 <code>counter</code> 之后有4字节填充（在64位平台），因为结构体被按照字节大小来填充。如果需要使整个结构体置为零，包括未使用的填充字节，使用 <code>memset(&amp;localThing, 0, sizeof(localThing))</code>，因为<code>sizeof(localThing) == 16 bytes</code>，即使可寻址内容只有 <code>8 + 4 = 12 bytes</code>。</p>
<p>如果需要重新初始化一个已经分配内存的结构体，可以定义一个全局的空结构体，然后赋值：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thing</span> &#123;</span></div><div class="line">        <span class="keyword">uint64_t</span> index;</div><div class="line">        <span class="keyword">uint32_t</span> counter;</div><div class="line">    &#125;;</div><div class="line"></div><div class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">thing</span> <span class="title">localThingNull</span> = &#123;</span><span class="number">0</span>&#125;;</div><div class="line">    .</div><div class="line">    .</div><div class="line">    .</div><div class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">thing</span> <span class="title">localThing</span> = &#123;</span>.counter = <span class="number">3</span>&#125;;</div><div class="line">    .</div><div class="line">    .</div><div class="line">    .</div><div class="line">    localThing = localThingNull;</div></pre></td></tr></table></figure>
<p>如果你运气不错在C99（或更新）环境，可以使用复合字面量来代替保存一个全局空结构体。（参见2001年的 <a href="http://www.drdobbs.com/the-new-c-compound-literals/184401404" target="_blank" rel="external">The New C: Compound Literals</a>）。</p>
<p>复合字面量允许你的编译器自动的创建临时匿名结构体，然后复制它们给一个目标值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line">localThing = (struct thing)&#123;<span class="number">0</span>&#125;;</div></pre></td></tr></table></figure>
<h2 id="C99-增加可变长数组支持（C11-将其设置为可选）"><a href="#C99-增加可变长数组支持（C11-将其设置为可选）" class="headerlink" title="C99 增加可变长数组支持（C11 将其设置为可选）"></a>C99 增加可变长数组支持（C11 将其设置为可选）</h2><p>所以，不要这么做（如果你知道你的数组很小，或者只是在做一个快速测试代码）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">uintmax_t</span> arrayLength = strtoumax(argv[<span class="number">1</span>], <span class="literal">NULL</span>, <span class="number">10</span>);</div><div class="line">  <span class="keyword">void</span> *<span class="built_in">array</span>[];</div><div class="line"></div><div class="line">  <span class="built_in">array</span> = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(*<span class="built_in">array</span>) * arrayLength);</div><div class="line"></div><div class="line">  <span class="comment">/* remember to free(array) when you're done using it */</span></div></pre></td></tr></table></figure>
<p>要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">uintmax_t</span> arrayLength = strtoumax(argv[<span class="number">1</span>], <span class="literal">NULL</span>, <span class="number">10</span>);</div><div class="line">    <span class="keyword">void</span> *<span class="built_in">array</span>[arrayLength];</div><div class="line"></div><div class="line">    <span class="comment">/* no need to free array */</span></div></pre></td></tr></table></figure>
<p><strong>重要警告</strong>：可变长数组跟普通数组一样（通常）是分配在栈上的。如果你不能静态的创建一个300w个元素的常规数组，那么也不要在运行时用这个语法尝试创建一个300w个元素的数组。它们不是可扩展的 python/ruby 的自增长列表。如果你定义了一个运行时的数组，并且数组长度对于当前栈太大，应用程序将会发生可怕的事情（崩溃、安全问题）。可变数组适合长度小、单一用途场景，但是不应该在生产软件时大规模应用。如果有时，你需要一个3个元素的数组，其他时候需要300w元素的数组，绝不要使用可变长数组。</p>
<p>万一你遇见VLA，知道它是 VLA（variable length arrays 可变长数组）语法还是有用的（或者想做快速一次性测试）。但是它经常被认为是 <a href="https://twitter.com/grynspan/status/685509158024691712" target="_blank" rel="external">危险反模式</a>，因为你可以非常简单的让你的程序崩溃，忘记检查元素长度边界，或者是忘记你正在一个没有剩余栈空间的奇怪的目标平台。</p>
<p>注意：必须要确保 <code>arrayLength</code> 在一个合理的大小。（例如，小于几KB，有时在某些平台，最大栈大小只有4KB）。你不能在栈上分配巨大的数组（百万级别），但是如果你知道是有限制的大小，使用 <a href="https://en.wikipedia.org/wiki/Variable-length_array" target="_blank" rel="external">C99 VLA</a> 就相对于人工在堆上请求内存会更加方便。</p>
<p>更加需要注意的是：上面的代码中没有输入检查，所以用户可以通过分配一个巨大的可变长数组，让应用程序崩溃。<a href="https://twitter.com/comex/status/685423016981966848" target="_blank" rel="external">有些人</a>声称可变长数组是反模式，但是如果你能够加强边界检查，它在一些场景下是可以略胜一筹的。</p>
<h2 id="C99-允许标注非重叠指针参数"><a href="#C99-允许标注非重叠指针参数" class="headerlink" title="C99 允许标注非重叠指针参数"></a>C99 允许标注非重叠指针参数</h2><p>参见 <a href="https://en.wikipedia.org/wiki/Restrict" target="_blank" rel="external">restrict keyword</a> （通常为 <code>__restrict</code>）</p>
<h2 id="参数类型"><a href="#参数类型" class="headerlink" title="参数类型"></a>参数类型</h2><p>如果一个函数接受任意的输入数据和将要执行的长度，则不要限制参数类型。</p>
<p>所以，不要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> <span class="title">processAddBytesOverflow</span><span class="params">(<span class="keyword">uint8_t</span> *bytes, <span class="keyword">uint32_t</span> len)</span> </span>&#123;</div><div class="line">    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</div><div class="line">        bytes[<span class="number">0</span>] += bytes[i];</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>要这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> <span class="title">processAddBytesOverflow</span><span class="params">(<span class="keyword">void</span> *input, <span class="keyword">uint32_t</span> len)</span> </span>&#123;</div><div class="line">    <span class="keyword">uint8_t</span> *bytes = input;</div><div class="line"></div><div class="line">    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</div><div class="line">        bytes[<span class="number">0</span>] += bytes[i];</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>函数的输入类型描述了代码的接口，而不是代码如何处理这些参数。上面代码中的接口表示“接受一个字节数组和数组的长度”，所以不用限制调用者只能传入 uint8_t 字节流。或许用户甚至希望能够传入更老的 <code>char *</code> 类型或者其他不能预期的值。</p>
<p>通过声明输入类型为 <code>void *</code>，然后在函数内部重新赋值，重新转换成期望实际类型，可以减少函数调用者对函数内部抽象的猜想。</p>
<p>一些读者已经指出对齐问题，但是我们正在访问输入的单字节元素，所以没问题。如果我们要转换输入为更宽的类型，我们就需要注意对齐问题了。对于处理跨平台对齐问题的不同的详细描述，参考 <a href="https://www.kernel.org/doc/Documentation/unaligned-memory-access.txt" target="_blank" rel="external">Unaligned Memory Access</a>（未对齐内存访问）。（提醒：该文章的主要内容不实关于C语言跨硬件架构的复杂性，因此要完全理解示例，需要一些外部知识和经验。）</p>
<h2 id="返回参数类型"><a href="#返回参数类型" class="headerlink" title="返回参数类型"></a>返回参数类型</h2><p>C99 提供了强大的头文件 <code>&lt;stdbool.h&gt;</code>，其中定义了 <code>true</code> 是 <code>1</code>， <code>false</code> 是 <code>0</code> 。</p>
<p>对于成功／失败的返回类型，函数需要返回 <code>true</code> 或 <code>false</code>，而不是人为的指定一个<code>1</code>或<code>0</code>的 <code>int32_t</code> 的返回类型（或者更糟糕，<code>1</code> 和 <code>-1</code>（或 <code>0</code> 代表成功，<code>1</code> 代表失败？或 <code>0</code> 代表成功，<code>-1</code> 代表失败？））。</p>
<p>如果函数会在参数无效的范围内修改了输入参数，而不是返回修改后的指针。那么应该将整个API中可能会被修改成无效的参数都强制使用双指针。在大规模使用中，用“对于某些接口，返回值使输入无效”的方式写代码，非常容易出错。</p>
<p>因此，不要这么写代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> *<span class="title">growthOptional</span><span class="params">(<span class="keyword">void</span> *grow, <span class="keyword">size_t</span> currentLen, <span class="keyword">size_t</span> newLen)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (newLen &gt; currentLen) &#123;</div><div class="line">        <span class="keyword">void</span> *newGrow = <span class="built_in">realloc</span>(grow, newLen);</div><div class="line">        <span class="keyword">if</span> (newGrow) &#123;</div><div class="line">            <span class="comment">/* resize success */</span></div><div class="line">            grow = newGrow;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            <span class="comment">/* resize failed, free existing and signal failure through NULL */</span></div><div class="line">            <span class="built_in">free</span>(grow);</div><div class="line">            grow = <span class="literal">NULL</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">return</span> grow;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>应该这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* Return value:</span></div><div class="line"><span class="comment">*  - 'true' if newLen &gt; currentLen and attempted to grow</span></div><div class="line"><span class="comment">*    - 'true' does not signify success here, the success is still in '*_grow'</span></div><div class="line"><span class="comment">*  - 'false' if newLen &lt;= currentLen */</span></div><div class="line"><span class="function"><span class="keyword">bool</span> <span class="title">growthOptional</span><span class="params">(<span class="keyword">void</span> **_grow, <span class="keyword">size_t</span> currentLen, <span class="keyword">size_t</span> newLen)</span> </span>&#123;</div><div class="line">    <span class="keyword">void</span> *grow = *_grow;</div><div class="line">    <span class="keyword">if</span> (newLen &gt; currentLen) &#123;</div><div class="line">        <span class="keyword">void</span> *newGrow = <span class="built_in">realloc</span>(grow, newLen);</div><div class="line">        <span class="keyword">if</span> (newGrow) &#123;</div><div class="line">            <span class="comment">/* resize success */</span></div><div class="line">            *_grow = newGrow;</div><div class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="comment">/* resize failure */</span></div><div class="line">        <span class="built_in">free</span>(grow);</div><div class="line">        *_grow = <span class="literal">NULL</span>;</div><div class="line"></div><div class="line">        <span class="comment">/* for this function,</span></div><div class="line"><span class="comment">         * 'true' doesn't mean success, it means 'attempted grow' */</span></div><div class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>或者，更好的方式是这么写：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><div class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span> growthResult &#123;</div><div class="line">    GROWTH_RESULT_SUCCESS = <span class="number">1</span>,</div><div class="line">    GROWTH_RESULT_FAILURE_GROW_NOT_NECESSARY,</div><div class="line">    GROWTH_RESULT_FAILURE_ALLOCATION_FAILED</div><div class="line">&#125; growthResult;</div><div class="line"></div><div class="line"><span class="function">growthResult <span class="title">growthOptional</span><span class="params">(<span class="keyword">void</span> **_grow, <span class="keyword">size_t</span> currentLen, <span class="keyword">size_t</span> newLen)</span> </span>&#123;</div><div class="line">    <span class="keyword">void</span> *grow = *_grow;</div><div class="line">    <span class="keyword">if</span> (newLen &gt; currentLen) &#123;</div><div class="line">        <span class="keyword">void</span> *newGrow = <span class="built_in">realloc</span>(grow, newLen);</div><div class="line">        <span class="keyword">if</span> (newGrow) &#123;</div><div class="line">            <span class="comment">/* resize success */</span></div><div class="line">            *_grow = newGrow;</div><div class="line">            <span class="keyword">return</span> GROWTH_RESULT_SUCCESS;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="comment">/* resize failure, don't remove data because we can signal error */</span></div><div class="line">        <span class="keyword">return</span> GROWTH_RESULT_FAILURE_ALLOCATION_FAILED;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">return</span> GROWTH_RESULT_FAILURE_GROW_NOT_NECESSARY;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h2><p>代码风格非常重要，同时又完全没什么价值。</p>
<p>如果你的项目有50页的代码风格指南，没人会帮助你。但是，如果你的代码可读性很差，也没人想要去帮助你。</p>
<p>通常的解决方法是使用自动的代码格式化工具。</p>
<p>2016年唯一可用的C格式化工具是 <a href="http://clang.llvm.org/docs/ClangFormat.html" target="_blank" rel="external">clang-format</a>。clang-format 拥有最好的自动C格式化的默认参数，并且现在仍然处于活跃的开发阶段。</p>
<p>下面是我个人惯用的运行clang-format 的脚本，包含一些不错的参数：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="meta">#!/usr/bin/env bash</span></div><div class="line"></div><div class="line">clang-format -style=<span class="string">"&#123;BasedOnStyle: llvm, IndentWidth: 4, AllowShortFunctionsOnASingleLine: None, KeepEmptyLinesAtTheStartOfBlocks: false&#125;"</span> <span class="string">"<span class="variable">$@</span>"</span></div></pre></td></tr></table></figure>
<p>然后调用这个脚本（假设这个脚本文件命名为 <code>cleanup-format</code>）：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><div class="line">matt@foo:~/repos/badcode% cleanup-format -i *.&#123;c,h,cc,cpp,hpp,cxx&#125;</div></pre></td></tr></table></figure>
<p>参数 -i 会将格式化后的内容覆盖原文件，而不是写入新文件或创建备份文件。</p>
<p>如果有很多文件，你可以并行的递归处理整个源码树：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="meta">#!/usr/bin/env bash</span></div><div class="line"></div><div class="line"><span class="comment"># <span class="doctag">note:</span> clang-tidy only accepts one file at a time, but we can run it</span></div><div class="line"><span class="comment">#       parallel against disjoint collections at once.</span></div><div class="line">find . \( -name \*.c -or -name \*.cpp -or -name \*.cc \) |xargs -n1 -P4 cleanup-tidy</div><div class="line"></div><div class="line"><span class="comment"># clang-format accepts multiple files during one run, but let's limit it to 12</span></div><div class="line"><span class="comment"># here so we (hopefully) avoid excessive memory usage.</span></div><div class="line">find . \( -name \*.c -or -name \*.cpp -or -name \*.cc -or -name \*.h \) |xargs -n12 -P4 cleanup-format -i</div></pre></td></tr></table></figure>
<p>现在，有一个新的 cleanup-tidy 脚本，内容如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="meta">#!/usr/bin/env bash</span></div><div class="line"></div><div class="line">clang-tidy \</div><div class="line">    -fix \</div><div class="line">    -fix-errors \</div><div class="line">    -header-filter=.* \</div><div class="line">    --checks=readability-braces-around-statements,misc-macro-parentheses \</div><div class="line">    <span class="variable">$1</span> \</div><div class="line">    -- -I.</div></pre></td></tr></table></figure>
<p><a href="http://clang.llvm.org/extra/clang-tidy/" target="_blank" rel="external">clang-tidy</a> 是策略驱动的代码重构工具，上面示例代码中开启了俩个修复项：</p>
<ul>
<li><code>readability-braces-around-statements</code> - 强制 <code>if / while / for</code> 所有语句都使用大括号包起来<ul>
<li>C 允许循环和条件后的单语句用“可选大括号”是一个历史事故。现在写代码时，循环和条件语句之后不使用大括号是不可原谅的事情。不要用“但是，编译器允许啊！”的借口争辩，对于代码可读性、可维护性、可理解性和可跳过性没有任何好处。你写代码不是为了取悦编译器，而是为了将来维护你代码的人，那时他们并不知道当时你为什么会存在这样的代码。</li>
</ul>
</li>
<li><code>misc-macro-parentheses</code> - 自动为宏中使用的所有参数加上括号。</li>
</ul>
<p><code>clang-tidy</code> 非常好用，但是对于一些复杂的代码可能会有问题。还有，<code>clang-tidy</code> 并没有做格式化的工作，所以你需要在整理代码之后，运行 <code>clang-format</code> 来整理新的大括号和重新推到宏。</p>
<h2 id="可读性"><a href="#可读性" class="headerlink" title="可读性"></a>可读性</h2><p>这里开始，写作速度好像慢下来了……</p>
<h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p>代码逻辑应该包含在代码文件中。</p>
<h3 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h3><p>源码文件尽量限制行数在1000行以内（1500行就已经是很糟糕的情况了）。如果也包含在原文件中（为了测试静态函数等），尽量调整这种情况。</p>
<h2 id="杂项的想法"><a href="#杂项的想法" class="headerlink" title="杂项的想法"></a>杂项的想法</h2><h3 id="永远不要使用-malloc"><a href="#永远不要使用-malloc" class="headerlink" title="永远不要使用 malloc"></a>永远不要使用 <code>malloc</code></h3><p>尽量使用 <code>calloc</code>，获取零内存没有性能损失。如果你不喜欢 <code>calloc(object count, size per object)</code> 的函数原型，可以封装下 <code>#define mycalloc(N) calloc(1, N)</code>。</p>
<p>摘取读者的一些评论如下：</p>
<ul>
<li><code>calloc</code> 申请巨大内存时，会有性能影响。</li>
<li><code>calloc</code> 在一些奇怪的平台有性能问题（最小的嵌入式系统、游戏机、30年前的老硬件）。</li>
<li>总是封装 <code>calloc(element count, size of each element)</code> 不是个好主意。</li>
<li>避免使用 <code>malloc</code> 的一个不错的理由是它不做整型溢出检查，这是个潜在安全问题。</li>
<li><code>calloc</code> 分配内存能够避免 valgrind 对于潜在的读或复制未初始化内存时的警告，因为分配内存时会自动初始化为0。</li>
</ul>
<p>以上都是非常好的点，然后我们还必须要做性能测试和回归测试，以保证跨编译器、平台、操作系统和硬件设备上的性能。</p>
<p>不像 <code>malloc()</code>，<code>calloc()</code> 可以检查整型溢出是它的一个优势，因为它会将其参数做乘法，以确认最终需要分配的内存大小。如果只是需要分配很小的内存，对 <code>calloc()</code> 封装没有问题。如果是药分配潜在无边界的数据流数据的内存，你可能需要使用原生的常规方法 <code>calloc(element count, size of each element)</code>，以方便实用。</p>
<p>没有任何建议是万事皆准的，但是试图给出完美的通用建议，最终会像读一本语言说明文档。</p>
<p>有关 <code>calloc()</code> 如何为你干净内存的参考，可以阅读以下文章：</p>
<ul>
<li><a href="https://blogs.fau.de/hager/archives/825" target="_blank" rel="external">Benchmarking fun with calloc() and zero pages (2007)</a></li>
<li><a href="https://en.wikipedia.org/wiki/Copy-on-write#Copy-on-write_in_virtual_memory_management" target="_blank" rel="external">Copy-on-write in virtual memory management</a> </li>
</ul>
<p>在2016年的大部分场景下，我仍旧坚持推荐使用 <code>calloc()</code> （假设：x64目标的平台，人性化的数据，不包括人类基因数量级别的数据）。任何与“期望”的偏离都会让我们陷入“领域知识”的绝望之中，这不是我们当下要讨论的问题。</p>
<p>子注：<code>calloc()</code> 传递给你的预先清零的内存是一次性处理的。如果使用 <code>realloc()</code> 来扩展你使用 <code>calloc()</code> 分配的内存，扩展的内存是没有清零的内存。如果需要将 realloc 分配的内存清零，必须要针对扩展的内存手动的调用 <code>memset()</code> 。</p>
<h3 id="永远不要使用-memset（如果你可以避免的话）"><a href="#永远不要使用-memset（如果你可以避免的话）" class="headerlink" title="永远不要使用 memset（如果你可以避免的话）"></a>永远不要使用 memset（如果你可以避免的话）</h3><p>当你能够静态的初始化一个结构体（或数组）为零（或者通过内连复合字面量赋值它为零，或者通过结构体外的一个预先置零全局变量赋值），就不要使用 <code>memset(ptr, 0, len)</code>。</p>
<p>尽管如此，如果你需要将结构体包括他的填充字节置零，<code>memset()</code> 是唯一的方法（因为 <code>{0}</code> 只能设置定义的字段，而不能填充未定义的偏移量）。</p>
<h2 id="了解更多"><a href="#了解更多" class="headerlink" title="了解更多"></a>了解更多</h2><p>参见 <a href="http://en.cppreference.com/w/c/types/integer" target="_blank" rel="external">Fixed width integer types (since C99)</a></p>
<p>参见苹果的  <a href="https://developer.apple.com/library/mac/documentation/Darwin/Conceptual/64bitPorting/MakingCode64-BitClean/MakingCode64-BitClean.html" target="_blank" rel="external">Making Code 64-Bit Clean</a></p>
<p>参见 sizes of <a href="https://www.securecoding.cert.org/confluence/pages/viewpage.action?pageId=4374" target="_blank" rel="external">C types across architectures</a>  除非你能为了每一行代码记住那么长的表的内容，需要明确定义整型的宽度，绝对不要使用 char/short/int/long 这些内置存储类型。</p>
<p>参见 <a href="http://www.viva64.com/en/a/0050/" target="_blank" rel="external">size_t and ptrdiff_t</a></p>
<p>参见 <a href="https://www.securecoding.cert.org/confluence/display/c/SEI+CERT+C+Coding+Standard" target="_blank" rel="external">Secure Coding</a>。如果你真心希望能够写出完美的代码，只需要简单记住其中上千个简单示例。</p>
<p>参见来自 Inria 的 Jens Gustedt 编写的 <a href="http://icube-icps.unistra.fr/img_auth.php/d/db/ModernC.pdf" target="_blank" rel="external">Modern C</a>。</p>
<p>参见 <a href="http://www.dotslashzero.net/2014/08/22/understanding-characterstring-literals-in-cc/" target="_blank" rel="external">Understanding Character/String Literals in C/C++</a> 了解更多在C11中的 Unicode 支持的内容。</p>
<h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>大规模的编写正确的代码本质上是不可能的。我们有多种炒作系统、运行时环境、程序库和硬件平台要考虑，更不用说像RAM中的随机位反转和块设备故障这些未知的问题。</p>
<p>我们能做的最好的是编写简单、易懂的代码，尽量减少间接代码和未注视的魔术代码。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;英文原文：&lt;a href=&quot;https://matt.sh/howto-c&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://matt.sh/howto-c&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;这原本是一篇我写在 2015 年没有发布出来的草稿。为了让大家也
    
    </summary>
    
      <category term="C/C++" scheme="http://blog.100dos.com/categories/C-C/"/>
    
    
      <category term="C/C++" scheme="http://blog.100dos.com/tags/C-C/"/>
    
  </entry>
  
  <entry>
    <title>商城抢购/秒杀模块设计</title>
    <link href="http://blog.100dos.com/2016/10/27/seckill-on-e-shop/"/>
    <id>http://blog.100dos.com/2016/10/27/seckill-on-e-shop/</id>
    <published>2016-10-27T10:37:48.000Z</published>
    <updated>2017-08-31T02:52:15.076Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>抢购是指在某一个时间段内商家低价甚至免费销售商品，消费者突发性的集中投入购买力，大批量的、超常规的购买某种或某类商品的现象。</p>
</blockquote>
<p>总结来说有以下俩个特点：</p>
<ul>
<li>商品数量有限，比如设置此次抢购一共1000个商品，抢完即结束</li>
<li>时间受限，比如会定义从何时开始，到何时结束</li>
</ul>
<p>14年的8月份，墨迹商城做了几次抢一毛钱抢毛巾、后续的每周蓝色星期二是抢空气果的抢购活动，当时非常火爆。墨迹本身的用户量就很大（目前有6亿用户），而且之前做了很久的宣传。大量的用户（想占便宜抢一毛钱毛巾的用户和对我们当时的新产品空气果发烧的用户）一拥而入。1毛钱抢毛巾的活动，一瞬间 1 万条毛巾就被抢光了。可以想象当时的疯狂程度……</p>
<h3 id="简单抢购需求描述／抢购流程描述"><a href="#简单抢购需求描述／抢购流程描述" class="headerlink" title="简单抢购需求描述／抢购流程描述"></a>简单抢购需求描述／抢购流程描述</h3><p>1.商品详情页显示商品信息。没有开始的抢购之前，抢购按钮为灰色且不可点击。按钮显示提示文字“马上开抢”，并显示抢购开始倒计时间；<br>2.抢购倒计时结束，抢购按钮方可点击，并显示“抢购”；<br>3.用户抢购商品成功，跳转到确认订单页面，完成下单、支付流程；<br>4.抢购失败，跳转到抢购失败页面；<br>5.已抢光的商品显示“已抢光”，抢购时间结束的商品显示“已结束”；<br>6.抢购流程结束。</p>
<h3 id="根据需求我们需要解决以下几个问题："><a href="#根据需求我们需要解决以下几个问题：" class="headerlink" title="根据需求我们需要解决以下几个问题："></a>根据需求我们需要解决以下几个问题：</h3><p>1、抢购之前用户会不停的刷新商品详情页面，怎么办？</p>
<p>答：商品详情页面静态化，包括页面中引用的静态文件、图片等都缓存到CDN节点上。至于页面中显示的倒计时，由于要抢购时间同步，所以不能使用客户端的时间。服务器完成一个简单时间接口即可，返回Json格式时间。单机 1w以上 QPS 应该没什么问题，因为本身不涉及复杂业务逻辑。在自己的 Mac Pro上测试，用 Openresty 实现的获取系统时间，能做到 7157 的QPS，服务器性一定会能更高。</p>
<p>2、抢购开始倒计时结束，大量用户同时抢购商品，如何应对流量暴增，是平实的几倍、几十倍？</p>
<p>答：肯定不能让大量的并发请求（并且是写请求）直接落到 Mysql，我们考虑用队列来实现。优先写入抢购队列，然后慢慢的写入Mysql，这样一台数据库服务器也就就够了。</p>
<p><img src="/images/seckill-on-e-shop/1.png" alt=""></p>
<p>抢购／秒杀，手快就能抢到，手慢就抢不到。所以为了减轻服务器的压力，也给用户好的体验。对于如果队列中抢购请求过多，远远超过抢购商品数的情况下，将直接返回“抢购失败”给用户。比如这次一共抢购100件商品，此时队列中已经有了10w个抢购请求。为了用户体验，再进来的抢购请求直接返回抢购失败。不让用户等待太久的时间（即使进入队列，很长时间才执行到，也早早的就已经没有可抢的商品了）。</p>
<p>3、如何限制抢购商品数量（库存）？</p>
<p>答：需要记录一个商品本次抢购的个数 N。每当从队列透过一个抢购请求到 DB，则 N - 1，直到 N = 0 商品被抢光。且为了保持系统简单。有用户抢购成功，但是不付款的情况，也不再考虑从新“回仓”的问题。如果考虑回仓的话，比如半个小时以后用户仍没有付款，则视为用户放弃购买这次抢购的商品。商品库存 +1，这时再去抢购仍然能抢到。就像12306抢车票设置的是45分钟后不付款，回仓，如果是老司机这个时候回去看看兴许真的还能买到票。</p>
<h3 id="详细设计"><a href="#详细设计" class="headerlink" title="详细设计"></a>详细设计</h3><p>略……</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;抢购是指在某一个时间段内商家低价甚至免费销售商品，消费者突发性的集中投入购买力，大批量的、超常规的购买某种或某类商品的现象。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;总结来说有以下俩个特点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;商品数量有限，比如设置此次抢
    
    </summary>
    
      <category term="架构/系统设计" scheme="http://blog.100dos.com/categories/%E6%9E%B6%E6%9E%84-%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/"/>
    
    
      <category term="抢购" scheme="http://blog.100dos.com/tags/%E6%8A%A2%E8%B4%AD/"/>
    
  </entry>
  
  <entry>
    <title>CentOS 6.6 安装 mysql 5.7</title>
    <link href="http://blog.100dos.com/2016/10/14/centsos6.6-mysql5.7-install/"/>
    <id>http://blog.100dos.com/2016/10/14/centsos6.6-mysql5.7-install/</id>
    <published>2016-10-14T04:05:45.000Z</published>
    <updated>2017-08-31T02:52:15.072Z</updated>
    
    <content type="html"><![CDATA[<p>在 CentOS 的 yum 默认安装的是 mysql 5.5 版本，由于开发需要 5.6 以上版本，索性就装个 5.7。</p>
<p>到官网下载对应的 rpm buddle 包，CentOS 6 选择选择如下对应的版本</p>
<blockquote>
<p><em>Red Hat Enterprise Linux 6 / Oracle Linux 6 (x86, 64-bit), RPM Bundle    5.7.15    440.2M</em><br><em>(mysql-5.7.15-1.el6.x86_64.rpm-bundle.tar)</em></p>
</blockquote>
<p>安装前，如果已经安装了 Mysql 5.5， 需要卸载旧版的 Mysql。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><div class="line">[root@v194 ~]# rpm -qa |grep mysql</div><div class="line">mysql-libs-5.1.73-7.el6.x86_64</div><div class="line">mysql-community-libs-5.6.33-2.el6.x86_64</div><div class="line">mysql-5.1.73-7.el6.x86_64</div><div class="line">mysql-community-server-5.6.28-2.el6.x86_64</div><div class="line"></div><div class="line">[root@v194 ~]# rpm -e --nodeps mysql-libs-5.1.73-7.el6.x86_64</div><div class="line">[root@v194 ~]# rpm -e --nodeps mysql-community-libs-5.6.33-2.el6.x86_64 mysql-5.1.73-7.el6.x86_64 mysql-community-server-5.6.28-2.el6.x86_64</div></pre></td></tr></table></figure>
<p>删除旧的Mysql目录，不删会有问题。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[root@v194 ~]# ls  -l  /var/lib|grep mysql</div><div class="line">drwxr-xr-x  5 mysql    mysql    4096 11月 17 2015 mysql</div><div class="line">[root@v194 ~]# rm -rf /var/lib/mysql</div></pre></td></tr></table></figure>
<p>解压安装下载的 bundle 包。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">~# tar xf mysql-5.7.15-1.el6.x86_64.rpm-bundle.tar</div><div class="line">~# cd mysql-5.7.15-1</div></pre></td></tr></table></figure>
<p><strong>按照common-&gt;libs-&gt;client-&gt;server 的顺序安装</strong>。</p>
<p>[root@v194 ~]# rpm -ivh mysql-community-common-5.7.15-1.el6.x86_64.rpm<br>[root@v194 ~]# rpm -ivh mysql-community-libs-5.7.15-1.el6.x86_64.rpm<br>[root@v194 ~]# rpm -ivh mysql-community-client-5.7.15-1.el6.x86_64.rpm<br>[root@v194 ~]# rpm -ivh mysql-community-server-5.7.15-1.el6.x86_64.rpm</p>
<p>启动 mysqld</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[root@v194 ~]# service mysqld start</div></pre></td></tr></table></figure>
<p>找到临时登录密码</p>
<p>Mysql 5.7 的密码不在是默认为空，而是在安装的log中。查找文件内容中的临时密码，密码为 yQO64H%Khrts</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[root@v194 ~]# vim /var/log/mysqld.log</div><div class="line">……</div><div class="line">2016-10-11T07:03:47.910712Z 1 [Note] A temporary password is generated for root@localhost: yQO64H%Khrts</div><div class="line">……</div></pre></td></tr></table></figure>
<p>登录 Mysql 客户端<br>[root@v194 ~]# /usr/bin/mysql -uroot -pyQO64H%Khrts -h127.0.0.1 -P3306</p>
<p>Mysql 5.7 必须要修改root 密码，<strong>并且 root 密码必须包含大写字母、数字和特殊字符</strong>。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">mysql&gt; alter user &apos;root&apos;@&apos;localhost&apos; identified by &apos;123abc!@#testABC&apos;;</div><div class="line">Query OK, 0 rows affected (0.00 sec)</div></pre></td></tr></table></figure>
<p>…………</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在 CentOS 的 yum 默认安装的是 mysql 5.5 版本，由于开发需要 5.6 以上版本，索性就装个 5.7。&lt;/p&gt;
&lt;p&gt;到官网下载对应的 rpm buddle 包，CentOS 6 选择选择如下对应的版本&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;em&gt;R
    
    </summary>
    
      <category term="linux" scheme="http://blog.100dos.com/categories/linux/"/>
    
    
      <category term="mysql" scheme="http://blog.100dos.com/tags/mysql/"/>
    
      <category term="centos" scheme="http://blog.100dos.com/tags/centos/"/>
    
  </entry>
  
</feed>
